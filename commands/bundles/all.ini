[Commands]
1\Name=Canonical Dispatcher
1\Command="
    copyq:
    var CopyQCore = (function () {
      function trim(value) { return String(value == null ? '' : value).replace(/^\\s+|\\s+$/g, ''); }
      function hasFormat(formats, expression) { var i; for (i = 0; i < (formats || []).length; i += 1) if (expression.test(String(formats[i]))) return true; return false; }
      function decode(part) {
        var alphabet = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/';
        var input = part.replace(/-/g, '+').replace(/_/g, '/'); var out = ''; var bits = 0; var bitCount = 0; var i; var n;
        while (input.length % 4) input += '=';
        for (i = 0; i < input.length; i += 1) { if (input.charAt(i) === '=') break; n = alphabet.indexOf(input.charAt(i)); if (n < 0) return null; bits = (bits << 6) | n; bitCount += 6; while (bitCount >= 8) { bitCount -= 8; out += String.fromCharCode((bits >> bitCount) & 255); } }
        return out;
      }
      function validJwt(text) { var parts = text.split('.'); var head; var body; try { head = parts.length === 3 ? JSON.parse(decode(parts[0])) : null; body = parts.length === 3 ? JSON.parse(decode(parts[1])) : null; } catch (e) { return false; } return !!(head && body && typeof head.alg === 'string' && /^[A-Za-z0-9_-]+$/.test(parts[2] || '')); }
      function credentialUrl(text) { return /^https?:\\/\\/[^/\\s:@]+:[^/\\s@]+@/i.test(text) || /[?&](?:access_token|api[_-]?key|password|secret|token)=[^&#\\s]+/i.test(text); }
      function secretFormat(formats) { return hasFormat(formats, /(?:application\\/x-copyq-secret|org\\.nspasteboard\\.concealedtype|clipboard viewer ignore|x-kde-passwordmanagerhint|nspasteboard concealed|mimesecret|com\\.agilebits\\.onepassword|1password|bitwarden|keepass|lastpass|password-manager)/i); }
      function structuredNonSecret(text) {
        return /^https?:\\/\\/\\S+$/i.test(text)
          || /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(text)
          || /^(?:[A-Za-z]:[\\\\/]|\\\\\\\\|\\/)[^\\r\\n]+$/.test(text)
          || /^v?\\d+\\.\\d+\\.\\d+(?:[-+][0-9A-Za-z.-]+)?$/.test(text)
          || /^[A-Z][A-Z0-9]{1,9}-\\d{1,10}$/.test(text);
      }
      function historicalOpaqueSecret(text) {
        var classes = 0;
        if (text.length < 10 || text.length > 3000 || /\\s/.test(text)) return false;
        if (/[a-z]/.test(text)) classes += 1;
        if (/[A-Z]/.test(text)) classes += 1;
        if (/\\d/.test(text)) classes += 1;
        return classes >= 2;
      }
      function isHighConfidenceSecret(value, formats) {
        var text = trim(value);
        if (secretFormat(formats)) return true;
        if (/^-----BEGIN (?:[A-Z0-9 ]+ )?(?:PRIVATE KEY|OPENSSH PRIVATE KEY)-----/m.test(text) || validJwt(text)) return true;
        if (/^(?:sk-[A-Za-z0-9_-]{20,}|ghp_[A-Za-z0-9]{30,}|github_pat_[A-Za-z0-9_]{30,}|glpat-[A-Za-z0-9_-]{20,}|xox[baprs]-[A-Za-z0-9-]{12,}|AIza[0-9A-Za-z_-]{30,}|AKIA[0-9A-Z]{16}|npm_[A-Za-z0-9_-]{20,}|pypi-[A-Za-z0-9_-]{20,})$/.test(text)) return true;
        if (/^authorization:\\s*(?:bearer|basic|token)\\s+\\S{12,}$/i.test(text)) return true;
        if(/^\\s*(?:export\\s+)?(?:[A-Za-z_][A-Za-z0-9_]*(?:PASSWORD|TOKEN|SECRET|API_KEY)[A-Za-z0-9_]*|PASSWORD|TOKEN|SECRET|API_KEY)\\s*=\\s*\\S{12,}\\s*$/i.test(text)) return true;
        if (credentialUrl(text)) return true;
        if (/^[0-9a-f]{32,128}$/i.test(text)) return true;
        if (structuredNonSecret(text)) return false;
        return historicalOpaqueSecret(text);
      }
      function isCode(text) { return /^\\s*(?:import|export|var|const|let|function|class|def|package|using|namespace|#include|#!)\\b/m.test(text) || /(?:=>|===|!==|&&|\\|\\|)/.test(text) || /\\b(?:if|for|while|switch|catch)\\s*\\(/.test(text) || /\\w+\\s*\\([^)]*\\)\\s*\\{/.test(text); }
      function isArtifact(value) {
        var text = trim(value); var parsed; var lines; var matches;
        if (!text) return false;
        if (/^[\\[{]/.test(text)) {
          try {
            parsed = JSON.parse(text);
            if (Object.prototype.toString.call(parsed) === '[object Array]') return parsed.length >= 2;
            if (parsed && Object.prototype.toString.call(parsed) === '[object Object]') return Object.keys(parsed).length >= 2;
          } catch (e) {}
        }
        if (!/[\\r\\n]/.test(text)) return false;
        lines = text.split(/\\r?\\n/).filter(function (line) { return trim(line) !== ''; });
        if (lines.length < 2) return false;
        if (/^diff --git\\s/m.test(text) || /^@@\\s+-\\d+(?:,\\d+)?\\s+\\+\\d+(?:,\\d+)?\\s+@@/m.test(text)) return true;
        if (/---\\s+backtrace\\s+---/i.test(text) || /^Traceback \\(most recent call last\\):/m.test(text)) return true;
        matches = text.match(/^\\s*at\\s+\\S.+$/gm); if (matches && matches.length >= 2) return true;
        if (/^(?:PS\\s+[A-Za-z]:\\\\[^>\\r\\n]*>|[$#>])\\s*\\S/m.test(text)) return true;
        if (/`\\s*$/m.test(text) && /^\\s{2,}-(?:\\w|-)\\S*/m.test(text)) return true;
        matches = text.match(/^(?:\\[[0-2]?\\d:[0-5]\\d:[0-5]\\d\\]|\\d{4}-\\d{2}-\\d{2}[T ][^\\s]+|\\s*(?:TRACE|DEBUG|INFO|WARN|ERROR|FATAL)\\b).+$/gim);
        if (matches && matches.length >= 2) return true;
        if (/^RELEASE_STAGE_FAILED:/m.test(text) && /(?:reason=|backtrace)/i.test(text)) return true;
        matches = text.match(/^\\s*(?:\\[[^\\]\\r\\n]+\\]|[A-Za-z_][A-Za-z0-9_.-]*\\s*(?:=|:)\\s*\\S.*)$/gm);
        return !!(matches && matches.length >= 3);
      }
      function isMarkdown(value) {
        var text = String(value == null ? '' : value); var listItems;
        if (!text) return false;
        if (/(^|\\n) {0,3}#{1,6}[ \\t]+\\S/.test(text)) return true;
        if (/(^|\\n) {0,3}```[^\\n]*\\n[\\s\\S]*\\n {0,3}```/.test(text)) return true;
        if (/\\[[^\\]\\n]+\\]\\((?:https?:\\/\\/|\\/|#|\\.{0,2}\\/)[^)]+\\)/.test(text)) return true;
        if (/(^|\\n) {0,3}>[ \\t]+\\S/.test(text)) return true;
        if (/(^|\\n)\\s*\\|?.+\\|.+\\n\\s*\\|?\\s*:?-{3,}:?\\s*\\|/.test(text)) return true;
        listItems = text.match(/(^|\\n) {0,3}(?:[-+*]|\\d+[.)])[ \\t]+\\S/g);
        return !!(listItems && listItems.length >= 2);
      }
      function fnv1a(text) { var hash = 2166136261; var i; for (i = 0; i < text.length; i += 1) { hash ^= text.charCodeAt(i); hash = (hash * 16777619) >>> 0; } return (hash >>> 0).toString(16); }
      function djb2(text) { var hash = 5381; var i; for (i = 0; i < text.length; i += 1) hash = ((hash << 5) + hash + text.charCodeAt(i)) | 0; return (hash >>> 0).toString(36); }
      function legacyKiloHash(text) { text = String(text == null ? '' : text); return text.length + '_' + djb2(text); }
      function normalizeFrequencyText(text) { return trim(text); }
      function v2HashesFor(text) { text = String(text == null ? '' : text); return { version: 2, first: fnv1a(text), second: djb2(text), key: 'v2:' + fnv1a(text) + ':' + djb2(text) }; }
      function hashesFor(text) { text = normalizeFrequencyText(text); return { version: 3, first: fnv1a(text), second: djb2(text), key: 'v3:' + fnv1a(text) + ':' + djb2(text) }; }
      function cloneCounters(counters) { var copy = {}; var key; for (key in counters || {}) if (Object.prototype.hasOwnProperty.call(counters, key)) copy[key] = { version: counters[key].version, first: counters[key].first, second: counters[key].second, count: counters[key].count, lastUsed: counters[key].lastUsed }; return copy; }
      function pruneCounters(state, maximum) {
        var keys = []; var key;
        for (key in state.counters) if (Object.prototype.hasOwnProperty.call(state.counters, key)) keys.push(key);
        keys.sort(function (a, b) { return (state.counters[a].lastUsed - state.counters[b].lastUsed) || (a < b ? -1 : 1); });
        while (keys.length > maximum) delete state.counters[keys.shift()];
      }
      function recordFrequency(state, v2State, legacy, text) {
        var next = { version: 3, counters: cloneCounters((state || {}).counters), clock: Number((state || {}).clock || 0) };
        var canonicalText = normalizeFrequencyText(text); var hash = hashesFor(canonicalText); var oldHash = v2HashesFor(canonicalText); var oldKey = legacyKiloHash(canonicalText); var entry; var imported = 0;
        if (!canonicalText) return { state: next, result: { count: 0, promote: false, key: '', canonicalText: '', hashes: hash } };
        entry = next.counters[hash.key];
        if (!entry) {
          if (v2State && v2State.counters && v2State.counters[oldHash.key]) imported += Number(v2State.counters[oldHash.key].count || 0);
          if (legacy && legacy[oldKey] !== undefined) imported += Number(legacy[oldKey] || 0);
          entry = { version: 3, first: hash.first, second: hash.second, count: imported, lastUsed: 0 };
        }
        entry.count += 1; entry.lastUsed = ++next.clock; next.counters[hash.key] = entry;
        pruneCounters(next, 4096);
        return { state: next, result: { count: entry.count, promote: entry.count >= 6, key: hash.key, canonicalText: canonicalText, hashes: hash } };
      }
      function dismissFrequency(state, key) {
        var next = { version: 3, counters: cloneCounters((state || {}).counters), clock: Number((state || {}).clock || 0) }; var entry = next.counters[key]; var parts = String(key || '').split(':'); var savedCount = 0;
        if (!entry) entry = { version: 3, first: parts[1] || '', second: parts[2] || '', count: 0, lastUsed: 0 };
        savedCount = Number(entry.count || 0); entry.count = 0; entry.lastUsed = ++next.clock; next.counters[key] = entry; pruneCounters(next, 4096);
        return { state: next, savedCount: savedCount };
      }
      function restoreFrequency(state, key, savedCount) {
        var next = { version: 3, counters: cloneCounters((state || {}).counters), clock: Number((state || {}).clock || 0) }; var parts = String(key || '').split(':'); var entry = next.counters[key];
        if (!entry) entry = { version: 3, first: parts[1] || '', second: parts[2] || '', count: 0, lastUsed: 0 };
        entry.count = Number(entry.count || 0) + Number(savedCount || 0); entry.lastUsed = ++next.clock; next.counters[key] = entry; pruneCounters(next, 4096);
        return { state: next, count: entry.count };
      }
      function isTrashExpired(deletedAt, now, retentionMs) {
        var deleted = Date.parse(String(deletedAt || ''));
        return isFinite(deleted) && Number(now) - deleted >= Number(retentionMs);
      }
      function route(input) { var text = String(input && input.text != null ? input.text : ''); var formats = input && input.formats || []; var eligible = normalizeFrequencyText(text) !== ''; if (hasFormat(formats, /^image\\//i) || (input && input.mimeOwner && hasFormat(formats, new RegExp('^' + input.mimeOwner.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&') + '$'))) || (input && input.mimeHidden && hasFormat(formats, new RegExp('^' + input.mimeHidden.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&') + '$')))) return { action: 'passthrough', frequencyEligible: false }; if (isHighConfidenceSecret(text, formats)) return { action: 'ignored', frequencyEligible: false }; if (text.length >= 5000) return { action: 'big', frequencyEligible: eligible }; if (isArtifact(text)) return { action: 'artifacts', frequencyEligible: eligible }; if (isCode(text)) return { action: 'code', frequencyEligible: eligible }; if (/^https?:\\/\\/\\S+$/i.test(text)) return { action: 'passthrough', frequencyEligible: eligible }; return { action: 'default', frequencyEligible: eligible }; }
      function sortFrequent(items) { return items.slice().sort(function (a, b) { return (b.count - a.count) || (a.order - b.order); }); }
      function titleCase(text) { var words = String(text).toLowerCase().split(/(\\s+)/); var minor = { a: 1, an: 1, the: 1, and: 1, but: 1, or: 1, for: 1, nor: 1, as: 1, at: 1, by: 1, from: 1, in: 1, into: 1, near: 1, of: 1, on: 1, onto: 1, to: 1, with: 1 }; var first = -1; var last = -1; var i; for (i = 0; i < words.length; i += 1) if (!/^\\s+$/.test(words[i])) { if (first < 0) first = i; last = i; } for (i = 0; i < words.length; i += 1) if (!/^\\s+$/.test(words[i])) { if (i !== first && i !== last && minor[words[i]]) continue; words[i] = words[i].charAt(0).toUpperCase() + words[i].slice(1); } return words.join('').replace(/\\bId\\b/g, 'ID').replace(/\\bTv\\b/g, 'TV'); }
      function sanitizeHtml(value) { var html = String(value == null ? '' : value); html = html.replace(/\\s(?:color|bgcolor)\\s*=\\s*(?:\"[^\"]*\"|'[^']*'|[^\\s>]+)/gi, ''); return html.replace(/\\sstyle\\s*=\\s*(\"([^\"]*)\"|'([^']*)')/gi, function (match, quoted, doubleQuoted, singleQuoted) { var quote = quoted.charAt(0); var style = doubleQuoted !== undefined ? doubleQuoted : singleQuoted; var declarations = style.split(';').filter(function (declaration) { var name = trim(declaration.split(':')[0]).toLowerCase(); return name && name !== 'color' && name !== 'background' && name !== 'background-color'; }); return declarations.length ? ' style=' + quote + declarations.join(';') + quote : ''; }); }
      function toggleCase(value) { var text = String(value == null ? '' : value); var i; var character; for (i = 0; i < text.length; i += 1) { character = text.charAt(i); if (character !== character.toUpperCase() && character === character.toLowerCase()) return text.toUpperCase(); } return text.toLowerCase(); }
      function validateJsonImport(text) { var parsed; var i; var key; var item; var value; try { parsed = JSON.parse(text); } catch (e) { return { ok: false, reason: 'JSON_PARSE_INVALID' }; } if (!parsed || Object.prototype.toString.call(parsed.copyq_items) !== '[object Array]') return { ok: false, reason: 'JSON_SHAPE_INVALID' }; for (i = 0; i < parsed.copyq_items.length; i += 1) { item = parsed.copyq_items[i]; if (!item || Object.prototype.toString.call(item) !== '[object Object]') return { ok: false, reason: 'JSON_SHAPE_INVALID' }; for (key in item) if (Object.prototype.hasOwnProperty.call(item, key)) { value = item[key]; if (typeof value !== 'string' && !(value && Object.prototype.toString.call(value) === '[object Object]' && typeof value.base64 === 'string')) return { ok: false, reason: 'JSON_SHAPE_INVALID' }; } } return { ok: true, value: parsed }; }
      function validateRegex(text) { try { return { ok: true, value: new RegExp(text, 'i') }; } catch (e) { return { ok: false, reason: 'REGEX_INVALID' }; } }
      function runDependency(execute, args) { var result = execute.apply(null, args || []); return result && result.exit_code === 0 ? { ok: true, value: result } : { ok: false, reason: 'DEPENDENCY_FAILED' }; }
      return { isHighConfidenceSecret: isHighConfidenceSecret, isCode: isCode, isArtifact: isArtifact, isMarkdown: isMarkdown, normalizeFrequencyText: normalizeFrequencyText, hashesFor: hashesFor, v2HashesFor: v2HashesFor, legacyKiloHash: legacyKiloHash, recordFrequency: recordFrequency, dismissFrequency: dismissFrequency, restoreFrequency: restoreFrequency, isTrashExpired: isTrashExpired, route: route, sortFrequent: sortFrequent, titleCase: titleCase, sanitizeHtml: sanitizeHtml, toggleCase: toggleCase, validateJsonImport: validateJsonImport, validateRegex: validateRegex, runDependency: runDependency };
    }());

    var LEGACY_KEY = 'frequent_usage_counts'; var V2_KEY = 'frequent_usage_counts_v2'; var V3_KEY = 'frequent_usage_counts_v3'; var FREQ_KEY_MIME = 'application/x-copyq-user-frequency-key'; var FREQ_COUNT_MIME = 'application/x-copyq-user-frequency-count';
    function settingsObject(key, fallback) { try { var value = settings(key); return value ? JSON.parse(value) : fallback; } catch (e) { return fallback; } }
    var formats = dataFormats(); var textData = data(mimeText); if (!textData) abort(); var text = str(textData);
    var routed = CopyQCore.route({ text: text, formats: formats, mimeOwner: mimeOwner, mimeHidden: mimeHidden });
    if (routed.action === 'passthrough' && !routed.frequencyEligible) abort(); if (routed.action === 'ignored') { notification('.id', 'secret-ignore', '.title', 'Ignoring secret in the clipboard', '.message', 'SECRET_IGNORED'); ignore(); abort(); }
    var frequency = null; if (routed.frequencyEligible) { var state = settingsObject(V3_KEY, { version: 3, counters: {}, clock: 0 }); var v2 = settingsObject(V2_KEY, { version: 2, counters: {}, clock: 0 }); var legacy = settingsObject(LEGACY_KEY, {}); frequency = CopyQCore.recordFrequency(state, v2, legacy, text); settings(V3_KEY, JSON.stringify(frequency.state)); }
    if (frequency && frequency.result.promote) { var originalTab = selectedTab(); var key = frequency.result.key; var frequentItem = {}; frequentItem[mimeText] = frequency.result.canonicalText; frequentItem[FREQ_KEY_MIME] = key; frequentItem[FREQ_COUNT_MIME] = String(frequency.result.count); var escapedKey = key.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&'); tab('Frequent'); var existing = ItemSelection('Frequent').select(new RegExp('^' + escapedKey + '$'), FREQ_KEY_MIME); if (existing.length) { existing.setItemAtIndex(0, frequentItem); existing.move(0); } else { insert(0, frequentItem); } tab(originalTab); }
    if (routed.action === 'big') { setData(mimeOutputTab, 'BIG'); abort(); } if (routed.action === 'artifacts') { setData(mimeOutputTab, 'Artifacts'); abort(); } if (routed.action === 'code') { setData(mimeOutputTab, 'Code'); abort(); } if (routed.action === 'passthrough') abort();"
1\Input=text/plain
1\Automatic=true
1\InternalId=canonical.dispatcher
2\Name=Move to Trash (Undoable)
2\Command="
    var CopyQCore = (function () {
      function trim(value) { return String(value == null ? '' : value).replace(/^\\s+|\\s+$/g, ''); }
      function hasFormat(formats, expression) { var i; for (i = 0; i < (formats || []).length; i += 1) if (expression.test(String(formats[i]))) return true; return false; }
      function decode(part) {
        var alphabet = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/';
        var input = part.replace(/-/g, '+').replace(/_/g, '/'); var out = ''; var bits = 0; var bitCount = 0; var i; var n;
        while (input.length % 4) input += '=';
        for (i = 0; i < input.length; i += 1) { if (input.charAt(i) === '=') break; n = alphabet.indexOf(input.charAt(i)); if (n < 0) return null; bits = (bits << 6) | n; bitCount += 6; while (bitCount >= 8) { bitCount -= 8; out += String.fromCharCode((bits >> bitCount) & 255); } }
        return out;
      }
      function validJwt(text) { var parts = text.split('.'); var head; var body; try { head = parts.length === 3 ? JSON.parse(decode(parts[0])) : null; body = parts.length === 3 ? JSON.parse(decode(parts[1])) : null; } catch (e) { return false; } return !!(head && body && typeof head.alg === 'string' && /^[A-Za-z0-9_-]+$/.test(parts[2] || '')); }
      function credentialUrl(text) { return /^https?:\\/\\/[^/\\s:@]+:[^/\\s@]+@/i.test(text) || /[?&](?:access_token|api[_-]?key|password|secret|token)=[^&#\\s]+/i.test(text); }
      function secretFormat(formats) { return hasFormat(formats, /(?:application\\/x-copyq-secret|org\\.nspasteboard\\.concealedtype|clipboard viewer ignore|x-kde-passwordmanagerhint|nspasteboard concealed|mimesecret|com\\.agilebits\\.onepassword|1password|bitwarden|keepass|lastpass|password-manager)/i); }
      function structuredNonSecret(text) {
        return /^https?:\\/\\/\\S+$/i.test(text)
          || /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(text)
          || /^(?:[A-Za-z]:[\\\\/]|\\\\\\\\|\\/)[^\\r\\n]+$/.test(text)
          || /^v?\\d+\\.\\d+\\.\\d+(?:[-+][0-9A-Za-z.-]+)?$/.test(text)
          || /^[A-Z][A-Z0-9]{1,9}-\\d{1,10}$/.test(text);
      }
      function historicalOpaqueSecret(text) {
        var classes = 0;
        if (text.length < 10 || text.length > 3000 || /\\s/.test(text)) return false;
        if (/[a-z]/.test(text)) classes += 1;
        if (/[A-Z]/.test(text)) classes += 1;
        if (/\\d/.test(text)) classes += 1;
        return classes >= 2;
      }
      function isHighConfidenceSecret(value, formats) {
        var text = trim(value);
        if (secretFormat(formats)) return true;
        if (/^-----BEGIN (?:[A-Z0-9 ]+ )?(?:PRIVATE KEY|OPENSSH PRIVATE KEY)-----/m.test(text) || validJwt(text)) return true;
        if (/^(?:sk-[A-Za-z0-9_-]{20,}|ghp_[A-Za-z0-9]{30,}|github_pat_[A-Za-z0-9_]{30,}|glpat-[A-Za-z0-9_-]{20,}|xox[baprs]-[A-Za-z0-9-]{12,}|AIza[0-9A-Za-z_-]{30,}|AKIA[0-9A-Z]{16}|npm_[A-Za-z0-9_-]{20,}|pypi-[A-Za-z0-9_-]{20,})$/.test(text)) return true;
        if (/^authorization:\\s*(?:bearer|basic|token)\\s+\\S{12,}$/i.test(text)) return true;
        if(/^\\s*(?:export\\s+)?(?:[A-Za-z_][A-Za-z0-9_]*(?:PASSWORD|TOKEN|SECRET|API_KEY)[A-Za-z0-9_]*|PASSWORD|TOKEN|SECRET|API_KEY)\\s*=\\s*\\S{12,}\\s*$/i.test(text)) return true;
        if (credentialUrl(text)) return true;
        if (/^[0-9a-f]{32,128}$/i.test(text)) return true;
        if (structuredNonSecret(text)) return false;
        return historicalOpaqueSecret(text);
      }
      function isCode(text) { return /^\\s*(?:import|export|var|const|let|function|class|def|package|using|namespace|#include|#!)\\b/m.test(text) || /(?:=>|===|!==|&&|\\|\\|)/.test(text) || /\\b(?:if|for|while|switch|catch)\\s*\\(/.test(text) || /\\w+\\s*\\([^)]*\\)\\s*\\{/.test(text); }
      function isArtifact(value) {
        var text = trim(value); var parsed; var lines; var matches;
        if (!text) return false;
        if (/^[\\[{]/.test(text)) {
          try {
            parsed = JSON.parse(text);
            if (Object.prototype.toString.call(parsed) === '[object Array]') return parsed.length >= 2;
            if (parsed && Object.prototype.toString.call(parsed) === '[object Object]') return Object.keys(parsed).length >= 2;
          } catch (e) {}
        }
        if (!/[\\r\\n]/.test(text)) return false;
        lines = text.split(/\\r?\\n/).filter(function (line) { return trim(line) !== ''; });
        if (lines.length < 2) return false;
        if (/^diff --git\\s/m.test(text) || /^@@\\s+-\\d+(?:,\\d+)?\\s+\\+\\d+(?:,\\d+)?\\s+@@/m.test(text)) return true;
        if (/---\\s+backtrace\\s+---/i.test(text) || /^Traceback \\(most recent call last\\):/m.test(text)) return true;
        matches = text.match(/^\\s*at\\s+\\S.+$/gm); if (matches && matches.length >= 2) return true;
        if (/^(?:PS\\s+[A-Za-z]:\\\\[^>\\r\\n]*>|[$#>])\\s*\\S/m.test(text)) return true;
        if (/`\\s*$/m.test(text) && /^\\s{2,}-(?:\\w|-)\\S*/m.test(text)) return true;
        matches = text.match(/^(?:\\[[0-2]?\\d:[0-5]\\d:[0-5]\\d\\]|\\d{4}-\\d{2}-\\d{2}[T ][^\\s]+|\\s*(?:TRACE|DEBUG|INFO|WARN|ERROR|FATAL)\\b).+$/gim);
        if (matches && matches.length >= 2) return true;
        if (/^RELEASE_STAGE_FAILED:/m.test(text) && /(?:reason=|backtrace)/i.test(text)) return true;
        matches = text.match(/^\\s*(?:\\[[^\\]\\r\\n]+\\]|[A-Za-z_][A-Za-z0-9_.-]*\\s*(?:=|:)\\s*\\S.*)$/gm);
        return !!(matches && matches.length >= 3);
      }
      function isMarkdown(value) {
        var text = String(value == null ? '' : value); var listItems;
        if (!text) return false;
        if (/(^|\\n) {0,3}#{1,6}[ \\t]+\\S/.test(text)) return true;
        if (/(^|\\n) {0,3}```[^\\n]*\\n[\\s\\S]*\\n {0,3}```/.test(text)) return true;
        if (/\\[[^\\]\\n]+\\]\\((?:https?:\\/\\/|\\/|#|\\.{0,2}\\/)[^)]+\\)/.test(text)) return true;
        if (/(^|\\n) {0,3}>[ \\t]+\\S/.test(text)) return true;
        if (/(^|\\n)\\s*\\|?.+\\|.+\\n\\s*\\|?\\s*:?-{3,}:?\\s*\\|/.test(text)) return true;
        listItems = text.match(/(^|\\n) {0,3}(?:[-+*]|\\d+[.)])[ \\t]+\\S/g);
        return !!(listItems && listItems.length >= 2);
      }
      function fnv1a(text) { var hash = 2166136261; var i; for (i = 0; i < text.length; i += 1) { hash ^= text.charCodeAt(i); hash = (hash * 16777619) >>> 0; } return (hash >>> 0).toString(16); }
      function djb2(text) { var hash = 5381; var i; for (i = 0; i < text.length; i += 1) hash = ((hash << 5) + hash + text.charCodeAt(i)) | 0; return (hash >>> 0).toString(36); }
      function legacyKiloHash(text) { text = String(text == null ? '' : text); return text.length + '_' + djb2(text); }
      function normalizeFrequencyText(text) { return trim(text); }
      function v2HashesFor(text) { text = String(text == null ? '' : text); return { version: 2, first: fnv1a(text), second: djb2(text), key: 'v2:' + fnv1a(text) + ':' + djb2(text) }; }
      function hashesFor(text) { text = normalizeFrequencyText(text); return { version: 3, first: fnv1a(text), second: djb2(text), key: 'v3:' + fnv1a(text) + ':' + djb2(text) }; }
      function cloneCounters(counters) { var copy = {}; var key; for (key in counters || {}) if (Object.prototype.hasOwnProperty.call(counters, key)) copy[key] = { version: counters[key].version, first: counters[key].first, second: counters[key].second, count: counters[key].count, lastUsed: counters[key].lastUsed }; return copy; }
      function pruneCounters(state, maximum) {
        var keys = []; var key;
        for (key in state.counters) if (Object.prototype.hasOwnProperty.call(state.counters, key)) keys.push(key);
        keys.sort(function (a, b) { return (state.counters[a].lastUsed - state.counters[b].lastUsed) || (a < b ? -1 : 1); });
        while (keys.length > maximum) delete state.counters[keys.shift()];
      }
      function recordFrequency(state, v2State, legacy, text) {
        var next = { version: 3, counters: cloneCounters((state || {}).counters), clock: Number((state || {}).clock || 0) };
        var canonicalText = normalizeFrequencyText(text); var hash = hashesFor(canonicalText); var oldHash = v2HashesFor(canonicalText); var oldKey = legacyKiloHash(canonicalText); var entry; var imported = 0;
        if (!canonicalText) return { state: next, result: { count: 0, promote: false, key: '', canonicalText: '', hashes: hash } };
        entry = next.counters[hash.key];
        if (!entry) {
          if (v2State && v2State.counters && v2State.counters[oldHash.key]) imported += Number(v2State.counters[oldHash.key].count || 0);
          if (legacy && legacy[oldKey] !== undefined) imported += Number(legacy[oldKey] || 0);
          entry = { version: 3, first: hash.first, second: hash.second, count: imported, lastUsed: 0 };
        }
        entry.count += 1; entry.lastUsed = ++next.clock; next.counters[hash.key] = entry;
        pruneCounters(next, 4096);
        return { state: next, result: { count: entry.count, promote: entry.count >= 6, key: hash.key, canonicalText: canonicalText, hashes: hash } };
      }
      function dismissFrequency(state, key) {
        var next = { version: 3, counters: cloneCounters((state || {}).counters), clock: Number((state || {}).clock || 0) }; var entry = next.counters[key]; var parts = String(key || '').split(':'); var savedCount = 0;
        if (!entry) entry = { version: 3, first: parts[1] || '', second: parts[2] || '', count: 0, lastUsed: 0 };
        savedCount = Number(entry.count || 0); entry.count = 0; entry.lastUsed = ++next.clock; next.counters[key] = entry; pruneCounters(next, 4096);
        return { state: next, savedCount: savedCount };
      }
      function restoreFrequency(state, key, savedCount) {
        var next = { version: 3, counters: cloneCounters((state || {}).counters), clock: Number((state || {}).clock || 0) }; var parts = String(key || '').split(':'); var entry = next.counters[key];
        if (!entry) entry = { version: 3, first: parts[1] || '', second: parts[2] || '', count: 0, lastUsed: 0 };
        entry.count = Number(entry.count || 0) + Number(savedCount || 0); entry.lastUsed = ++next.clock; next.counters[key] = entry; pruneCounters(next, 4096);
        return { state: next, count: entry.count };
      }
      function isTrashExpired(deletedAt, now, retentionMs) {
        var deleted = Date.parse(String(deletedAt || ''));
        return isFinite(deleted) && Number(now) - deleted >= Number(retentionMs);
      }
      function route(input) { var text = String(input && input.text != null ? input.text : ''); var formats = input && input.formats || []; var eligible = normalizeFrequencyText(text) !== ''; if (hasFormat(formats, /^image\\//i) || (input && input.mimeOwner && hasFormat(formats, new RegExp('^' + input.mimeOwner.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&') + '$'))) || (input && input.mimeHidden && hasFormat(formats, new RegExp('^' + input.mimeHidden.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&') + '$')))) return { action: 'passthrough', frequencyEligible: false }; if (isHighConfidenceSecret(text, formats)) return { action: 'ignored', frequencyEligible: false }; if (text.length >= 5000) return { action: 'big', frequencyEligible: eligible }; if (isArtifact(text)) return { action: 'artifacts', frequencyEligible: eligible }; if (isCode(text)) return { action: 'code', frequencyEligible: eligible }; if (/^https?:\\/\\/\\S+$/i.test(text)) return { action: 'passthrough', frequencyEligible: eligible }; return { action: 'default', frequencyEligible: eligible }; }
      function sortFrequent(items) { return items.slice().sort(function (a, b) { return (b.count - a.count) || (a.order - b.order); }); }
      function titleCase(text) { var words = String(text).toLowerCase().split(/(\\s+)/); var minor = { a: 1, an: 1, the: 1, and: 1, but: 1, or: 1, for: 1, nor: 1, as: 1, at: 1, by: 1, from: 1, in: 1, into: 1, near: 1, of: 1, on: 1, onto: 1, to: 1, with: 1 }; var first = -1; var last = -1; var i; for (i = 0; i < words.length; i += 1) if (!/^\\s+$/.test(words[i])) { if (first < 0) first = i; last = i; } for (i = 0; i < words.length; i += 1) if (!/^\\s+$/.test(words[i])) { if (i !== first && i !== last && minor[words[i]]) continue; words[i] = words[i].charAt(0).toUpperCase() + words[i].slice(1); } return words.join('').replace(/\\bId\\b/g, 'ID').replace(/\\bTv\\b/g, 'TV'); }
      function sanitizeHtml(value) { var html = String(value == null ? '' : value); html = html.replace(/\\s(?:color|bgcolor)\\s*=\\s*(?:\"[^\"]*\"|'[^']*'|[^\\s>]+)/gi, ''); return html.replace(/\\sstyle\\s*=\\s*(\"([^\"]*)\"|'([^']*)')/gi, function (match, quoted, doubleQuoted, singleQuoted) { var quote = quoted.charAt(0); var style = doubleQuoted !== undefined ? doubleQuoted : singleQuoted; var declarations = style.split(';').filter(function (declaration) { var name = trim(declaration.split(':')[0]).toLowerCase(); return name && name !== 'color' && name !== 'background' && name !== 'background-color'; }); return declarations.length ? ' style=' + quote + declarations.join(';') + quote : ''; }); }
      function toggleCase(value) { var text = String(value == null ? '' : value); var i; var character; for (i = 0; i < text.length; i += 1) { character = text.charAt(i); if (character !== character.toUpperCase() && character === character.toLowerCase()) return text.toUpperCase(); } return text.toLowerCase(); }
      function validateJsonImport(text) { var parsed; var i; var key; var item; var value; try { parsed = JSON.parse(text); } catch (e) { return { ok: false, reason: 'JSON_PARSE_INVALID' }; } if (!parsed || Object.prototype.toString.call(parsed.copyq_items) !== '[object Array]') return { ok: false, reason: 'JSON_SHAPE_INVALID' }; for (i = 0; i < parsed.copyq_items.length; i += 1) { item = parsed.copyq_items[i]; if (!item || Object.prototype.toString.call(item) !== '[object Object]') return { ok: false, reason: 'JSON_SHAPE_INVALID' }; for (key in item) if (Object.prototype.hasOwnProperty.call(item, key)) { value = item[key]; if (typeof value !== 'string' && !(value && Object.prototype.toString.call(value) === '[object Object]' && typeof value.base64 === 'string')) return { ok: false, reason: 'JSON_SHAPE_INVALID' }; } } return { ok: true, value: parsed }; }
      function validateRegex(text) { try { return { ok: true, value: new RegExp(text, 'i') }; } catch (e) { return { ok: false, reason: 'REGEX_INVALID' }; } }
      function runDependency(execute, args) { var result = execute.apply(null, args || []); return result && result.exit_code === 0 ? { ok: true, value: result } : { ok: false, reason: 'DEPENDENCY_FAILED' }; }
      return { isHighConfidenceSecret: isHighConfidenceSecret, isCode: isCode, isArtifact: isArtifact, isMarkdown: isMarkdown, normalizeFrequencyText: normalizeFrequencyText, hashesFor: hashesFor, v2HashesFor: v2HashesFor, legacyKiloHash: legacyKiloHash, recordFrequency: recordFrequency, dismissFrequency: dismissFrequency, restoreFrequency: restoreFrequency, isTrashExpired: isTrashExpired, route: route, sortFrequent: sortFrequent, titleCase: titleCase, sanitizeHtml: sanitizeHtml, toggleCase: toggleCase, validateJsonImport: validateJsonImport, validateRegex: validateRegex, runDependency: runDependency };
    }());

    var onItemsRemoved_ = global.onItemsRemoved || function () {};
    var TRASH_TAB = '(trash)'; var V3_KEY = 'frequent_usage_counts_v3'; var INTERNAL_REMOVE_KEY = 'copyq_undo_internal_remove'; var SOURCE_TAB_MIME = 'application/x-copyq-trash-source-tab'; var SOURCE_ROW_MIME = 'application/x-copyq-trash-source-row'; var BATCH_MIME = 'application/x-copyq-trash-batch'; var DELETED_AT_MIME = 'application/x-copyq-trash-deleted-at'; var FREQ_KEY_MIME = 'application/x-copyq-user-frequency-key'; var FREQ_COUNT_MIME = 'application/x-copyq-user-frequency-count'; var RESTORE_BATCH_MIME = 'application/x-copyq-undo-restore-batch'; var RETENTION_MS = 30 * 24 * 60 * 60 * 1000;
    function settingsObject(key, fallback) { try { var value = settings(key); return value ? JSON.parse(value) : fallback; } catch (e) { return fallback; } }
    function hasTab(name) { var names = tab(); for (var i = 0; i < names.length; i += 1) if (names[i] === name) return true; return false; }
    function cleanupTrash() { if (!hasTab(TRASH_TAB)) return; var originalTab = selectedTab(); var expired = []; tab(TRASH_TAB); var now = new Date().getTime(); for (var i = 0; i < size(); i += 1) if (CopyQCore.isTrashExpired(str(read(DELETED_AT_MIME, i)), now, RETENTION_MS)) expired.unshift(i); if (expired.length) remove.apply(this, expired); tab(originalTab); }
    function isInternalRemoval() { var markerData = settings(INTERNAL_REMOVE_KEY); if (!markerData) return false; var marker = str(markerData); var items = ItemSelection().current().items(); if (!items.length) return false; for (var i = 0; i < items.length; i += 1) if (str(items[i][BATCH_MIME]) !== marker && str(items[i][RESTORE_BATCH_MIME]) !== marker) return false; return true; }
    global.onItemsRemoved = function () { var sourceTab = selectedTab(); if (isInternalRemoval()) return; if (sourceTab === TRASH_TAB) return onItemsRemoved_(); var selected = ItemSelection().selectRemovable(); var outsideCurrent = ItemSelection().current().invert(); selected.deselectSelection(outsideCurrent); if (!selected.length) return onItemsRemoved_(); var rows = selected.rows(); var items = selected.items(); cleanupTrash(); var batch = String(new Date().getTime()) + ':' + String(Math.floor(Math.random() * 1000000000)); var deletedAt = new Date().toISOString(); var oldStateValue = settings(V3_KEY); var state = settingsObject(V3_KEY, { version: 3, counters: {}, clock: 0 }); for (var i = 0; i < items.length; i += 1) { var item = items[i]; item[SOURCE_TAB_MIME] = sourceTab; item[SOURCE_ROW_MIME] = String(rows[i]); item[BATCH_MIME] = batch; item[DELETED_AT_MIME] = deletedAt; if (sourceTab === 'Frequent') { var key = str(item[FREQ_KEY_MIME]); if (!key) key = CopyQCore.hashesFor(str(item[mimeText])).key; var metadataCount = Number(str(item[FREQ_COUNT_MIME]) || 0); var dismissed = CopyQCore.dismissFrequency(state, key); state = dismissed.state; item[FREQ_KEY_MIME] = key; item[FREQ_COUNT_MIME] = String(dismissed.savedCount || metadataCount); } } settings(V3_KEY, JSON.stringify(state)); try { tab(TRASH_TAB); write(0, items); tab(sourceTab); onItemsRemoved_(); } catch (error) { settings(INTERNAL_REMOVE_KEY, batch); try { var escapedBatch = batch.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&'); var written = ItemSelection(TRASH_TAB).select(new RegExp('^' + escapedBatch + '$'), BATCH_MIME); if (written.length) { tab(TRASH_TAB); remove.apply(this, written.rows()); } } catch (removeError) {} settings(INTERNAL_REMOVE_KEY, ''); settings(V3_KEY, oldStateValue || ''); tab(sourceTab); throw error; } };
    settings(INTERNAL_REMOVE_KEY, ''); cleanupTrash();"
2\Input=text/plain
2\IsScript=true
2\InternalId=canonical.undoable-delete-listener
3\Name=Undo Delete
3\Command="
    copyq:
    var CopyQCore = (function () {
      function trim(value) { return String(value == null ? '' : value).replace(/^\\s+|\\s+$/g, ''); }
      function hasFormat(formats, expression) { var i; for (i = 0; i < (formats || []).length; i += 1) if (expression.test(String(formats[i]))) return true; return false; }
      function decode(part) {
        var alphabet = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/';
        var input = part.replace(/-/g, '+').replace(/_/g, '/'); var out = ''; var bits = 0; var bitCount = 0; var i; var n;
        while (input.length % 4) input += '=';
        for (i = 0; i < input.length; i += 1) { if (input.charAt(i) === '=') break; n = alphabet.indexOf(input.charAt(i)); if (n < 0) return null; bits = (bits << 6) | n; bitCount += 6; while (bitCount >= 8) { bitCount -= 8; out += String.fromCharCode((bits >> bitCount) & 255); } }
        return out;
      }
      function validJwt(text) { var parts = text.split('.'); var head; var body; try { head = parts.length === 3 ? JSON.parse(decode(parts[0])) : null; body = parts.length === 3 ? JSON.parse(decode(parts[1])) : null; } catch (e) { return false; } return !!(head && body && typeof head.alg === 'string' && /^[A-Za-z0-9_-]+$/.test(parts[2] || '')); }
      function credentialUrl(text) { return /^https?:\\/\\/[^/\\s:@]+:[^/\\s@]+@/i.test(text) || /[?&](?:access_token|api[_-]?key|password|secret|token)=[^&#\\s]+/i.test(text); }
      function secretFormat(formats) { return hasFormat(formats, /(?:application\\/x-copyq-secret|org\\.nspasteboard\\.concealedtype|clipboard viewer ignore|x-kde-passwordmanagerhint|nspasteboard concealed|mimesecret|com\\.agilebits\\.onepassword|1password|bitwarden|keepass|lastpass|password-manager)/i); }
      function structuredNonSecret(text) {
        return /^https?:\\/\\/\\S+$/i.test(text)
          || /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(text)
          || /^(?:[A-Za-z]:[\\\\/]|\\\\\\\\|\\/)[^\\r\\n]+$/.test(text)
          || /^v?\\d+\\.\\d+\\.\\d+(?:[-+][0-9A-Za-z.-]+)?$/.test(text)
          || /^[A-Z][A-Z0-9]{1,9}-\\d{1,10}$/.test(text);
      }
      function historicalOpaqueSecret(text) {
        var classes = 0;
        if (text.length < 10 || text.length > 3000 || /\\s/.test(text)) return false;
        if (/[a-z]/.test(text)) classes += 1;
        if (/[A-Z]/.test(text)) classes += 1;
        if (/\\d/.test(text)) classes += 1;
        return classes >= 2;
      }
      function isHighConfidenceSecret(value, formats) {
        var text = trim(value);
        if (secretFormat(formats)) return true;
        if (/^-----BEGIN (?:[A-Z0-9 ]+ )?(?:PRIVATE KEY|OPENSSH PRIVATE KEY)-----/m.test(text) || validJwt(text)) return true;
        if (/^(?:sk-[A-Za-z0-9_-]{20,}|ghp_[A-Za-z0-9]{30,}|github_pat_[A-Za-z0-9_]{30,}|glpat-[A-Za-z0-9_-]{20,}|xox[baprs]-[A-Za-z0-9-]{12,}|AIza[0-9A-Za-z_-]{30,}|AKIA[0-9A-Z]{16}|npm_[A-Za-z0-9_-]{20,}|pypi-[A-Za-z0-9_-]{20,})$/.test(text)) return true;
        if (/^authorization:\\s*(?:bearer|basic|token)\\s+\\S{12,}$/i.test(text)) return true;
        if(/^\\s*(?:export\\s+)?(?:[A-Za-z_][A-Za-z0-9_]*(?:PASSWORD|TOKEN|SECRET|API_KEY)[A-Za-z0-9_]*|PASSWORD|TOKEN|SECRET|API_KEY)\\s*=\\s*\\S{12,}\\s*$/i.test(text)) return true;
        if (credentialUrl(text)) return true;
        if (/^[0-9a-f]{32,128}$/i.test(text)) return true;
        if (structuredNonSecret(text)) return false;
        return historicalOpaqueSecret(text);
      }
      function isCode(text) { return /^\\s*(?:import|export|var|const|let|function|class|def|package|using|namespace|#include|#!)\\b/m.test(text) || /(?:=>|===|!==|&&|\\|\\|)/.test(text) || /\\b(?:if|for|while|switch|catch)\\s*\\(/.test(text) || /\\w+\\s*\\([^)]*\\)\\s*\\{/.test(text); }
      function isArtifact(value) {
        var text = trim(value); var parsed; var lines; var matches;
        if (!text) return false;
        if (/^[\\[{]/.test(text)) {
          try {
            parsed = JSON.parse(text);
            if (Object.prototype.toString.call(parsed) === '[object Array]') return parsed.length >= 2;
            if (parsed && Object.prototype.toString.call(parsed) === '[object Object]') return Object.keys(parsed).length >= 2;
          } catch (e) {}
        }
        if (!/[\\r\\n]/.test(text)) return false;
        lines = text.split(/\\r?\\n/).filter(function (line) { return trim(line) !== ''; });
        if (lines.length < 2) return false;
        if (/^diff --git\\s/m.test(text) || /^@@\\s+-\\d+(?:,\\d+)?\\s+\\+\\d+(?:,\\d+)?\\s+@@/m.test(text)) return true;
        if (/---\\s+backtrace\\s+---/i.test(text) || /^Traceback \\(most recent call last\\):/m.test(text)) return true;
        matches = text.match(/^\\s*at\\s+\\S.+$/gm); if (matches && matches.length >= 2) return true;
        if (/^(?:PS\\s+[A-Za-z]:\\\\[^>\\r\\n]*>|[$#>])\\s*\\S/m.test(text)) return true;
        if (/`\\s*$/m.test(text) && /^\\s{2,}-(?:\\w|-)\\S*/m.test(text)) return true;
        matches = text.match(/^(?:\\[[0-2]?\\d:[0-5]\\d:[0-5]\\d\\]|\\d{4}-\\d{2}-\\d{2}[T ][^\\s]+|\\s*(?:TRACE|DEBUG|INFO|WARN|ERROR|FATAL)\\b).+$/gim);
        if (matches && matches.length >= 2) return true;
        if (/^RELEASE_STAGE_FAILED:/m.test(text) && /(?:reason=|backtrace)/i.test(text)) return true;
        matches = text.match(/^\\s*(?:\\[[^\\]\\r\\n]+\\]|[A-Za-z_][A-Za-z0-9_.-]*\\s*(?:=|:)\\s*\\S.*)$/gm);
        return !!(matches && matches.length >= 3);
      }
      function isMarkdown(value) {
        var text = String(value == null ? '' : value); var listItems;
        if (!text) return false;
        if (/(^|\\n) {0,3}#{1,6}[ \\t]+\\S/.test(text)) return true;
        if (/(^|\\n) {0,3}```[^\\n]*\\n[\\s\\S]*\\n {0,3}```/.test(text)) return true;
        if (/\\[[^\\]\\n]+\\]\\((?:https?:\\/\\/|\\/|#|\\.{0,2}\\/)[^)]+\\)/.test(text)) return true;
        if (/(^|\\n) {0,3}>[ \\t]+\\S/.test(text)) return true;
        if (/(^|\\n)\\s*\\|?.+\\|.+\\n\\s*\\|?\\s*:?-{3,}:?\\s*\\|/.test(text)) return true;
        listItems = text.match(/(^|\\n) {0,3}(?:[-+*]|\\d+[.)])[ \\t]+\\S/g);
        return !!(listItems && listItems.length >= 2);
      }
      function fnv1a(text) { var hash = 2166136261; var i; for (i = 0; i < text.length; i += 1) { hash ^= text.charCodeAt(i); hash = (hash * 16777619) >>> 0; } return (hash >>> 0).toString(16); }
      function djb2(text) { var hash = 5381; var i; for (i = 0; i < text.length; i += 1) hash = ((hash << 5) + hash + text.charCodeAt(i)) | 0; return (hash >>> 0).toString(36); }
      function legacyKiloHash(text) { text = String(text == null ? '' : text); return text.length + '_' + djb2(text); }
      function normalizeFrequencyText(text) { return trim(text); }
      function v2HashesFor(text) { text = String(text == null ? '' : text); return { version: 2, first: fnv1a(text), second: djb2(text), key: 'v2:' + fnv1a(text) + ':' + djb2(text) }; }
      function hashesFor(text) { text = normalizeFrequencyText(text); return { version: 3, first: fnv1a(text), second: djb2(text), key: 'v3:' + fnv1a(text) + ':' + djb2(text) }; }
      function cloneCounters(counters) { var copy = {}; var key; for (key in counters || {}) if (Object.prototype.hasOwnProperty.call(counters, key)) copy[key] = { version: counters[key].version, first: counters[key].first, second: counters[key].second, count: counters[key].count, lastUsed: counters[key].lastUsed }; return copy; }
      function pruneCounters(state, maximum) {
        var keys = []; var key;
        for (key in state.counters) if (Object.prototype.hasOwnProperty.call(state.counters, key)) keys.push(key);
        keys.sort(function (a, b) { return (state.counters[a].lastUsed - state.counters[b].lastUsed) || (a < b ? -1 : 1); });
        while (keys.length > maximum) delete state.counters[keys.shift()];
      }
      function recordFrequency(state, v2State, legacy, text) {
        var next = { version: 3, counters: cloneCounters((state || {}).counters), clock: Number((state || {}).clock || 0) };
        var canonicalText = normalizeFrequencyText(text); var hash = hashesFor(canonicalText); var oldHash = v2HashesFor(canonicalText); var oldKey = legacyKiloHash(canonicalText); var entry; var imported = 0;
        if (!canonicalText) return { state: next, result: { count: 0, promote: false, key: '', canonicalText: '', hashes: hash } };
        entry = next.counters[hash.key];
        if (!entry) {
          if (v2State && v2State.counters && v2State.counters[oldHash.key]) imported += Number(v2State.counters[oldHash.key].count || 0);
          if (legacy && legacy[oldKey] !== undefined) imported += Number(legacy[oldKey] || 0);
          entry = { version: 3, first: hash.first, second: hash.second, count: imported, lastUsed: 0 };
        }
        entry.count += 1; entry.lastUsed = ++next.clock; next.counters[hash.key] = entry;
        pruneCounters(next, 4096);
        return { state: next, result: { count: entry.count, promote: entry.count >= 6, key: hash.key, canonicalText: canonicalText, hashes: hash } };
      }
      function dismissFrequency(state, key) {
        var next = { version: 3, counters: cloneCounters((state || {}).counters), clock: Number((state || {}).clock || 0) }; var entry = next.counters[key]; var parts = String(key || '').split(':'); var savedCount = 0;
        if (!entry) entry = { version: 3, first: parts[1] || '', second: parts[2] || '', count: 0, lastUsed: 0 };
        savedCount = Number(entry.count || 0); entry.count = 0; entry.lastUsed = ++next.clock; next.counters[key] = entry; pruneCounters(next, 4096);
        return { state: next, savedCount: savedCount };
      }
      function restoreFrequency(state, key, savedCount) {
        var next = { version: 3, counters: cloneCounters((state || {}).counters), clock: Number((state || {}).clock || 0) }; var parts = String(key || '').split(':'); var entry = next.counters[key];
        if (!entry) entry = { version: 3, first: parts[1] || '', second: parts[2] || '', count: 0, lastUsed: 0 };
        entry.count = Number(entry.count || 0) + Number(savedCount || 0); entry.lastUsed = ++next.clock; next.counters[key] = entry; pruneCounters(next, 4096);
        return { state: next, count: entry.count };
      }
      function isTrashExpired(deletedAt, now, retentionMs) {
        var deleted = Date.parse(String(deletedAt || ''));
        return isFinite(deleted) && Number(now) - deleted >= Number(retentionMs);
      }
      function route(input) { var text = String(input && input.text != null ? input.text : ''); var formats = input && input.formats || []; var eligible = normalizeFrequencyText(text) !== ''; if (hasFormat(formats, /^image\\//i) || (input && input.mimeOwner && hasFormat(formats, new RegExp('^' + input.mimeOwner.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&') + '$'))) || (input && input.mimeHidden && hasFormat(formats, new RegExp('^' + input.mimeHidden.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&') + '$')))) return { action: 'passthrough', frequencyEligible: false }; if (isHighConfidenceSecret(text, formats)) return { action: 'ignored', frequencyEligible: false }; if (text.length >= 5000) return { action: 'big', frequencyEligible: eligible }; if (isArtifact(text)) return { action: 'artifacts', frequencyEligible: eligible }; if (isCode(text)) return { action: 'code', frequencyEligible: eligible }; if (/^https?:\\/\\/\\S+$/i.test(text)) return { action: 'passthrough', frequencyEligible: eligible }; return { action: 'default', frequencyEligible: eligible }; }
      function sortFrequent(items) { return items.slice().sort(function (a, b) { return (b.count - a.count) || (a.order - b.order); }); }
      function titleCase(text) { var words = String(text).toLowerCase().split(/(\\s+)/); var minor = { a: 1, an: 1, the: 1, and: 1, but: 1, or: 1, for: 1, nor: 1, as: 1, at: 1, by: 1, from: 1, in: 1, into: 1, near: 1, of: 1, on: 1, onto: 1, to: 1, with: 1 }; var first = -1; var last = -1; var i; for (i = 0; i < words.length; i += 1) if (!/^\\s+$/.test(words[i])) { if (first < 0) first = i; last = i; } for (i = 0; i < words.length; i += 1) if (!/^\\s+$/.test(words[i])) { if (i !== first && i !== last && minor[words[i]]) continue; words[i] = words[i].charAt(0).toUpperCase() + words[i].slice(1); } return words.join('').replace(/\\bId\\b/g, 'ID').replace(/\\bTv\\b/g, 'TV'); }
      function sanitizeHtml(value) { var html = String(value == null ? '' : value); html = html.replace(/\\s(?:color|bgcolor)\\s*=\\s*(?:\"[^\"]*\"|'[^']*'|[^\\s>]+)/gi, ''); return html.replace(/\\sstyle\\s*=\\s*(\"([^\"]*)\"|'([^']*)')/gi, function (match, quoted, doubleQuoted, singleQuoted) { var quote = quoted.charAt(0); var style = doubleQuoted !== undefined ? doubleQuoted : singleQuoted; var declarations = style.split(';').filter(function (declaration) { var name = trim(declaration.split(':')[0]).toLowerCase(); return name && name !== 'color' && name !== 'background' && name !== 'background-color'; }); return declarations.length ? ' style=' + quote + declarations.join(';') + quote : ''; }); }
      function toggleCase(value) { var text = String(value == null ? '' : value); var i; var character; for (i = 0; i < text.length; i += 1) { character = text.charAt(i); if (character !== character.toUpperCase() && character === character.toLowerCase()) return text.toUpperCase(); } return text.toLowerCase(); }
      function validateJsonImport(text) { var parsed; var i; var key; var item; var value; try { parsed = JSON.parse(text); } catch (e) { return { ok: false, reason: 'JSON_PARSE_INVALID' }; } if (!parsed || Object.prototype.toString.call(parsed.copyq_items) !== '[object Array]') return { ok: false, reason: 'JSON_SHAPE_INVALID' }; for (i = 0; i < parsed.copyq_items.length; i += 1) { item = parsed.copyq_items[i]; if (!item || Object.prototype.toString.call(item) !== '[object Object]') return { ok: false, reason: 'JSON_SHAPE_INVALID' }; for (key in item) if (Object.prototype.hasOwnProperty.call(item, key)) { value = item[key]; if (typeof value !== 'string' && !(value && Object.prototype.toString.call(value) === '[object Object]' && typeof value.base64 === 'string')) return { ok: false, reason: 'JSON_SHAPE_INVALID' }; } } return { ok: true, value: parsed }; }
      function validateRegex(text) { try { return { ok: true, value: new RegExp(text, 'i') }; } catch (e) { return { ok: false, reason: 'REGEX_INVALID' }; } }
      function runDependency(execute, args) { var result = execute.apply(null, args || []); return result && result.exit_code === 0 ? { ok: true, value: result } : { ok: false, reason: 'DEPENDENCY_FAILED' }; }
      return { isHighConfidenceSecret: isHighConfidenceSecret, isCode: isCode, isArtifact: isArtifact, isMarkdown: isMarkdown, normalizeFrequencyText: normalizeFrequencyText, hashesFor: hashesFor, v2HashesFor: v2HashesFor, legacyKiloHash: legacyKiloHash, recordFrequency: recordFrequency, dismissFrequency: dismissFrequency, restoreFrequency: restoreFrequency, isTrashExpired: isTrashExpired, route: route, sortFrequent: sortFrequent, titleCase: titleCase, sanitizeHtml: sanitizeHtml, toggleCase: toggleCase, validateJsonImport: validateJsonImport, validateRegex: validateRegex, runDependency: runDependency };
    }());

    var TRASH_TAB = '(trash)'; var V3_KEY = 'frequent_usage_counts_v3'; var INTERNAL_REMOVE_KEY = 'copyq_undo_internal_remove'; var SOURCE_TAB_MIME = 'application/x-copyq-trash-source-tab'; var SOURCE_ROW_MIME = 'application/x-copyq-trash-source-row'; var BATCH_MIME = 'application/x-copyq-trash-batch'; var DELETED_AT_MIME = 'application/x-copyq-trash-deleted-at'; var FREQ_KEY_MIME = 'application/x-copyq-user-frequency-key'; var FREQ_COUNT_MIME = 'application/x-copyq-user-frequency-count'; var RESTORE_BATCH_MIME = 'application/x-copyq-undo-restore-batch';
    function settingsObject(key, fallback) { try { var value = settings(key); return value ? JSON.parse(value) : fallback; } catch (e) { return fallback; } } function hasTab(name) { var names = tab(); for (var i = 0; i < names.length; i += 1) if (names[i] === name) return true; return false; }
    if (!hasTab(TRASH_TAB)) { notification('.id', 'undo-delete', '.title', 'Undo Delete', '.message', 'NOTHING_TO_UNDO'); abort(); } tab(TRASH_TAB); if (!size()) { notification('.id', 'undo-delete', '.title', 'Undo Delete', '.message', 'NOTHING_TO_UNDO'); abort(); }
    var batch = str(read(BATCH_MIME, 0)); if (!batch) { notification('.id', 'undo-delete', '.title', 'Undo Delete', '.message', 'NOTHING_TO_UNDO'); abort(); } var records = []; var trashRows = []; for (var i = 0; i < size() && str(read(BATCH_MIME, i)) === batch; i += 1) { var item = getItem(i); records.push({ item: item, row: Number(str(item[SOURCE_ROW_MIME]) || 0), tab: str(item[SOURCE_TAB_MIME]) || '&Clipboard', key: str(item[FREQ_KEY_MIME]), saved: Number(str(item[FREQ_COUNT_MIME]) || 0) }); trashRows.push(i); }
    records.sort(function (a, b) { return a.row - b.row; }); var oldStateValue = settings(V3_KEY); var state = settingsObject(V3_KEY, { version: 3, counters: {}, clock: 0 }); var targetTab = records.length && hasTab(records[0].tab) ? records[0].tab : '&Clipboard'; var changed = []; var inserted = null; var trashCommitted = false; try { for (i = 0; i < records.length; i += 1) { var record = records[i]; var restoredCount = 0; if (targetTab === 'Frequent' && record.key) { var restored = CopyQCore.restoreFrequency(state, record.key, record.saved); state = restored.state; restoredCount = restored.count; record.item[FREQ_COUNT_MIME] = String(restoredCount); } delete record.item[SOURCE_TAB_MIME]; delete record.item[SOURCE_ROW_MIME]; delete record.item[BATCH_MIME]; delete record.item[DELETED_AT_MIME]; if (targetTab !== 'Frequent') { delete record.item[FREQ_KEY_MIME]; delete record.item[FREQ_COUNT_MIME]; } tab(targetTab); if (targetTab === 'Frequent' && record.key) { var escapedKey = record.key.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&'); var existing = ItemSelection('Frequent').select(new RegExp('^' + escapedKey + '$'), FREQ_KEY_MIME); if (existing.length) { changed.push({ selection: existing, item: existing.itemAtIndex(0), row: existing.rows()[0] }); var current = existing.itemAtIndex(0); current[mimeText] = record.item[mimeText]; current[FREQ_KEY_MIME] = record.key; current[FREQ_COUNT_MIME] = String(restoredCount); existing.setItemAtIndex(0, current); existing.move(0); } else { record.item[RESTORE_BATCH_MIME] = batch; insert(Math.min(record.row, size()), record.item); } } else { record.item[RESTORE_BATCH_MIME] = batch; insert(Math.min(record.row, size()), record.item); } } var escapedBatch = batch.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&'); inserted = ItemSelection(targetTab).select(new RegExp('^' + escapedBatch + '$'), RESTORE_BATCH_MIME); inserted.setItemsFormat(RESTORE_BATCH_MIME, undefined); settings(V3_KEY, JSON.stringify(state)); settings(INTERNAL_REMOVE_KEY, batch); tab(TRASH_TAB); remove.apply(this, trashRows); trashCommitted = true; settings(INTERNAL_REMOVE_KEY, ''); tab(targetTab); } catch (error) { if (trashCommitted) { try { settings(INTERNAL_REMOVE_KEY, ''); } catch (guardError) {} try { tab(targetTab); } catch (tabError) {} throw error; } try { var rollbackInserted = inserted; if (!rollbackInserted) { var rollbackPattern = batch.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&'); rollbackInserted = ItemSelection(targetTab).select(new RegExp('^' + rollbackPattern + '$'), RESTORE_BATCH_MIME); } if (rollbackInserted && rollbackInserted.length) { rollbackInserted.setItemsFormat(RESTORE_BATCH_MIME, batch); settings(INTERNAL_REMOVE_KEY, batch); tab(targetTab); remove.apply(this, rollbackInserted.rows()); } } catch (removeError) {} settings(INTERNAL_REMOVE_KEY, ''); for (i = changed.length - 1; i >= 0; i -= 1) { try { changed[i].selection.setItemAtIndex(0, changed[i].item); changed[i].selection.move(changed[i].row); } catch (restoreError) {} } settings(V3_KEY, oldStateValue || ''); tab(TRASH_TAB); throw error; }"
3\Input=text/plain
3\InMenu=true
3\Shortcut=ctrl+z
3\InternalId=canonical.undo-delete
4\Name=Remove Background and Text Colors
4\Command="
    copyq:
    var CopyQCore = (function () {
      function trim(value) { return String(value == null ? '' : value).replace(/^\\s+|\\s+$/g, ''); }
      function hasFormat(formats, expression) { var i; for (i = 0; i < (formats || []).length; i += 1) if (expression.test(String(formats[i]))) return true; return false; }
      function decode(part) {
        var alphabet = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/';
        var input = part.replace(/-/g, '+').replace(/_/g, '/'); var out = ''; var bits = 0; var bitCount = 0; var i; var n;
        while (input.length % 4) input += '=';
        for (i = 0; i < input.length; i += 1) { if (input.charAt(i) === '=') break; n = alphabet.indexOf(input.charAt(i)); if (n < 0) return null; bits = (bits << 6) | n; bitCount += 6; while (bitCount >= 8) { bitCount -= 8; out += String.fromCharCode((bits >> bitCount) & 255); } }
        return out;
      }
      function validJwt(text) { var parts = text.split('.'); var head; var body; try { head = parts.length === 3 ? JSON.parse(decode(parts[0])) : null; body = parts.length === 3 ? JSON.parse(decode(parts[1])) : null; } catch (e) { return false; } return !!(head && body && typeof head.alg === 'string' && /^[A-Za-z0-9_-]+$/.test(parts[2] || '')); }
      function credentialUrl(text) { return /^https?:\\/\\/[^/\\s:@]+:[^/\\s@]+@/i.test(text) || /[?&](?:access_token|api[_-]?key|password|secret|token)=[^&#\\s]+/i.test(text); }
      function secretFormat(formats) { return hasFormat(formats, /(?:application\\/x-copyq-secret|org\\.nspasteboard\\.concealedtype|clipboard viewer ignore|x-kde-passwordmanagerhint|nspasteboard concealed|mimesecret|com\\.agilebits\\.onepassword|1password|bitwarden|keepass|lastpass|password-manager)/i); }
      function structuredNonSecret(text) {
        return /^https?:\\/\\/\\S+$/i.test(text)
          || /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(text)
          || /^(?:[A-Za-z]:[\\\\/]|\\\\\\\\|\\/)[^\\r\\n]+$/.test(text)
          || /^v?\\d+\\.\\d+\\.\\d+(?:[-+][0-9A-Za-z.-]+)?$/.test(text)
          || /^[A-Z][A-Z0-9]{1,9}-\\d{1,10}$/.test(text);
      }
      function historicalOpaqueSecret(text) {
        var classes = 0;
        if (text.length < 10 || text.length > 3000 || /\\s/.test(text)) return false;
        if (/[a-z]/.test(text)) classes += 1;
        if (/[A-Z]/.test(text)) classes += 1;
        if (/\\d/.test(text)) classes += 1;
        return classes >= 2;
      }
      function isHighConfidenceSecret(value, formats) {
        var text = trim(value);
        if (secretFormat(formats)) return true;
        if (/^-----BEGIN (?:[A-Z0-9 ]+ )?(?:PRIVATE KEY|OPENSSH PRIVATE KEY)-----/m.test(text) || validJwt(text)) return true;
        if (/^(?:sk-[A-Za-z0-9_-]{20,}|ghp_[A-Za-z0-9]{30,}|github_pat_[A-Za-z0-9_]{30,}|glpat-[A-Za-z0-9_-]{20,}|xox[baprs]-[A-Za-z0-9-]{12,}|AIza[0-9A-Za-z_-]{30,}|AKIA[0-9A-Z]{16}|npm_[A-Za-z0-9_-]{20,}|pypi-[A-Za-z0-9_-]{20,})$/.test(text)) return true;
        if (/^authorization:\\s*(?:bearer|basic|token)\\s+\\S{12,}$/i.test(text)) return true;
        if(/^\\s*(?:export\\s+)?(?:[A-Za-z_][A-Za-z0-9_]*(?:PASSWORD|TOKEN|SECRET|API_KEY)[A-Za-z0-9_]*|PASSWORD|TOKEN|SECRET|API_KEY)\\s*=\\s*\\S{12,}\\s*$/i.test(text)) return true;
        if (credentialUrl(text)) return true;
        if (/^[0-9a-f]{32,128}$/i.test(text)) return true;
        if (structuredNonSecret(text)) return false;
        return historicalOpaqueSecret(text);
      }
      function isCode(text) { return /^\\s*(?:import|export|var|const|let|function|class|def|package|using|namespace|#include|#!)\\b/m.test(text) || /(?:=>|===|!==|&&|\\|\\|)/.test(text) || /\\b(?:if|for|while|switch|catch)\\s*\\(/.test(text) || /\\w+\\s*\\([^)]*\\)\\s*\\{/.test(text); }
      function isArtifact(value) {
        var text = trim(value); var parsed; var lines; var matches;
        if (!text) return false;
        if (/^[\\[{]/.test(text)) {
          try {
            parsed = JSON.parse(text);
            if (Object.prototype.toString.call(parsed) === '[object Array]') return parsed.length >= 2;
            if (parsed && Object.prototype.toString.call(parsed) === '[object Object]') return Object.keys(parsed).length >= 2;
          } catch (e) {}
        }
        if (!/[\\r\\n]/.test(text)) return false;
        lines = text.split(/\\r?\\n/).filter(function (line) { return trim(line) !== ''; });
        if (lines.length < 2) return false;
        if (/^diff --git\\s/m.test(text) || /^@@\\s+-\\d+(?:,\\d+)?\\s+\\+\\d+(?:,\\d+)?\\s+@@/m.test(text)) return true;
        if (/---\\s+backtrace\\s+---/i.test(text) || /^Traceback \\(most recent call last\\):/m.test(text)) return true;
        matches = text.match(/^\\s*at\\s+\\S.+$/gm); if (matches && matches.length >= 2) return true;
        if (/^(?:PS\\s+[A-Za-z]:\\\\[^>\\r\\n]*>|[$#>])\\s*\\S/m.test(text)) return true;
        if (/`\\s*$/m.test(text) && /^\\s{2,}-(?:\\w|-)\\S*/m.test(text)) return true;
        matches = text.match(/^(?:\\[[0-2]?\\d:[0-5]\\d:[0-5]\\d\\]|\\d{4}-\\d{2}-\\d{2}[T ][^\\s]+|\\s*(?:TRACE|DEBUG|INFO|WARN|ERROR|FATAL)\\b).+$/gim);
        if (matches && matches.length >= 2) return true;
        if (/^RELEASE_STAGE_FAILED:/m.test(text) && /(?:reason=|backtrace)/i.test(text)) return true;
        matches = text.match(/^\\s*(?:\\[[^\\]\\r\\n]+\\]|[A-Za-z_][A-Za-z0-9_.-]*\\s*(?:=|:)\\s*\\S.*)$/gm);
        return !!(matches && matches.length >= 3);
      }
      function isMarkdown(value) {
        var text = String(value == null ? '' : value); var listItems;
        if (!text) return false;
        if (/(^|\\n) {0,3}#{1,6}[ \\t]+\\S/.test(text)) return true;
        if (/(^|\\n) {0,3}```[^\\n]*\\n[\\s\\S]*\\n {0,3}```/.test(text)) return true;
        if (/\\[[^\\]\\n]+\\]\\((?:https?:\\/\\/|\\/|#|\\.{0,2}\\/)[^)]+\\)/.test(text)) return true;
        if (/(^|\\n) {0,3}>[ \\t]+\\S/.test(text)) return true;
        if (/(^|\\n)\\s*\\|?.+\\|.+\\n\\s*\\|?\\s*:?-{3,}:?\\s*\\|/.test(text)) return true;
        listItems = text.match(/(^|\\n) {0,3}(?:[-+*]|\\d+[.)])[ \\t]+\\S/g);
        return !!(listItems && listItems.length >= 2);
      }
      function fnv1a(text) { var hash = 2166136261; var i; for (i = 0; i < text.length; i += 1) { hash ^= text.charCodeAt(i); hash = (hash * 16777619) >>> 0; } return (hash >>> 0).toString(16); }
      function djb2(text) { var hash = 5381; var i; for (i = 0; i < text.length; i += 1) hash = ((hash << 5) + hash + text.charCodeAt(i)) | 0; return (hash >>> 0).toString(36); }
      function legacyKiloHash(text) { text = String(text == null ? '' : text); return text.length + '_' + djb2(text); }
      function normalizeFrequencyText(text) { return trim(text); }
      function v2HashesFor(text) { text = String(text == null ? '' : text); return { version: 2, first: fnv1a(text), second: djb2(text), key: 'v2:' + fnv1a(text) + ':' + djb2(text) }; }
      function hashesFor(text) { text = normalizeFrequencyText(text); return { version: 3, first: fnv1a(text), second: djb2(text), key: 'v3:' + fnv1a(text) + ':' + djb2(text) }; }
      function cloneCounters(counters) { var copy = {}; var key; for (key in counters || {}) if (Object.prototype.hasOwnProperty.call(counters, key)) copy[key] = { version: counters[key].version, first: counters[key].first, second: counters[key].second, count: counters[key].count, lastUsed: counters[key].lastUsed }; return copy; }
      function pruneCounters(state, maximum) {
        var keys = []; var key;
        for (key in state.counters) if (Object.prototype.hasOwnProperty.call(state.counters, key)) keys.push(key);
        keys.sort(function (a, b) { return (state.counters[a].lastUsed - state.counters[b].lastUsed) || (a < b ? -1 : 1); });
        while (keys.length > maximum) delete state.counters[keys.shift()];
      }
      function recordFrequency(state, v2State, legacy, text) {
        var next = { version: 3, counters: cloneCounters((state || {}).counters), clock: Number((state || {}).clock || 0) };
        var canonicalText = normalizeFrequencyText(text); var hash = hashesFor(canonicalText); var oldHash = v2HashesFor(canonicalText); var oldKey = legacyKiloHash(canonicalText); var entry; var imported = 0;
        if (!canonicalText) return { state: next, result: { count: 0, promote: false, key: '', canonicalText: '', hashes: hash } };
        entry = next.counters[hash.key];
        if (!entry) {
          if (v2State && v2State.counters && v2State.counters[oldHash.key]) imported += Number(v2State.counters[oldHash.key].count || 0);
          if (legacy && legacy[oldKey] !== undefined) imported += Number(legacy[oldKey] || 0);
          entry = { version: 3, first: hash.first, second: hash.second, count: imported, lastUsed: 0 };
        }
        entry.count += 1; entry.lastUsed = ++next.clock; next.counters[hash.key] = entry;
        pruneCounters(next, 4096);
        return { state: next, result: { count: entry.count, promote: entry.count >= 6, key: hash.key, canonicalText: canonicalText, hashes: hash } };
      }
      function dismissFrequency(state, key) {
        var next = { version: 3, counters: cloneCounters((state || {}).counters), clock: Number((state || {}).clock || 0) }; var entry = next.counters[key]; var parts = String(key || '').split(':'); var savedCount = 0;
        if (!entry) entry = { version: 3, first: parts[1] || '', second: parts[2] || '', count: 0, lastUsed: 0 };
        savedCount = Number(entry.count || 0); entry.count = 0; entry.lastUsed = ++next.clock; next.counters[key] = entry; pruneCounters(next, 4096);
        return { state: next, savedCount: savedCount };
      }
      function restoreFrequency(state, key, savedCount) {
        var next = { version: 3, counters: cloneCounters((state || {}).counters), clock: Number((state || {}).clock || 0) }; var parts = String(key || '').split(':'); var entry = next.counters[key];
        if (!entry) entry = { version: 3, first: parts[1] || '', second: parts[2] || '', count: 0, lastUsed: 0 };
        entry.count = Number(entry.count || 0) + Number(savedCount || 0); entry.lastUsed = ++next.clock; next.counters[key] = entry; pruneCounters(next, 4096);
        return { state: next, count: entry.count };
      }
      function isTrashExpired(deletedAt, now, retentionMs) {
        var deleted = Date.parse(String(deletedAt || ''));
        return isFinite(deleted) && Number(now) - deleted >= Number(retentionMs);
      }
      function route(input) { var text = String(input && input.text != null ? input.text : ''); var formats = input && input.formats || []; var eligible = normalizeFrequencyText(text) !== ''; if (hasFormat(formats, /^image\\//i) || (input && input.mimeOwner && hasFormat(formats, new RegExp('^' + input.mimeOwner.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&') + '$'))) || (input && input.mimeHidden && hasFormat(formats, new RegExp('^' + input.mimeHidden.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&') + '$')))) return { action: 'passthrough', frequencyEligible: false }; if (isHighConfidenceSecret(text, formats)) return { action: 'ignored', frequencyEligible: false }; if (text.length >= 5000) return { action: 'big', frequencyEligible: eligible }; if (isArtifact(text)) return { action: 'artifacts', frequencyEligible: eligible }; if (isCode(text)) return { action: 'code', frequencyEligible: eligible }; if (/^https?:\\/\\/\\S+$/i.test(text)) return { action: 'passthrough', frequencyEligible: eligible }; return { action: 'default', frequencyEligible: eligible }; }
      function sortFrequent(items) { return items.slice().sort(function (a, b) { return (b.count - a.count) || (a.order - b.order); }); }
      function titleCase(text) { var words = String(text).toLowerCase().split(/(\\s+)/); var minor = { a: 1, an: 1, the: 1, and: 1, but: 1, or: 1, for: 1, nor: 1, as: 1, at: 1, by: 1, from: 1, in: 1, into: 1, near: 1, of: 1, on: 1, onto: 1, to: 1, with: 1 }; var first = -1; var last = -1; var i; for (i = 0; i < words.length; i += 1) if (!/^\\s+$/.test(words[i])) { if (first < 0) first = i; last = i; } for (i = 0; i < words.length; i += 1) if (!/^\\s+$/.test(words[i])) { if (i !== first && i !== last && minor[words[i]]) continue; words[i] = words[i].charAt(0).toUpperCase() + words[i].slice(1); } return words.join('').replace(/\\bId\\b/g, 'ID').replace(/\\bTv\\b/g, 'TV'); }
      function sanitizeHtml(value) { var html = String(value == null ? '' : value); html = html.replace(/\\s(?:color|bgcolor)\\s*=\\s*(?:\"[^\"]*\"|'[^']*'|[^\\s>]+)/gi, ''); return html.replace(/\\sstyle\\s*=\\s*(\"([^\"]*)\"|'([^']*)')/gi, function (match, quoted, doubleQuoted, singleQuoted) { var quote = quoted.charAt(0); var style = doubleQuoted !== undefined ? doubleQuoted : singleQuoted; var declarations = style.split(';').filter(function (declaration) { var name = trim(declaration.split(':')[0]).toLowerCase(); return name && name !== 'color' && name !== 'background' && name !== 'background-color'; }); return declarations.length ? ' style=' + quote + declarations.join(';') + quote : ''; }); }
      function toggleCase(value) { var text = String(value == null ? '' : value); var i; var character; for (i = 0; i < text.length; i += 1) { character = text.charAt(i); if (character !== character.toUpperCase() && character === character.toLowerCase()) return text.toUpperCase(); } return text.toLowerCase(); }
      function validateJsonImport(text) { var parsed; var i; var key; var item; var value; try { parsed = JSON.parse(text); } catch (e) { return { ok: false, reason: 'JSON_PARSE_INVALID' }; } if (!parsed || Object.prototype.toString.call(parsed.copyq_items) !== '[object Array]') return { ok: false, reason: 'JSON_SHAPE_INVALID' }; for (i = 0; i < parsed.copyq_items.length; i += 1) { item = parsed.copyq_items[i]; if (!item || Object.prototype.toString.call(item) !== '[object Object]') return { ok: false, reason: 'JSON_SHAPE_INVALID' }; for (key in item) if (Object.prototype.hasOwnProperty.call(item, key)) { value = item[key]; if (typeof value !== 'string' && !(value && Object.prototype.toString.call(value) === '[object Object]' && typeof value.base64 === 'string')) return { ok: false, reason: 'JSON_SHAPE_INVALID' }; } } return { ok: true, value: parsed }; }
      function validateRegex(text) { try { return { ok: true, value: new RegExp(text, 'i') }; } catch (e) { return { ok: false, reason: 'REGEX_INVALID' }; } }
      function runDependency(execute, args) { var result = execute.apply(null, args || []); return result && result.exit_code === 0 ? { ok: true, value: result } : { ok: false, reason: 'DEPENDENCY_FAILED' }; }
      return { isHighConfidenceSecret: isHighConfidenceSecret, isCode: isCode, isArtifact: isArtifact, isMarkdown: isMarkdown, normalizeFrequencyText: normalizeFrequencyText, hashesFor: hashesFor, v2HashesFor: v2HashesFor, legacyKiloHash: legacyKiloHash, recordFrequency: recordFrequency, dismissFrequency: dismissFrequency, restoreFrequency: restoreFrequency, isTrashExpired: isTrashExpired, route: route, sortFrequent: sortFrequent, titleCase: titleCase, sanitizeHtml: sanitizeHtml, toggleCase: toggleCase, validateJsonImport: validateJsonImport, validateRegex: validateRegex, runDependency: runDependency };
    }());

    var html = str(data(mimeHtml)); if (!html) abort();
    var sanitized = CopyQCore.sanitizeHtml(html); if (sanitized === html) abort(); setData(mimeHtml, sanitized);"
4\Input=text/html
4\Automatic=true
4\InMenu=true
4\InternalId=canonical.html-sanitizer
5\Name=Translate to English
5\Command="
    copyq:
    var items = selectedItemsData(); var item = items.length ? items[0] : null; var textData = item && item[mimeText] ? item[mimeText] : input(); var text = str(textData);
    function translationFailure(reason) { notification('.id', 'translate-en', '.title', 'Translate to English', '.message', reason); abort(); } if (!text) translationFailure('TRANSLATE_EMPTY');
    var programFiles = str(env('ProgramFiles')).replace(/\\\\/g, '/'); var localAppData = str(env('LOCALAPPDATA')).replace(/\\\\/g, '/'); var helper = localAppData + '/CopyQCommandSuite/translation/Invoke-CopyQAzureTranslation.ps1'; var encodedText = str(toBase64(textData)); var result = null; try { result = execute('pwsh.exe', '-NoLogo', '-NoProfile', '-NonInteractive', '-File', helper, null, encodedText); } catch (e) {} if (!result || result.exit_code !== 0) { try { result = execute(programFiles + '/PowerShell/7/pwsh.exe', '-NoLogo', '-NoProfile', '-NonInteractive', '-File', helper, null, encodedText); } catch (e2) {} }
    if (!result || result.exit_code !== 0) { var reason = str(result && result.stderr).replace(/\\s+$/, ''); if (!/^(?:TRANSLATE_NOT_CONFIGURED|TRANSLATE_AUTH_FAILED|TRANSLATE_REGION_FAILED|TRANSLATE_RATE_LIMITED|TRANSLATE_FAILED|TRANSLATE_EMPTY)$/.test(reason)) reason = 'TRANSLATE_FAILED'; translationFailure(reason); } var encodedTranslation = str(result.stdout).replace(/\\s+$/, ''); var translated; try { translated = str(fromBase64(encodedTranslation)); } catch (e) { translationFailure('TRANSLATE_FAILED'); } if (!translated) translationFailure('TRANSLATE_EMPTY'); copy(mimeText, translated);"
5\Input=text/plain
5\InMenu=true
5\InternalId=canonical.translate-en
6\Name=Render Markdown
6\Command="
    copyq:
    var CopyQCore = (function () {
      function trim(value) { return String(value == null ? '' : value).replace(/^\\s+|\\s+$/g, ''); }
      function hasFormat(formats, expression) { var i; for (i = 0; i < (formats || []).length; i += 1) if (expression.test(String(formats[i]))) return true; return false; }
      function decode(part) {
        var alphabet = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/';
        var input = part.replace(/-/g, '+').replace(/_/g, '/'); var out = ''; var bits = 0; var bitCount = 0; var i; var n;
        while (input.length % 4) input += '=';
        for (i = 0; i < input.length; i += 1) { if (input.charAt(i) === '=') break; n = alphabet.indexOf(input.charAt(i)); if (n < 0) return null; bits = (bits << 6) | n; bitCount += 6; while (bitCount >= 8) { bitCount -= 8; out += String.fromCharCode((bits >> bitCount) & 255); } }
        return out;
      }
      function validJwt(text) { var parts = text.split('.'); var head; var body; try { head = parts.length === 3 ? JSON.parse(decode(parts[0])) : null; body = parts.length === 3 ? JSON.parse(decode(parts[1])) : null; } catch (e) { return false; } return !!(head && body && typeof head.alg === 'string' && /^[A-Za-z0-9_-]+$/.test(parts[2] || '')); }
      function credentialUrl(text) { return /^https?:\\/\\/[^/\\s:@]+:[^/\\s@]+@/i.test(text) || /[?&](?:access_token|api[_-]?key|password|secret|token)=[^&#\\s]+/i.test(text); }
      function secretFormat(formats) { return hasFormat(formats, /(?:application\\/x-copyq-secret|org\\.nspasteboard\\.concealedtype|clipboard viewer ignore|x-kde-passwordmanagerhint|nspasteboard concealed|mimesecret|com\\.agilebits\\.onepassword|1password|bitwarden|keepass|lastpass|password-manager)/i); }
      function structuredNonSecret(text) {
        return /^https?:\\/\\/\\S+$/i.test(text)
          || /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(text)
          || /^(?:[A-Za-z]:[\\\\/]|\\\\\\\\|\\/)[^\\r\\n]+$/.test(text)
          || /^v?\\d+\\.\\d+\\.\\d+(?:[-+][0-9A-Za-z.-]+)?$/.test(text)
          || /^[A-Z][A-Z0-9]{1,9}-\\d{1,10}$/.test(text);
      }
      function historicalOpaqueSecret(text) {
        var classes = 0;
        if (text.length < 10 || text.length > 3000 || /\\s/.test(text)) return false;
        if (/[a-z]/.test(text)) classes += 1;
        if (/[A-Z]/.test(text)) classes += 1;
        if (/\\d/.test(text)) classes += 1;
        return classes >= 2;
      }
      function isHighConfidenceSecret(value, formats) {
        var text = trim(value);
        if (secretFormat(formats)) return true;
        if (/^-----BEGIN (?:[A-Z0-9 ]+ )?(?:PRIVATE KEY|OPENSSH PRIVATE KEY)-----/m.test(text) || validJwt(text)) return true;
        if (/^(?:sk-[A-Za-z0-9_-]{20,}|ghp_[A-Za-z0-9]{30,}|github_pat_[A-Za-z0-9_]{30,}|glpat-[A-Za-z0-9_-]{20,}|xox[baprs]-[A-Za-z0-9-]{12,}|AIza[0-9A-Za-z_-]{30,}|AKIA[0-9A-Z]{16}|npm_[A-Za-z0-9_-]{20,}|pypi-[A-Za-z0-9_-]{20,})$/.test(text)) return true;
        if (/^authorization:\\s*(?:bearer|basic|token)\\s+\\S{12,}$/i.test(text)) return true;
        if(/^\\s*(?:export\\s+)?(?:[A-Za-z_][A-Za-z0-9_]*(?:PASSWORD|TOKEN|SECRET|API_KEY)[A-Za-z0-9_]*|PASSWORD|TOKEN|SECRET|API_KEY)\\s*=\\s*\\S{12,}\\s*$/i.test(text)) return true;
        if (credentialUrl(text)) return true;
        if (/^[0-9a-f]{32,128}$/i.test(text)) return true;
        if (structuredNonSecret(text)) return false;
        return historicalOpaqueSecret(text);
      }
      function isCode(text) { return /^\\s*(?:import|export|var|const|let|function|class|def|package|using|namespace|#include|#!)\\b/m.test(text) || /(?:=>|===|!==|&&|\\|\\|)/.test(text) || /\\b(?:if|for|while|switch|catch)\\s*\\(/.test(text) || /\\w+\\s*\\([^)]*\\)\\s*\\{/.test(text); }
      function isArtifact(value) {
        var text = trim(value); var parsed; var lines; var matches;
        if (!text) return false;
        if (/^[\\[{]/.test(text)) {
          try {
            parsed = JSON.parse(text);
            if (Object.prototype.toString.call(parsed) === '[object Array]') return parsed.length >= 2;
            if (parsed && Object.prototype.toString.call(parsed) === '[object Object]') return Object.keys(parsed).length >= 2;
          } catch (e) {}
        }
        if (!/[\\r\\n]/.test(text)) return false;
        lines = text.split(/\\r?\\n/).filter(function (line) { return trim(line) !== ''; });
        if (lines.length < 2) return false;
        if (/^diff --git\\s/m.test(text) || /^@@\\s+-\\d+(?:,\\d+)?\\s+\\+\\d+(?:,\\d+)?\\s+@@/m.test(text)) return true;
        if (/---\\s+backtrace\\s+---/i.test(text) || /^Traceback \\(most recent call last\\):/m.test(text)) return true;
        matches = text.match(/^\\s*at\\s+\\S.+$/gm); if (matches && matches.length >= 2) return true;
        if (/^(?:PS\\s+[A-Za-z]:\\\\[^>\\r\\n]*>|[$#>])\\s*\\S/m.test(text)) return true;
        if (/`\\s*$/m.test(text) && /^\\s{2,}-(?:\\w|-)\\S*/m.test(text)) return true;
        matches = text.match(/^(?:\\[[0-2]?\\d:[0-5]\\d:[0-5]\\d\\]|\\d{4}-\\d{2}-\\d{2}[T ][^\\s]+|\\s*(?:TRACE|DEBUG|INFO|WARN|ERROR|FATAL)\\b).+$/gim);
        if (matches && matches.length >= 2) return true;
        if (/^RELEASE_STAGE_FAILED:/m.test(text) && /(?:reason=|backtrace)/i.test(text)) return true;
        matches = text.match(/^\\s*(?:\\[[^\\]\\r\\n]+\\]|[A-Za-z_][A-Za-z0-9_.-]*\\s*(?:=|:)\\s*\\S.*)$/gm);
        return !!(matches && matches.length >= 3);
      }
      function isMarkdown(value) {
        var text = String(value == null ? '' : value); var listItems;
        if (!text) return false;
        if (/(^|\\n) {0,3}#{1,6}[ \\t]+\\S/.test(text)) return true;
        if (/(^|\\n) {0,3}```[^\\n]*\\n[\\s\\S]*\\n {0,3}```/.test(text)) return true;
        if (/\\[[^\\]\\n]+\\]\\((?:https?:\\/\\/|\\/|#|\\.{0,2}\\/)[^)]+\\)/.test(text)) return true;
        if (/(^|\\n) {0,3}>[ \\t]+\\S/.test(text)) return true;
        if (/(^|\\n)\\s*\\|?.+\\|.+\\n\\s*\\|?\\s*:?-{3,}:?\\s*\\|/.test(text)) return true;
        listItems = text.match(/(^|\\n) {0,3}(?:[-+*]|\\d+[.)])[ \\t]+\\S/g);
        return !!(listItems && listItems.length >= 2);
      }
      function fnv1a(text) { var hash = 2166136261; var i; for (i = 0; i < text.length; i += 1) { hash ^= text.charCodeAt(i); hash = (hash * 16777619) >>> 0; } return (hash >>> 0).toString(16); }
      function djb2(text) { var hash = 5381; var i; for (i = 0; i < text.length; i += 1) hash = ((hash << 5) + hash + text.charCodeAt(i)) | 0; return (hash >>> 0).toString(36); }
      function legacyKiloHash(text) { text = String(text == null ? '' : text); return text.length + '_' + djb2(text); }
      function normalizeFrequencyText(text) { return trim(text); }
      function v2HashesFor(text) { text = String(text == null ? '' : text); return { version: 2, first: fnv1a(text), second: djb2(text), key: 'v2:' + fnv1a(text) + ':' + djb2(text) }; }
      function hashesFor(text) { text = normalizeFrequencyText(text); return { version: 3, first: fnv1a(text), second: djb2(text), key: 'v3:' + fnv1a(text) + ':' + djb2(text) }; }
      function cloneCounters(counters) { var copy = {}; var key; for (key in counters || {}) if (Object.prototype.hasOwnProperty.call(counters, key)) copy[key] = { version: counters[key].version, first: counters[key].first, second: counters[key].second, count: counters[key].count, lastUsed: counters[key].lastUsed }; return copy; }
      function pruneCounters(state, maximum) {
        var keys = []; var key;
        for (key in state.counters) if (Object.prototype.hasOwnProperty.call(state.counters, key)) keys.push(key);
        keys.sort(function (a, b) { return (state.counters[a].lastUsed - state.counters[b].lastUsed) || (a < b ? -1 : 1); });
        while (keys.length > maximum) delete state.counters[keys.shift()];
      }
      function recordFrequency(state, v2State, legacy, text) {
        var next = { version: 3, counters: cloneCounters((state || {}).counters), clock: Number((state || {}).clock || 0) };
        var canonicalText = normalizeFrequencyText(text); var hash = hashesFor(canonicalText); var oldHash = v2HashesFor(canonicalText); var oldKey = legacyKiloHash(canonicalText); var entry; var imported = 0;
        if (!canonicalText) return { state: next, result: { count: 0, promote: false, key: '', canonicalText: '', hashes: hash } };
        entry = next.counters[hash.key];
        if (!entry) {
          if (v2State && v2State.counters && v2State.counters[oldHash.key]) imported += Number(v2State.counters[oldHash.key].count || 0);
          if (legacy && legacy[oldKey] !== undefined) imported += Number(legacy[oldKey] || 0);
          entry = { version: 3, first: hash.first, second: hash.second, count: imported, lastUsed: 0 };
        }
        entry.count += 1; entry.lastUsed = ++next.clock; next.counters[hash.key] = entry;
        pruneCounters(next, 4096);
        return { state: next, result: { count: entry.count, promote: entry.count >= 6, key: hash.key, canonicalText: canonicalText, hashes: hash } };
      }
      function dismissFrequency(state, key) {
        var next = { version: 3, counters: cloneCounters((state || {}).counters), clock: Number((state || {}).clock || 0) }; var entry = next.counters[key]; var parts = String(key || '').split(':'); var savedCount = 0;
        if (!entry) entry = { version: 3, first: parts[1] || '', second: parts[2] || '', count: 0, lastUsed: 0 };
        savedCount = Number(entry.count || 0); entry.count = 0; entry.lastUsed = ++next.clock; next.counters[key] = entry; pruneCounters(next, 4096);
        return { state: next, savedCount: savedCount };
      }
      function restoreFrequency(state, key, savedCount) {
        var next = { version: 3, counters: cloneCounters((state || {}).counters), clock: Number((state || {}).clock || 0) }; var parts = String(key || '').split(':'); var entry = next.counters[key];
        if (!entry) entry = { version: 3, first: parts[1] || '', second: parts[2] || '', count: 0, lastUsed: 0 };
        entry.count = Number(entry.count || 0) + Number(savedCount || 0); entry.lastUsed = ++next.clock; next.counters[key] = entry; pruneCounters(next, 4096);
        return { state: next, count: entry.count };
      }
      function isTrashExpired(deletedAt, now, retentionMs) {
        var deleted = Date.parse(String(deletedAt || ''));
        return isFinite(deleted) && Number(now) - deleted >= Number(retentionMs);
      }
      function route(input) { var text = String(input && input.text != null ? input.text : ''); var formats = input && input.formats || []; var eligible = normalizeFrequencyText(text) !== ''; if (hasFormat(formats, /^image\\//i) || (input && input.mimeOwner && hasFormat(formats, new RegExp('^' + input.mimeOwner.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&') + '$'))) || (input && input.mimeHidden && hasFormat(formats, new RegExp('^' + input.mimeHidden.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&') + '$')))) return { action: 'passthrough', frequencyEligible: false }; if (isHighConfidenceSecret(text, formats)) return { action: 'ignored', frequencyEligible: false }; if (text.length >= 5000) return { action: 'big', frequencyEligible: eligible }; if (isArtifact(text)) return { action: 'artifacts', frequencyEligible: eligible }; if (isCode(text)) return { action: 'code', frequencyEligible: eligible }; if (/^https?:\\/\\/\\S+$/i.test(text)) return { action: 'passthrough', frequencyEligible: eligible }; return { action: 'default', frequencyEligible: eligible }; }
      function sortFrequent(items) { return items.slice().sort(function (a, b) { return (b.count - a.count) || (a.order - b.order); }); }
      function titleCase(text) { var words = String(text).toLowerCase().split(/(\\s+)/); var minor = { a: 1, an: 1, the: 1, and: 1, but: 1, or: 1, for: 1, nor: 1, as: 1, at: 1, by: 1, from: 1, in: 1, into: 1, near: 1, of: 1, on: 1, onto: 1, to: 1, with: 1 }; var first = -1; var last = -1; var i; for (i = 0; i < words.length; i += 1) if (!/^\\s+$/.test(words[i])) { if (first < 0) first = i; last = i; } for (i = 0; i < words.length; i += 1) if (!/^\\s+$/.test(words[i])) { if (i !== first && i !== last && minor[words[i]]) continue; words[i] = words[i].charAt(0).toUpperCase() + words[i].slice(1); } return words.join('').replace(/\\bId\\b/g, 'ID').replace(/\\bTv\\b/g, 'TV'); }
      function sanitizeHtml(value) { var html = String(value == null ? '' : value); html = html.replace(/\\s(?:color|bgcolor)\\s*=\\s*(?:\"[^\"]*\"|'[^']*'|[^\\s>]+)/gi, ''); return html.replace(/\\sstyle\\s*=\\s*(\"([^\"]*)\"|'([^']*)')/gi, function (match, quoted, doubleQuoted, singleQuoted) { var quote = quoted.charAt(0); var style = doubleQuoted !== undefined ? doubleQuoted : singleQuoted; var declarations = style.split(';').filter(function (declaration) { var name = trim(declaration.split(':')[0]).toLowerCase(); return name && name !== 'color' && name !== 'background' && name !== 'background-color'; }); return declarations.length ? ' style=' + quote + declarations.join(';') + quote : ''; }); }
      function toggleCase(value) { var text = String(value == null ? '' : value); var i; var character; for (i = 0; i < text.length; i += 1) { character = text.charAt(i); if (character !== character.toUpperCase() && character === character.toLowerCase()) return text.toUpperCase(); } return text.toLowerCase(); }
      function validateJsonImport(text) { var parsed; var i; var key; var item; var value; try { parsed = JSON.parse(text); } catch (e) { return { ok: false, reason: 'JSON_PARSE_INVALID' }; } if (!parsed || Object.prototype.toString.call(parsed.copyq_items) !== '[object Array]') return { ok: false, reason: 'JSON_SHAPE_INVALID' }; for (i = 0; i < parsed.copyq_items.length; i += 1) { item = parsed.copyq_items[i]; if (!item || Object.prototype.toString.call(item) !== '[object Object]') return { ok: false, reason: 'JSON_SHAPE_INVALID' }; for (key in item) if (Object.prototype.hasOwnProperty.call(item, key)) { value = item[key]; if (typeof value !== 'string' && !(value && Object.prototype.toString.call(value) === '[object Object]' && typeof value.base64 === 'string')) return { ok: false, reason: 'JSON_SHAPE_INVALID' }; } } return { ok: true, value: parsed }; }
      function validateRegex(text) { try { return { ok: true, value: new RegExp(text, 'i') }; } catch (e) { return { ok: false, reason: 'REGEX_INVALID' }; } }
      function runDependency(execute, args) { var result = execute.apply(null, args || []); return result && result.exit_code === 0 ? { ok: true, value: result } : { ok: false, reason: 'DEPENDENCY_FAILED' }; }
      return { isHighConfidenceSecret: isHighConfidenceSecret, isCode: isCode, isArtifact: isArtifact, isMarkdown: isMarkdown, normalizeFrequencyText: normalizeFrequencyText, hashesFor: hashesFor, v2HashesFor: v2HashesFor, legacyKiloHash: legacyKiloHash, recordFrequency: recordFrequency, dismissFrequency: dismissFrequency, restoreFrequency: restoreFrequency, isTrashExpired: isTrashExpired, route: route, sortFrequent: sortFrequent, titleCase: titleCase, sanitizeHtml: sanitizeHtml, toggleCase: toggleCase, validateJsonImport: validateJsonImport, validateRegex: validateRegex, runDependency: runDependency };
    }());

    var text = data(mimeText); if (!text || !CopyQCore.isMarkdown(str(text))) abort();
    var appData = str(env('APPDATA')).replace(/\\\\/g, '/'); var result = null; try { result = execute('marked.cmd', null, text); } catch (e) {} if (!result || result.exit_code !== 0) { try { result = execute(appData + '/npm/marked.cmd', null, text); } catch (e2) {} }
    if (!result || result.exit_code !== 0) { notification('.id', 'markdown', '.message', 'MARKDOWN_FAILED'); abort(); } setData(mimeHtml, result.stdout);"
6\Input=text/plain
6\Automatic=true
6\InMenu=true
6\InternalId=canonical.markdown-render
7\Name=Highlight Code
7\Command="
    copyq:
    var text = data(mimeText); if (!text) abort();
    var script = 'import re\\nimport sys\\nfrom pygments import highlight\\nfrom pygments.lexers import guess_lexer\\nfrom pygments.formatters import HtmlFormatter\\ncode = sys.stdin.read()\\nquote = chr(34)\\nfragment = highlight(code, guess_lexer(code), HtmlFormatter(noclasses=True, nowrap=True, style=\"xcode\"))\\npattern = \"<span style=\" + quote + \"color: (#[0-9A-Fa-f]{3,6})\" + quote + \">\"\\nfragment = re.sub(pattern, lambda match: \"<font color=\" + quote + match.group(1) + quote + \">\", fragment).replace(\"</span>\", \"</font>\")\\nstyle = \"font-family: Consolas; font-size: 14px; white-space: pre-wrap; margin-top: 0px; margin-bottom: 0px;\"\\nhtml = \"<table width=\" + quote + \"100%\" + quote + \" border=\" + quote + \"0\" + quote + \" cellspacing=\" + quote + \"0\" + quote + \" cellpadding=\" + quote + \"8\" + quote + \" bgcolor=\" + quote + \"#ffffff\" + quote + \"><tr><td bgcolor=\" + quote + \"#ffffff\" + quote + \"><pre style=\" + quote + style + quote + \">\" + fragment + \"</pre></td></tr></table>\"\\nsys.stdout.write(html)';
    var result = null; try { result = execute('py.exe', '-3', '-c', script, null, text); } catch (e) {} if (!result || result.exit_code !== 0) { try { result = execute('python.exe', '-c', script, null, text); } catch (e2) {} } if (!result || result.exit_code !== 0) { notification('.id', 'pygments', '.title', 'Highlight Code', '.message', 'PYGMENTS_FAILED'); abort(); } setData(mimeHtml, result.stdout);"
7\Input=text/plain
7\InMenu=true
7\InternalId=canonical.pygments-highlight
8\Name=Copy Text in Image
8\Command="
    copyq:
    var items = selectedItemsData(); var item = items.length ? items[0] : null; var png = item && item['image/png'] ? item['image/png'] : data('image/png'); if (!png || png.size() === 0) png = input();
    if (!png || png.size() === 0) { notification('.id', 'ocr', '.title', 'Copy Text in Image', '.message', 'OCR_NO_PNG'); abort(); }
    var programFiles = str(env('ProgramFiles')).replace(/\\\\/g, '/'); var result = null; try { result = execute('tesseract.exe', 'stdin', 'stdout', '-l', 'eng+heb', '--oem', '3', null, png); } catch (e) {} if (!result || result.exit_code !== 0) { try { result = execute(programFiles + '/Tesseract-OCR/tesseract.exe', 'stdin', 'stdout', '-l', 'eng+heb', '--oem', '3', null, png); } catch (e2) {} }
    if (!result || result.exit_code !== 0) { notification('.id', 'ocr', '.title', 'Copy Text in Image', '.message', 'OCR_FAILED'); abort(); } var text = str(result.stdout).replace(/\\s+$/, ''); if (!text) { notification('.id', 'ocr', '.title', 'Copy Text in Image', '.message', 'OCR_NO_TEXT'); abort(); } copy(mimeText, text);"
8\Input=image/png
8\InMenu=true
8\IsGlobalShortcut=true
8\GlobalShortcut=meta+ctrl+t
8\InternalId=canonical.ocr
9\Name=Copy Items as JSON
9\Command="
    copyq:
    function out(d) { var s = str(d); return d.equals(new ByteArray(s)) ? s : { base64: toBase64(d) }; }
    var items = selectedItemsData(); for (var i in items) for (var f in items[i]) items[i][f] = out(items[i][f]); copy(JSON.stringify({ copyq_items: items }, null, 2));"
9\Input=text/plain
9\InMenu=true
9\InternalId=canonical.copy-json
10\Name=Paste Items from JSON
10\Command="
    copyq:
    var CopyQCore = (function () {
      function trim(value) { return String(value == null ? '' : value).replace(/^\\s+|\\s+$/g, ''); }
      function hasFormat(formats, expression) { var i; for (i = 0; i < (formats || []).length; i += 1) if (expression.test(String(formats[i]))) return true; return false; }
      function decode(part) {
        var alphabet = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/';
        var input = part.replace(/-/g, '+').replace(/_/g, '/'); var out = ''; var bits = 0; var bitCount = 0; var i; var n;
        while (input.length % 4) input += '=';
        for (i = 0; i < input.length; i += 1) { if (input.charAt(i) === '=') break; n = alphabet.indexOf(input.charAt(i)); if (n < 0) return null; bits = (bits << 6) | n; bitCount += 6; while (bitCount >= 8) { bitCount -= 8; out += String.fromCharCode((bits >> bitCount) & 255); } }
        return out;
      }
      function validJwt(text) { var parts = text.split('.'); var head; var body; try { head = parts.length === 3 ? JSON.parse(decode(parts[0])) : null; body = parts.length === 3 ? JSON.parse(decode(parts[1])) : null; } catch (e) { return false; } return !!(head && body && typeof head.alg === 'string' && /^[A-Za-z0-9_-]+$/.test(parts[2] || '')); }
      function credentialUrl(text) { return /^https?:\\/\\/[^/\\s:@]+:[^/\\s@]+@/i.test(text) || /[?&](?:access_token|api[_-]?key|password|secret|token)=[^&#\\s]+/i.test(text); }
      function secretFormat(formats) { return hasFormat(formats, /(?:application\\/x-copyq-secret|org\\.nspasteboard\\.concealedtype|clipboard viewer ignore|x-kde-passwordmanagerhint|nspasteboard concealed|mimesecret|com\\.agilebits\\.onepassword|1password|bitwarden|keepass|lastpass|password-manager)/i); }
      function structuredNonSecret(text) {
        return /^https?:\\/\\/\\S+$/i.test(text)
          || /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(text)
          || /^(?:[A-Za-z]:[\\\\/]|\\\\\\\\|\\/)[^\\r\\n]+$/.test(text)
          || /^v?\\d+\\.\\d+\\.\\d+(?:[-+][0-9A-Za-z.-]+)?$/.test(text)
          || /^[A-Z][A-Z0-9]{1,9}-\\d{1,10}$/.test(text);
      }
      function historicalOpaqueSecret(text) {
        var classes = 0;
        if (text.length < 10 || text.length > 3000 || /\\s/.test(text)) return false;
        if (/[a-z]/.test(text)) classes += 1;
        if (/[A-Z]/.test(text)) classes += 1;
        if (/\\d/.test(text)) classes += 1;
        return classes >= 2;
      }
      function isHighConfidenceSecret(value, formats) {
        var text = trim(value);
        if (secretFormat(formats)) return true;
        if (/^-----BEGIN (?:[A-Z0-9 ]+ )?(?:PRIVATE KEY|OPENSSH PRIVATE KEY)-----/m.test(text) || validJwt(text)) return true;
        if (/^(?:sk-[A-Za-z0-9_-]{20,}|ghp_[A-Za-z0-9]{30,}|github_pat_[A-Za-z0-9_]{30,}|glpat-[A-Za-z0-9_-]{20,}|xox[baprs]-[A-Za-z0-9-]{12,}|AIza[0-9A-Za-z_-]{30,}|AKIA[0-9A-Z]{16}|npm_[A-Za-z0-9_-]{20,}|pypi-[A-Za-z0-9_-]{20,})$/.test(text)) return true;
        if (/^authorization:\\s*(?:bearer|basic|token)\\s+\\S{12,}$/i.test(text)) return true;
        if(/^\\s*(?:export\\s+)?(?:[A-Za-z_][A-Za-z0-9_]*(?:PASSWORD|TOKEN|SECRET|API_KEY)[A-Za-z0-9_]*|PASSWORD|TOKEN|SECRET|API_KEY)\\s*=\\s*\\S{12,}\\s*$/i.test(text)) return true;
        if (credentialUrl(text)) return true;
        if (/^[0-9a-f]{32,128}$/i.test(text)) return true;
        if (structuredNonSecret(text)) return false;
        return historicalOpaqueSecret(text);
      }
      function isCode(text) { return /^\\s*(?:import|export|var|const|let|function|class|def|package|using|namespace|#include|#!)\\b/m.test(text) || /(?:=>|===|!==|&&|\\|\\|)/.test(text) || /\\b(?:if|for|while|switch|catch)\\s*\\(/.test(text) || /\\w+\\s*\\([^)]*\\)\\s*\\{/.test(text); }
      function isArtifact(value) {
        var text = trim(value); var parsed; var lines; var matches;
        if (!text) return false;
        if (/^[\\[{]/.test(text)) {
          try {
            parsed = JSON.parse(text);
            if (Object.prototype.toString.call(parsed) === '[object Array]') return parsed.length >= 2;
            if (parsed && Object.prototype.toString.call(parsed) === '[object Object]') return Object.keys(parsed).length >= 2;
          } catch (e) {}
        }
        if (!/[\\r\\n]/.test(text)) return false;
        lines = text.split(/\\r?\\n/).filter(function (line) { return trim(line) !== ''; });
        if (lines.length < 2) return false;
        if (/^diff --git\\s/m.test(text) || /^@@\\s+-\\d+(?:,\\d+)?\\s+\\+\\d+(?:,\\d+)?\\s+@@/m.test(text)) return true;
        if (/---\\s+backtrace\\s+---/i.test(text) || /^Traceback \\(most recent call last\\):/m.test(text)) return true;
        matches = text.match(/^\\s*at\\s+\\S.+$/gm); if (matches && matches.length >= 2) return true;
        if (/^(?:PS\\s+[A-Za-z]:\\\\[^>\\r\\n]*>|[$#>])\\s*\\S/m.test(text)) return true;
        if (/`\\s*$/m.test(text) && /^\\s{2,}-(?:\\w|-)\\S*/m.test(text)) return true;
        matches = text.match(/^(?:\\[[0-2]?\\d:[0-5]\\d:[0-5]\\d\\]|\\d{4}-\\d{2}-\\d{2}[T ][^\\s]+|\\s*(?:TRACE|DEBUG|INFO|WARN|ERROR|FATAL)\\b).+$/gim);
        if (matches && matches.length >= 2) return true;
        if (/^RELEASE_STAGE_FAILED:/m.test(text) && /(?:reason=|backtrace)/i.test(text)) return true;
        matches = text.match(/^\\s*(?:\\[[^\\]\\r\\n]+\\]|[A-Za-z_][A-Za-z0-9_.-]*\\s*(?:=|:)\\s*\\S.*)$/gm);
        return !!(matches && matches.length >= 3);
      }
      function isMarkdown(value) {
        var text = String(value == null ? '' : value); var listItems;
        if (!text) return false;
        if (/(^|\\n) {0,3}#{1,6}[ \\t]+\\S/.test(text)) return true;
        if (/(^|\\n) {0,3}```[^\\n]*\\n[\\s\\S]*\\n {0,3}```/.test(text)) return true;
        if (/\\[[^\\]\\n]+\\]\\((?:https?:\\/\\/|\\/|#|\\.{0,2}\\/)[^)]+\\)/.test(text)) return true;
        if (/(^|\\n) {0,3}>[ \\t]+\\S/.test(text)) return true;
        if (/(^|\\n)\\s*\\|?.+\\|.+\\n\\s*\\|?\\s*:?-{3,}:?\\s*\\|/.test(text)) return true;
        listItems = text.match(/(^|\\n) {0,3}(?:[-+*]|\\d+[.)])[ \\t]+\\S/g);
        return !!(listItems && listItems.length >= 2);
      }
      function fnv1a(text) { var hash = 2166136261; var i; for (i = 0; i < text.length; i += 1) { hash ^= text.charCodeAt(i); hash = (hash * 16777619) >>> 0; } return (hash >>> 0).toString(16); }
      function djb2(text) { var hash = 5381; var i; for (i = 0; i < text.length; i += 1) hash = ((hash << 5) + hash + text.charCodeAt(i)) | 0; return (hash >>> 0).toString(36); }
      function legacyKiloHash(text) { text = String(text == null ? '' : text); return text.length + '_' + djb2(text); }
      function normalizeFrequencyText(text) { return trim(text); }
      function v2HashesFor(text) { text = String(text == null ? '' : text); return { version: 2, first: fnv1a(text), second: djb2(text), key: 'v2:' + fnv1a(text) + ':' + djb2(text) }; }
      function hashesFor(text) { text = normalizeFrequencyText(text); return { version: 3, first: fnv1a(text), second: djb2(text), key: 'v3:' + fnv1a(text) + ':' + djb2(text) }; }
      function cloneCounters(counters) { var copy = {}; var key; for (key in counters || {}) if (Object.prototype.hasOwnProperty.call(counters, key)) copy[key] = { version: counters[key].version, first: counters[key].first, second: counters[key].second, count: counters[key].count, lastUsed: counters[key].lastUsed }; return copy; }
      function pruneCounters(state, maximum) {
        var keys = []; var key;
        for (key in state.counters) if (Object.prototype.hasOwnProperty.call(state.counters, key)) keys.push(key);
        keys.sort(function (a, b) { return (state.counters[a].lastUsed - state.counters[b].lastUsed) || (a < b ? -1 : 1); });
        while (keys.length > maximum) delete state.counters[keys.shift()];
      }
      function recordFrequency(state, v2State, legacy, text) {
        var next = { version: 3, counters: cloneCounters((state || {}).counters), clock: Number((state || {}).clock || 0) };
        var canonicalText = normalizeFrequencyText(text); var hash = hashesFor(canonicalText); var oldHash = v2HashesFor(canonicalText); var oldKey = legacyKiloHash(canonicalText); var entry; var imported = 0;
        if (!canonicalText) return { state: next, result: { count: 0, promote: false, key: '', canonicalText: '', hashes: hash } };
        entry = next.counters[hash.key];
        if (!entry) {
          if (v2State && v2State.counters && v2State.counters[oldHash.key]) imported += Number(v2State.counters[oldHash.key].count || 0);
          if (legacy && legacy[oldKey] !== undefined) imported += Number(legacy[oldKey] || 0);
          entry = { version: 3, first: hash.first, second: hash.second, count: imported, lastUsed: 0 };
        }
        entry.count += 1; entry.lastUsed = ++next.clock; next.counters[hash.key] = entry;
        pruneCounters(next, 4096);
        return { state: next, result: { count: entry.count, promote: entry.count >= 6, key: hash.key, canonicalText: canonicalText, hashes: hash } };
      }
      function dismissFrequency(state, key) {
        var next = { version: 3, counters: cloneCounters((state || {}).counters), clock: Number((state || {}).clock || 0) }; var entry = next.counters[key]; var parts = String(key || '').split(':'); var savedCount = 0;
        if (!entry) entry = { version: 3, first: parts[1] || '', second: parts[2] || '', count: 0, lastUsed: 0 };
        savedCount = Number(entry.count || 0); entry.count = 0; entry.lastUsed = ++next.clock; next.counters[key] = entry; pruneCounters(next, 4096);
        return { state: next, savedCount: savedCount };
      }
      function restoreFrequency(state, key, savedCount) {
        var next = { version: 3, counters: cloneCounters((state || {}).counters), clock: Number((state || {}).clock || 0) }; var parts = String(key || '').split(':'); var entry = next.counters[key];
        if (!entry) entry = { version: 3, first: parts[1] || '', second: parts[2] || '', count: 0, lastUsed: 0 };
        entry.count = Number(entry.count || 0) + Number(savedCount || 0); entry.lastUsed = ++next.clock; next.counters[key] = entry; pruneCounters(next, 4096);
        return { state: next, count: entry.count };
      }
      function isTrashExpired(deletedAt, now, retentionMs) {
        var deleted = Date.parse(String(deletedAt || ''));
        return isFinite(deleted) && Number(now) - deleted >= Number(retentionMs);
      }
      function route(input) { var text = String(input && input.text != null ? input.text : ''); var formats = input && input.formats || []; var eligible = normalizeFrequencyText(text) !== ''; if (hasFormat(formats, /^image\\//i) || (input && input.mimeOwner && hasFormat(formats, new RegExp('^' + input.mimeOwner.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&') + '$'))) || (input && input.mimeHidden && hasFormat(formats, new RegExp('^' + input.mimeHidden.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&') + '$')))) return { action: 'passthrough', frequencyEligible: false }; if (isHighConfidenceSecret(text, formats)) return { action: 'ignored', frequencyEligible: false }; if (text.length >= 5000) return { action: 'big', frequencyEligible: eligible }; if (isArtifact(text)) return { action: 'artifacts', frequencyEligible: eligible }; if (isCode(text)) return { action: 'code', frequencyEligible: eligible }; if (/^https?:\\/\\/\\S+$/i.test(text)) return { action: 'passthrough', frequencyEligible: eligible }; return { action: 'default', frequencyEligible: eligible }; }
      function sortFrequent(items) { return items.slice().sort(function (a, b) { return (b.count - a.count) || (a.order - b.order); }); }
      function titleCase(text) { var words = String(text).toLowerCase().split(/(\\s+)/); var minor = { a: 1, an: 1, the: 1, and: 1, but: 1, or: 1, for: 1, nor: 1, as: 1, at: 1, by: 1, from: 1, in: 1, into: 1, near: 1, of: 1, on: 1, onto: 1, to: 1, with: 1 }; var first = -1; var last = -1; var i; for (i = 0; i < words.length; i += 1) if (!/^\\s+$/.test(words[i])) { if (first < 0) first = i; last = i; } for (i = 0; i < words.length; i += 1) if (!/^\\s+$/.test(words[i])) { if (i !== first && i !== last && minor[words[i]]) continue; words[i] = words[i].charAt(0).toUpperCase() + words[i].slice(1); } return words.join('').replace(/\\bId\\b/g, 'ID').replace(/\\bTv\\b/g, 'TV'); }
      function sanitizeHtml(value) { var html = String(value == null ? '' : value); html = html.replace(/\\s(?:color|bgcolor)\\s*=\\s*(?:\"[^\"]*\"|'[^']*'|[^\\s>]+)/gi, ''); return html.replace(/\\sstyle\\s*=\\s*(\"([^\"]*)\"|'([^']*)')/gi, function (match, quoted, doubleQuoted, singleQuoted) { var quote = quoted.charAt(0); var style = doubleQuoted !== undefined ? doubleQuoted : singleQuoted; var declarations = style.split(';').filter(function (declaration) { var name = trim(declaration.split(':')[0]).toLowerCase(); return name && name !== 'color' && name !== 'background' && name !== 'background-color'; }); return declarations.length ? ' style=' + quote + declarations.join(';') + quote : ''; }); }
      function toggleCase(value) { var text = String(value == null ? '' : value); var i; var character; for (i = 0; i < text.length; i += 1) { character = text.charAt(i); if (character !== character.toUpperCase() && character === character.toLowerCase()) return text.toUpperCase(); } return text.toLowerCase(); }
      function validateJsonImport(text) { var parsed; var i; var key; var item; var value; try { parsed = JSON.parse(text); } catch (e) { return { ok: false, reason: 'JSON_PARSE_INVALID' }; } if (!parsed || Object.prototype.toString.call(parsed.copyq_items) !== '[object Array]') return { ok: false, reason: 'JSON_SHAPE_INVALID' }; for (i = 0; i < parsed.copyq_items.length; i += 1) { item = parsed.copyq_items[i]; if (!item || Object.prototype.toString.call(item) !== '[object Object]') return { ok: false, reason: 'JSON_SHAPE_INVALID' }; for (key in item) if (Object.prototype.hasOwnProperty.call(item, key)) { value = item[key]; if (typeof value !== 'string' && !(value && Object.prototype.toString.call(value) === '[object Object]' && typeof value.base64 === 'string')) return { ok: false, reason: 'JSON_SHAPE_INVALID' }; } } return { ok: true, value: parsed }; }
      function validateRegex(text) { try { return { ok: true, value: new RegExp(text, 'i') }; } catch (e) { return { ok: false, reason: 'REGEX_INVALID' }; } }
      function runDependency(execute, args) { var result = execute.apply(null, args || []); return result && result.exit_code === 0 ? { ok: true, value: result } : { ok: false, reason: 'DEPENDENCY_FAILED' }; }
      return { isHighConfidenceSecret: isHighConfidenceSecret, isCode: isCode, isArtifact: isArtifact, isMarkdown: isMarkdown, normalizeFrequencyText: normalizeFrequencyText, hashesFor: hashesFor, v2HashesFor: v2HashesFor, legacyKiloHash: legacyKiloHash, recordFrequency: recordFrequency, dismissFrequency: dismissFrequency, restoreFrequency: restoreFrequency, isTrashExpired: isTrashExpired, route: route, sortFrequent: sortFrequent, titleCase: titleCase, sanitizeHtml: sanitizeHtml, toggleCase: toggleCase, validateJsonImport: validateJsonImport, validateRegex: validateRegex, runDependency: runDependency };
    }());

    var checked = CopyQCore.validateJsonImport(str(clipboard())); if (!checked.ok) { notification('.id', 'json', '.message', checked.reason); abort(); }
    function incoming(d) { return typeof d === 'string' ? new ByteArray(d) : fromBase64(d.base64); } for (var i in checked.value.copyq_items) { var item = checked.value.copyq_items[i]; for (var f in item) item[f] = incoming(item[f]); setItem(i, item); }"
10\Input=text/plain
10\InMenu=true
10\InternalId=canonical.paste-json
11\Name=Search All Tabs
11\Command="
    copyq:
    var CopyQCore = (function () {
      function trim(value) { return String(value == null ? '' : value).replace(/^\\s+|\\s+$/g, ''); }
      function hasFormat(formats, expression) { var i; for (i = 0; i < (formats || []).length; i += 1) if (expression.test(String(formats[i]))) return true; return false; }
      function decode(part) {
        var alphabet = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/';
        var input = part.replace(/-/g, '+').replace(/_/g, '/'); var out = ''; var bits = 0; var bitCount = 0; var i; var n;
        while (input.length % 4) input += '=';
        for (i = 0; i < input.length; i += 1) { if (input.charAt(i) === '=') break; n = alphabet.indexOf(input.charAt(i)); if (n < 0) return null; bits = (bits << 6) | n; bitCount += 6; while (bitCount >= 8) { bitCount -= 8; out += String.fromCharCode((bits >> bitCount) & 255); } }
        return out;
      }
      function validJwt(text) { var parts = text.split('.'); var head; var body; try { head = parts.length === 3 ? JSON.parse(decode(parts[0])) : null; body = parts.length === 3 ? JSON.parse(decode(parts[1])) : null; } catch (e) { return false; } return !!(head && body && typeof head.alg === 'string' && /^[A-Za-z0-9_-]+$/.test(parts[2] || '')); }
      function credentialUrl(text) { return /^https?:\\/\\/[^/\\s:@]+:[^/\\s@]+@/i.test(text) || /[?&](?:access_token|api[_-]?key|password|secret|token)=[^&#\\s]+/i.test(text); }
      function secretFormat(formats) { return hasFormat(formats, /(?:application\\/x-copyq-secret|org\\.nspasteboard\\.concealedtype|clipboard viewer ignore|x-kde-passwordmanagerhint|nspasteboard concealed|mimesecret|com\\.agilebits\\.onepassword|1password|bitwarden|keepass|lastpass|password-manager)/i); }
      function structuredNonSecret(text) {
        return /^https?:\\/\\/\\S+$/i.test(text)
          || /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(text)
          || /^(?:[A-Za-z]:[\\\\/]|\\\\\\\\|\\/)[^\\r\\n]+$/.test(text)
          || /^v?\\d+\\.\\d+\\.\\d+(?:[-+][0-9A-Za-z.-]+)?$/.test(text)
          || /^[A-Z][A-Z0-9]{1,9}-\\d{1,10}$/.test(text);
      }
      function historicalOpaqueSecret(text) {
        var classes = 0;
        if (text.length < 10 || text.length > 3000 || /\\s/.test(text)) return false;
        if (/[a-z]/.test(text)) classes += 1;
        if (/[A-Z]/.test(text)) classes += 1;
        if (/\\d/.test(text)) classes += 1;
        return classes >= 2;
      }
      function isHighConfidenceSecret(value, formats) {
        var text = trim(value);
        if (secretFormat(formats)) return true;
        if (/^-----BEGIN (?:[A-Z0-9 ]+ )?(?:PRIVATE KEY|OPENSSH PRIVATE KEY)-----/m.test(text) || validJwt(text)) return true;
        if (/^(?:sk-[A-Za-z0-9_-]{20,}|ghp_[A-Za-z0-9]{30,}|github_pat_[A-Za-z0-9_]{30,}|glpat-[A-Za-z0-9_-]{20,}|xox[baprs]-[A-Za-z0-9-]{12,}|AIza[0-9A-Za-z_-]{30,}|AKIA[0-9A-Z]{16}|npm_[A-Za-z0-9_-]{20,}|pypi-[A-Za-z0-9_-]{20,})$/.test(text)) return true;
        if (/^authorization:\\s*(?:bearer|basic|token)\\s+\\S{12,}$/i.test(text)) return true;
        if(/^\\s*(?:export\\s+)?(?:[A-Za-z_][A-Za-z0-9_]*(?:PASSWORD|TOKEN|SECRET|API_KEY)[A-Za-z0-9_]*|PASSWORD|TOKEN|SECRET|API_KEY)\\s*=\\s*\\S{12,}\\s*$/i.test(text)) return true;
        if (credentialUrl(text)) return true;
        if (/^[0-9a-f]{32,128}$/i.test(text)) return true;
        if (structuredNonSecret(text)) return false;
        return historicalOpaqueSecret(text);
      }
      function isCode(text) { return /^\\s*(?:import|export|var|const|let|function|class|def|package|using|namespace|#include|#!)\\b/m.test(text) || /(?:=>|===|!==|&&|\\|\\|)/.test(text) || /\\b(?:if|for|while|switch|catch)\\s*\\(/.test(text) || /\\w+\\s*\\([^)]*\\)\\s*\\{/.test(text); }
      function isArtifact(value) {
        var text = trim(value); var parsed; var lines; var matches;
        if (!text) return false;
        if (/^[\\[{]/.test(text)) {
          try {
            parsed = JSON.parse(text);
            if (Object.prototype.toString.call(parsed) === '[object Array]') return parsed.length >= 2;
            if (parsed && Object.prototype.toString.call(parsed) === '[object Object]') return Object.keys(parsed).length >= 2;
          } catch (e) {}
        }
        if (!/[\\r\\n]/.test(text)) return false;
        lines = text.split(/\\r?\\n/).filter(function (line) { return trim(line) !== ''; });
        if (lines.length < 2) return false;
        if (/^diff --git\\s/m.test(text) || /^@@\\s+-\\d+(?:,\\d+)?\\s+\\+\\d+(?:,\\d+)?\\s+@@/m.test(text)) return true;
        if (/---\\s+backtrace\\s+---/i.test(text) || /^Traceback \\(most recent call last\\):/m.test(text)) return true;
        matches = text.match(/^\\s*at\\s+\\S.+$/gm); if (matches && matches.length >= 2) return true;
        if (/^(?:PS\\s+[A-Za-z]:\\\\[^>\\r\\n]*>|[$#>])\\s*\\S/m.test(text)) return true;
        if (/`\\s*$/m.test(text) && /^\\s{2,}-(?:\\w|-)\\S*/m.test(text)) return true;
        matches = text.match(/^(?:\\[[0-2]?\\d:[0-5]\\d:[0-5]\\d\\]|\\d{4}-\\d{2}-\\d{2}[T ][^\\s]+|\\s*(?:TRACE|DEBUG|INFO|WARN|ERROR|FATAL)\\b).+$/gim);
        if (matches && matches.length >= 2) return true;
        if (/^RELEASE_STAGE_FAILED:/m.test(text) && /(?:reason=|backtrace)/i.test(text)) return true;
        matches = text.match(/^\\s*(?:\\[[^\\]\\r\\n]+\\]|[A-Za-z_][A-Za-z0-9_.-]*\\s*(?:=|:)\\s*\\S.*)$/gm);
        return !!(matches && matches.length >= 3);
      }
      function isMarkdown(value) {
        var text = String(value == null ? '' : value); var listItems;
        if (!text) return false;
        if (/(^|\\n) {0,3}#{1,6}[ \\t]+\\S/.test(text)) return true;
        if (/(^|\\n) {0,3}```[^\\n]*\\n[\\s\\S]*\\n {0,3}```/.test(text)) return true;
        if (/\\[[^\\]\\n]+\\]\\((?:https?:\\/\\/|\\/|#|\\.{0,2}\\/)[^)]+\\)/.test(text)) return true;
        if (/(^|\\n) {0,3}>[ \\t]+\\S/.test(text)) return true;
        if (/(^|\\n)\\s*\\|?.+\\|.+\\n\\s*\\|?\\s*:?-{3,}:?\\s*\\|/.test(text)) return true;
        listItems = text.match(/(^|\\n) {0,3}(?:[-+*]|\\d+[.)])[ \\t]+\\S/g);
        return !!(listItems && listItems.length >= 2);
      }
      function fnv1a(text) { var hash = 2166136261; var i; for (i = 0; i < text.length; i += 1) { hash ^= text.charCodeAt(i); hash = (hash * 16777619) >>> 0; } return (hash >>> 0).toString(16); }
      function djb2(text) { var hash = 5381; var i; for (i = 0; i < text.length; i += 1) hash = ((hash << 5) + hash + text.charCodeAt(i)) | 0; return (hash >>> 0).toString(36); }
      function legacyKiloHash(text) { text = String(text == null ? '' : text); return text.length + '_' + djb2(text); }
      function normalizeFrequencyText(text) { return trim(text); }
      function v2HashesFor(text) { text = String(text == null ? '' : text); return { version: 2, first: fnv1a(text), second: djb2(text), key: 'v2:' + fnv1a(text) + ':' + djb2(text) }; }
      function hashesFor(text) { text = normalizeFrequencyText(text); return { version: 3, first: fnv1a(text), second: djb2(text), key: 'v3:' + fnv1a(text) + ':' + djb2(text) }; }
      function cloneCounters(counters) { var copy = {}; var key; for (key in counters || {}) if (Object.prototype.hasOwnProperty.call(counters, key)) copy[key] = { version: counters[key].version, first: counters[key].first, second: counters[key].second, count: counters[key].count, lastUsed: counters[key].lastUsed }; return copy; }
      function pruneCounters(state, maximum) {
        var keys = []; var key;
        for (key in state.counters) if (Object.prototype.hasOwnProperty.call(state.counters, key)) keys.push(key);
        keys.sort(function (a, b) { return (state.counters[a].lastUsed - state.counters[b].lastUsed) || (a < b ? -1 : 1); });
        while (keys.length > maximum) delete state.counters[keys.shift()];
      }
      function recordFrequency(state, v2State, legacy, text) {
        var next = { version: 3, counters: cloneCounters((state || {}).counters), clock: Number((state || {}).clock || 0) };
        var canonicalText = normalizeFrequencyText(text); var hash = hashesFor(canonicalText); var oldHash = v2HashesFor(canonicalText); var oldKey = legacyKiloHash(canonicalText); var entry; var imported = 0;
        if (!canonicalText) return { state: next, result: { count: 0, promote: false, key: '', canonicalText: '', hashes: hash } };
        entry = next.counters[hash.key];
        if (!entry) {
          if (v2State && v2State.counters && v2State.counters[oldHash.key]) imported += Number(v2State.counters[oldHash.key].count || 0);
          if (legacy && legacy[oldKey] !== undefined) imported += Number(legacy[oldKey] || 0);
          entry = { version: 3, first: hash.first, second: hash.second, count: imported, lastUsed: 0 };
        }
        entry.count += 1; entry.lastUsed = ++next.clock; next.counters[hash.key] = entry;
        pruneCounters(next, 4096);
        return { state: next, result: { count: entry.count, promote: entry.count >= 6, key: hash.key, canonicalText: canonicalText, hashes: hash } };
      }
      function dismissFrequency(state, key) {
        var next = { version: 3, counters: cloneCounters((state || {}).counters), clock: Number((state || {}).clock || 0) }; var entry = next.counters[key]; var parts = String(key || '').split(':'); var savedCount = 0;
        if (!entry) entry = { version: 3, first: parts[1] || '', second: parts[2] || '', count: 0, lastUsed: 0 };
        savedCount = Number(entry.count || 0); entry.count = 0; entry.lastUsed = ++next.clock; next.counters[key] = entry; pruneCounters(next, 4096);
        return { state: next, savedCount: savedCount };
      }
      function restoreFrequency(state, key, savedCount) {
        var next = { version: 3, counters: cloneCounters((state || {}).counters), clock: Number((state || {}).clock || 0) }; var parts = String(key || '').split(':'); var entry = next.counters[key];
        if (!entry) entry = { version: 3, first: parts[1] || '', second: parts[2] || '', count: 0, lastUsed: 0 };
        entry.count = Number(entry.count || 0) + Number(savedCount || 0); entry.lastUsed = ++next.clock; next.counters[key] = entry; pruneCounters(next, 4096);
        return { state: next, count: entry.count };
      }
      function isTrashExpired(deletedAt, now, retentionMs) {
        var deleted = Date.parse(String(deletedAt || ''));
        return isFinite(deleted) && Number(now) - deleted >= Number(retentionMs);
      }
      function route(input) { var text = String(input && input.text != null ? input.text : ''); var formats = input && input.formats || []; var eligible = normalizeFrequencyText(text) !== ''; if (hasFormat(formats, /^image\\//i) || (input && input.mimeOwner && hasFormat(formats, new RegExp('^' + input.mimeOwner.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&') + '$'))) || (input && input.mimeHidden && hasFormat(formats, new RegExp('^' + input.mimeHidden.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&') + '$')))) return { action: 'passthrough', frequencyEligible: false }; if (isHighConfidenceSecret(text, formats)) return { action: 'ignored', frequencyEligible: false }; if (text.length >= 5000) return { action: 'big', frequencyEligible: eligible }; if (isArtifact(text)) return { action: 'artifacts', frequencyEligible: eligible }; if (isCode(text)) return { action: 'code', frequencyEligible: eligible }; if (/^https?:\\/\\/\\S+$/i.test(text)) return { action: 'passthrough', frequencyEligible: eligible }; return { action: 'default', frequencyEligible: eligible }; }
      function sortFrequent(items) { return items.slice().sort(function (a, b) { return (b.count - a.count) || (a.order - b.order); }); }
      function titleCase(text) { var words = String(text).toLowerCase().split(/(\\s+)/); var minor = { a: 1, an: 1, the: 1, and: 1, but: 1, or: 1, for: 1, nor: 1, as: 1, at: 1, by: 1, from: 1, in: 1, into: 1, near: 1, of: 1, on: 1, onto: 1, to: 1, with: 1 }; var first = -1; var last = -1; var i; for (i = 0; i < words.length; i += 1) if (!/^\\s+$/.test(words[i])) { if (first < 0) first = i; last = i; } for (i = 0; i < words.length; i += 1) if (!/^\\s+$/.test(words[i])) { if (i !== first && i !== last && minor[words[i]]) continue; words[i] = words[i].charAt(0).toUpperCase() + words[i].slice(1); } return words.join('').replace(/\\bId\\b/g, 'ID').replace(/\\bTv\\b/g, 'TV'); }
      function sanitizeHtml(value) { var html = String(value == null ? '' : value); html = html.replace(/\\s(?:color|bgcolor)\\s*=\\s*(?:\"[^\"]*\"|'[^']*'|[^\\s>]+)/gi, ''); return html.replace(/\\sstyle\\s*=\\s*(\"([^\"]*)\"|'([^']*)')/gi, function (match, quoted, doubleQuoted, singleQuoted) { var quote = quoted.charAt(0); var style = doubleQuoted !== undefined ? doubleQuoted : singleQuoted; var declarations = style.split(';').filter(function (declaration) { var name = trim(declaration.split(':')[0]).toLowerCase(); return name && name !== 'color' && name !== 'background' && name !== 'background-color'; }); return declarations.length ? ' style=' + quote + declarations.join(';') + quote : ''; }); }
      function toggleCase(value) { var text = String(value == null ? '' : value); var i; var character; for (i = 0; i < text.length; i += 1) { character = text.charAt(i); if (character !== character.toUpperCase() && character === character.toLowerCase()) return text.toUpperCase(); } return text.toLowerCase(); }
      function validateJsonImport(text) { var parsed; var i; var key; var item; var value; try { parsed = JSON.parse(text); } catch (e) { return { ok: false, reason: 'JSON_PARSE_INVALID' }; } if (!parsed || Object.prototype.toString.call(parsed.copyq_items) !== '[object Array]') return { ok: false, reason: 'JSON_SHAPE_INVALID' }; for (i = 0; i < parsed.copyq_items.length; i += 1) { item = parsed.copyq_items[i]; if (!item || Object.prototype.toString.call(item) !== '[object Object]') return { ok: false, reason: 'JSON_SHAPE_INVALID' }; for (key in item) if (Object.prototype.hasOwnProperty.call(item, key)) { value = item[key]; if (typeof value !== 'string' && !(value && Object.prototype.toString.call(value) === '[object Object]' && typeof value.base64 === 'string')) return { ok: false, reason: 'JSON_SHAPE_INVALID' }; } } return { ok: true, value: parsed }; }
      function validateRegex(text) { try { return { ok: true, value: new RegExp(text, 'i') }; } catch (e) { return { ok: false, reason: 'REGEX_INVALID' }; } }
      function runDependency(execute, args) { var result = execute.apply(null, args || []); return result && result.exit_code === 0 ? { ok: true, value: result } : { ok: false, reason: 'DEPENDENCY_FAILED' }; }
      return { isHighConfidenceSecret: isHighConfidenceSecret, isCode: isCode, isArtifact: isArtifact, isMarkdown: isMarkdown, normalizeFrequencyText: normalizeFrequencyText, hashesFor: hashesFor, v2HashesFor: v2HashesFor, legacyKiloHash: legacyKiloHash, recordFrequency: recordFrequency, dismissFrequency: dismissFrequency, restoreFrequency: restoreFrequency, isTrashExpired: isTrashExpired, route: route, sortFrequent: sortFrequent, titleCase: titleCase, sanitizeHtml: sanitizeHtml, toggleCase: toggleCase, validateJsonImport: validateJsonImport, validateRegex: validateRegex, runDependency: runDependency };
    }());

    var query = dialog('Search'); if (!query) abort(); var checked = CopyQCore.validateRegex(query); if (!checked.ok) { notification('.id', 'regex', '.message', checked.reason); abort(); }
    var names = tab(); try { removeTab('Search'); } catch (e) {} tab('Search'); for (var t = 0; t < names.length; t += 1) { if (names[t] === 'Search') continue; tab(names[t]); for (var i = 0; i < size(); i += 1) { var item = getitem(i); if (checked.value.test(str(item[mimeText]))) { tab('Search'); insert(0, item); tab(names[t]); } } } tab('Search');"
11\Input=text/plain
11\InMenu=true
11\InternalId=canonical.regex-search
12\Name=To Title Case
12\Command="
    copyq:
    var CopyQCore = (function () {
      function trim(value) { return String(value == null ? '' : value).replace(/^\\s+|\\s+$/g, ''); }
      function hasFormat(formats, expression) { var i; for (i = 0; i < (formats || []).length; i += 1) if (expression.test(String(formats[i]))) return true; return false; }
      function decode(part) {
        var alphabet = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/';
        var input = part.replace(/-/g, '+').replace(/_/g, '/'); var out = ''; var bits = 0; var bitCount = 0; var i; var n;
        while (input.length % 4) input += '=';
        for (i = 0; i < input.length; i += 1) { if (input.charAt(i) === '=') break; n = alphabet.indexOf(input.charAt(i)); if (n < 0) return null; bits = (bits << 6) | n; bitCount += 6; while (bitCount >= 8) { bitCount -= 8; out += String.fromCharCode((bits >> bitCount) & 255); } }
        return out;
      }
      function validJwt(text) { var parts = text.split('.'); var head; var body; try { head = parts.length === 3 ? JSON.parse(decode(parts[0])) : null; body = parts.length === 3 ? JSON.parse(decode(parts[1])) : null; } catch (e) { return false; } return !!(head && body && typeof head.alg === 'string' && /^[A-Za-z0-9_-]+$/.test(parts[2] || '')); }
      function credentialUrl(text) { return /^https?:\\/\\/[^/\\s:@]+:[^/\\s@]+@/i.test(text) || /[?&](?:access_token|api[_-]?key|password|secret|token)=[^&#\\s]+/i.test(text); }
      function secretFormat(formats) { return hasFormat(formats, /(?:application\\/x-copyq-secret|org\\.nspasteboard\\.concealedtype|clipboard viewer ignore|x-kde-passwordmanagerhint|nspasteboard concealed|mimesecret|com\\.agilebits\\.onepassword|1password|bitwarden|keepass|lastpass|password-manager)/i); }
      function structuredNonSecret(text) {
        return /^https?:\\/\\/\\S+$/i.test(text)
          || /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(text)
          || /^(?:[A-Za-z]:[\\\\/]|\\\\\\\\|\\/)[^\\r\\n]+$/.test(text)
          || /^v?\\d+\\.\\d+\\.\\d+(?:[-+][0-9A-Za-z.-]+)?$/.test(text)
          || /^[A-Z][A-Z0-9]{1,9}-\\d{1,10}$/.test(text);
      }
      function historicalOpaqueSecret(text) {
        var classes = 0;
        if (text.length < 10 || text.length > 3000 || /\\s/.test(text)) return false;
        if (/[a-z]/.test(text)) classes += 1;
        if (/[A-Z]/.test(text)) classes += 1;
        if (/\\d/.test(text)) classes += 1;
        return classes >= 2;
      }
      function isHighConfidenceSecret(value, formats) {
        var text = trim(value);
        if (secretFormat(formats)) return true;
        if (/^-----BEGIN (?:[A-Z0-9 ]+ )?(?:PRIVATE KEY|OPENSSH PRIVATE KEY)-----/m.test(text) || validJwt(text)) return true;
        if (/^(?:sk-[A-Za-z0-9_-]{20,}|ghp_[A-Za-z0-9]{30,}|github_pat_[A-Za-z0-9_]{30,}|glpat-[A-Za-z0-9_-]{20,}|xox[baprs]-[A-Za-z0-9-]{12,}|AIza[0-9A-Za-z_-]{30,}|AKIA[0-9A-Z]{16}|npm_[A-Za-z0-9_-]{20,}|pypi-[A-Za-z0-9_-]{20,})$/.test(text)) return true;
        if (/^authorization:\\s*(?:bearer|basic|token)\\s+\\S{12,}$/i.test(text)) return true;
        if(/^\\s*(?:export\\s+)?(?:[A-Za-z_][A-Za-z0-9_]*(?:PASSWORD|TOKEN|SECRET|API_KEY)[A-Za-z0-9_]*|PASSWORD|TOKEN|SECRET|API_KEY)\\s*=\\s*\\S{12,}\\s*$/i.test(text)) return true;
        if (credentialUrl(text)) return true;
        if (/^[0-9a-f]{32,128}$/i.test(text)) return true;
        if (structuredNonSecret(text)) return false;
        return historicalOpaqueSecret(text);
      }
      function isCode(text) { return /^\\s*(?:import|export|var|const|let|function|class|def|package|using|namespace|#include|#!)\\b/m.test(text) || /(?:=>|===|!==|&&|\\|\\|)/.test(text) || /\\b(?:if|for|while|switch|catch)\\s*\\(/.test(text) || /\\w+\\s*\\([^)]*\\)\\s*\\{/.test(text); }
      function isArtifact(value) {
        var text = trim(value); var parsed; var lines; var matches;
        if (!text) return false;
        if (/^[\\[{]/.test(text)) {
          try {
            parsed = JSON.parse(text);
            if (Object.prototype.toString.call(parsed) === '[object Array]') return parsed.length >= 2;
            if (parsed && Object.prototype.toString.call(parsed) === '[object Object]') return Object.keys(parsed).length >= 2;
          } catch (e) {}
        }
        if (!/[\\r\\n]/.test(text)) return false;
        lines = text.split(/\\r?\\n/).filter(function (line) { return trim(line) !== ''; });
        if (lines.length < 2) return false;
        if (/^diff --git\\s/m.test(text) || /^@@\\s+-\\d+(?:,\\d+)?\\s+\\+\\d+(?:,\\d+)?\\s+@@/m.test(text)) return true;
        if (/---\\s+backtrace\\s+---/i.test(text) || /^Traceback \\(most recent call last\\):/m.test(text)) return true;
        matches = text.match(/^\\s*at\\s+\\S.+$/gm); if (matches && matches.length >= 2) return true;
        if (/^(?:PS\\s+[A-Za-z]:\\\\[^>\\r\\n]*>|[$#>])\\s*\\S/m.test(text)) return true;
        if (/`\\s*$/m.test(text) && /^\\s{2,}-(?:\\w|-)\\S*/m.test(text)) return true;
        matches = text.match(/^(?:\\[[0-2]?\\d:[0-5]\\d:[0-5]\\d\\]|\\d{4}-\\d{2}-\\d{2}[T ][^\\s]+|\\s*(?:TRACE|DEBUG|INFO|WARN|ERROR|FATAL)\\b).+$/gim);
        if (matches && matches.length >= 2) return true;
        if (/^RELEASE_STAGE_FAILED:/m.test(text) && /(?:reason=|backtrace)/i.test(text)) return true;
        matches = text.match(/^\\s*(?:\\[[^\\]\\r\\n]+\\]|[A-Za-z_][A-Za-z0-9_.-]*\\s*(?:=|:)\\s*\\S.*)$/gm);
        return !!(matches && matches.length >= 3);
      }
      function isMarkdown(value) {
        var text = String(value == null ? '' : value); var listItems;
        if (!text) return false;
        if (/(^|\\n) {0,3}#{1,6}[ \\t]+\\S/.test(text)) return true;
        if (/(^|\\n) {0,3}```[^\\n]*\\n[\\s\\S]*\\n {0,3}```/.test(text)) return true;
        if (/\\[[^\\]\\n]+\\]\\((?:https?:\\/\\/|\\/|#|\\.{0,2}\\/)[^)]+\\)/.test(text)) return true;
        if (/(^|\\n) {0,3}>[ \\t]+\\S/.test(text)) return true;
        if (/(^|\\n)\\s*\\|?.+\\|.+\\n\\s*\\|?\\s*:?-{3,}:?\\s*\\|/.test(text)) return true;
        listItems = text.match(/(^|\\n) {0,3}(?:[-+*]|\\d+[.)])[ \\t]+\\S/g);
        return !!(listItems && listItems.length >= 2);
      }
      function fnv1a(text) { var hash = 2166136261; var i; for (i = 0; i < text.length; i += 1) { hash ^= text.charCodeAt(i); hash = (hash * 16777619) >>> 0; } return (hash >>> 0).toString(16); }
      function djb2(text) { var hash = 5381; var i; for (i = 0; i < text.length; i += 1) hash = ((hash << 5) + hash + text.charCodeAt(i)) | 0; return (hash >>> 0).toString(36); }
      function legacyKiloHash(text) { text = String(text == null ? '' : text); return text.length + '_' + djb2(text); }
      function normalizeFrequencyText(text) { return trim(text); }
      function v2HashesFor(text) { text = String(text == null ? '' : text); return { version: 2, first: fnv1a(text), second: djb2(text), key: 'v2:' + fnv1a(text) + ':' + djb2(text) }; }
      function hashesFor(text) { text = normalizeFrequencyText(text); return { version: 3, first: fnv1a(text), second: djb2(text), key: 'v3:' + fnv1a(text) + ':' + djb2(text) }; }
      function cloneCounters(counters) { var copy = {}; var key; for (key in counters || {}) if (Object.prototype.hasOwnProperty.call(counters, key)) copy[key] = { version: counters[key].version, first: counters[key].first, second: counters[key].second, count: counters[key].count, lastUsed: counters[key].lastUsed }; return copy; }
      function pruneCounters(state, maximum) {
        var keys = []; var key;
        for (key in state.counters) if (Object.prototype.hasOwnProperty.call(state.counters, key)) keys.push(key);
        keys.sort(function (a, b) { return (state.counters[a].lastUsed - state.counters[b].lastUsed) || (a < b ? -1 : 1); });
        while (keys.length > maximum) delete state.counters[keys.shift()];
      }
      function recordFrequency(state, v2State, legacy, text) {
        var next = { version: 3, counters: cloneCounters((state || {}).counters), clock: Number((state || {}).clock || 0) };
        var canonicalText = normalizeFrequencyText(text); var hash = hashesFor(canonicalText); var oldHash = v2HashesFor(canonicalText); var oldKey = legacyKiloHash(canonicalText); var entry; var imported = 0;
        if (!canonicalText) return { state: next, result: { count: 0, promote: false, key: '', canonicalText: '', hashes: hash } };
        entry = next.counters[hash.key];
        if (!entry) {
          if (v2State && v2State.counters && v2State.counters[oldHash.key]) imported += Number(v2State.counters[oldHash.key].count || 0);
          if (legacy && legacy[oldKey] !== undefined) imported += Number(legacy[oldKey] || 0);
          entry = { version: 3, first: hash.first, second: hash.second, count: imported, lastUsed: 0 };
        }
        entry.count += 1; entry.lastUsed = ++next.clock; next.counters[hash.key] = entry;
        pruneCounters(next, 4096);
        return { state: next, result: { count: entry.count, promote: entry.count >= 6, key: hash.key, canonicalText: canonicalText, hashes: hash } };
      }
      function dismissFrequency(state, key) {
        var next = { version: 3, counters: cloneCounters((state || {}).counters), clock: Number((state || {}).clock || 0) }; var entry = next.counters[key]; var parts = String(key || '').split(':'); var savedCount = 0;
        if (!entry) entry = { version: 3, first: parts[1] || '', second: parts[2] || '', count: 0, lastUsed: 0 };
        savedCount = Number(entry.count || 0); entry.count = 0; entry.lastUsed = ++next.clock; next.counters[key] = entry; pruneCounters(next, 4096);
        return { state: next, savedCount: savedCount };
      }
      function restoreFrequency(state, key, savedCount) {
        var next = { version: 3, counters: cloneCounters((state || {}).counters), clock: Number((state || {}).clock || 0) }; var parts = String(key || '').split(':'); var entry = next.counters[key];
        if (!entry) entry = { version: 3, first: parts[1] || '', second: parts[2] || '', count: 0, lastUsed: 0 };
        entry.count = Number(entry.count || 0) + Number(savedCount || 0); entry.lastUsed = ++next.clock; next.counters[key] = entry; pruneCounters(next, 4096);
        return { state: next, count: entry.count };
      }
      function isTrashExpired(deletedAt, now, retentionMs) {
        var deleted = Date.parse(String(deletedAt || ''));
        return isFinite(deleted) && Number(now) - deleted >= Number(retentionMs);
      }
      function route(input) { var text = String(input && input.text != null ? input.text : ''); var formats = input && input.formats || []; var eligible = normalizeFrequencyText(text) !== ''; if (hasFormat(formats, /^image\\//i) || (input && input.mimeOwner && hasFormat(formats, new RegExp('^' + input.mimeOwner.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&') + '$'))) || (input && input.mimeHidden && hasFormat(formats, new RegExp('^' + input.mimeHidden.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&') + '$')))) return { action: 'passthrough', frequencyEligible: false }; if (isHighConfidenceSecret(text, formats)) return { action: 'ignored', frequencyEligible: false }; if (text.length >= 5000) return { action: 'big', frequencyEligible: eligible }; if (isArtifact(text)) return { action: 'artifacts', frequencyEligible: eligible }; if (isCode(text)) return { action: 'code', frequencyEligible: eligible }; if (/^https?:\\/\\/\\S+$/i.test(text)) return { action: 'passthrough', frequencyEligible: eligible }; return { action: 'default', frequencyEligible: eligible }; }
      function sortFrequent(items) { return items.slice().sort(function (a, b) { return (b.count - a.count) || (a.order - b.order); }); }
      function titleCase(text) { var words = String(text).toLowerCase().split(/(\\s+)/); var minor = { a: 1, an: 1, the: 1, and: 1, but: 1, or: 1, for: 1, nor: 1, as: 1, at: 1, by: 1, from: 1, in: 1, into: 1, near: 1, of: 1, on: 1, onto: 1, to: 1, with: 1 }; var first = -1; var last = -1; var i; for (i = 0; i < words.length; i += 1) if (!/^\\s+$/.test(words[i])) { if (first < 0) first = i; last = i; } for (i = 0; i < words.length; i += 1) if (!/^\\s+$/.test(words[i])) { if (i !== first && i !== last && minor[words[i]]) continue; words[i] = words[i].charAt(0).toUpperCase() + words[i].slice(1); } return words.join('').replace(/\\bId\\b/g, 'ID').replace(/\\bTv\\b/g, 'TV'); }
      function sanitizeHtml(value) { var html = String(value == null ? '' : value); html = html.replace(/\\s(?:color|bgcolor)\\s*=\\s*(?:\"[^\"]*\"|'[^']*'|[^\\s>]+)/gi, ''); return html.replace(/\\sstyle\\s*=\\s*(\"([^\"]*)\"|'([^']*)')/gi, function (match, quoted, doubleQuoted, singleQuoted) { var quote = quoted.charAt(0); var style = doubleQuoted !== undefined ? doubleQuoted : singleQuoted; var declarations = style.split(';').filter(function (declaration) { var name = trim(declaration.split(':')[0]).toLowerCase(); return name && name !== 'color' && name !== 'background' && name !== 'background-color'; }); return declarations.length ? ' style=' + quote + declarations.join(';') + quote : ''; }); }
      function toggleCase(value) { var text = String(value == null ? '' : value); var i; var character; for (i = 0; i < text.length; i += 1) { character = text.charAt(i); if (character !== character.toUpperCase() && character === character.toLowerCase()) return text.toUpperCase(); } return text.toLowerCase(); }
      function validateJsonImport(text) { var parsed; var i; var key; var item; var value; try { parsed = JSON.parse(text); } catch (e) { return { ok: false, reason: 'JSON_PARSE_INVALID' }; } if (!parsed || Object.prototype.toString.call(parsed.copyq_items) !== '[object Array]') return { ok: false, reason: 'JSON_SHAPE_INVALID' }; for (i = 0; i < parsed.copyq_items.length; i += 1) { item = parsed.copyq_items[i]; if (!item || Object.prototype.toString.call(item) !== '[object Object]') return { ok: false, reason: 'JSON_SHAPE_INVALID' }; for (key in item) if (Object.prototype.hasOwnProperty.call(item, key)) { value = item[key]; if (typeof value !== 'string' && !(value && Object.prototype.toString.call(value) === '[object Object]' && typeof value.base64 === 'string')) return { ok: false, reason: 'JSON_SHAPE_INVALID' }; } } return { ok: true, value: parsed }; }
      function validateRegex(text) { try { return { ok: true, value: new RegExp(text, 'i') }; } catch (e) { return { ok: false, reason: 'REGEX_INVALID' }; } }
      function runDependency(execute, args) { var result = execute.apply(null, args || []); return result && result.exit_code === 0 ? { ok: true, value: result } : { ok: false, reason: 'DEPENDENCY_FAILED' }; }
      return { isHighConfidenceSecret: isHighConfidenceSecret, isCode: isCode, isArtifact: isArtifact, isMarkdown: isMarkdown, normalizeFrequencyText: normalizeFrequencyText, hashesFor: hashesFor, v2HashesFor: v2HashesFor, legacyKiloHash: legacyKiloHash, recordFrequency: recordFrequency, dismissFrequency: dismissFrequency, restoreFrequency: restoreFrequency, isTrashExpired: isTrashExpired, route: route, sortFrequent: sortFrequent, titleCase: titleCase, sanitizeHtml: sanitizeHtml, toggleCase: toggleCase, validateJsonImport: validateJsonImport, validateRegex: validateRegex, runDependency: runDependency };
    }());

    if (!copy()) abort(); var text = str(clipboard()); var result = CopyQCore.titleCase(text); if (result === text) abort(); copy(result); paste();"
12\Input=text/plain
12\InMenu=true
12\InternalId=canonical.smart-title
13\Name=Toggle Upper/Lower Case
13\Command="
    copyq:
    var CopyQCore = (function () {
      function trim(value) { return String(value == null ? '' : value).replace(/^\\s+|\\s+$/g, ''); }
      function hasFormat(formats, expression) { var i; for (i = 0; i < (formats || []).length; i += 1) if (expression.test(String(formats[i]))) return true; return false; }
      function decode(part) {
        var alphabet = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/';
        var input = part.replace(/-/g, '+').replace(/_/g, '/'); var out = ''; var bits = 0; var bitCount = 0; var i; var n;
        while (input.length % 4) input += '=';
        for (i = 0; i < input.length; i += 1) { if (input.charAt(i) === '=') break; n = alphabet.indexOf(input.charAt(i)); if (n < 0) return null; bits = (bits << 6) | n; bitCount += 6; while (bitCount >= 8) { bitCount -= 8; out += String.fromCharCode((bits >> bitCount) & 255); } }
        return out;
      }
      function validJwt(text) { var parts = text.split('.'); var head; var body; try { head = parts.length === 3 ? JSON.parse(decode(parts[0])) : null; body = parts.length === 3 ? JSON.parse(decode(parts[1])) : null; } catch (e) { return false; } return !!(head && body && typeof head.alg === 'string' && /^[A-Za-z0-9_-]+$/.test(parts[2] || '')); }
      function credentialUrl(text) { return /^https?:\\/\\/[^/\\s:@]+:[^/\\s@]+@/i.test(text) || /[?&](?:access_token|api[_-]?key|password|secret|token)=[^&#\\s]+/i.test(text); }
      function secretFormat(formats) { return hasFormat(formats, /(?:application\\/x-copyq-secret|org\\.nspasteboard\\.concealedtype|clipboard viewer ignore|x-kde-passwordmanagerhint|nspasteboard concealed|mimesecret|com\\.agilebits\\.onepassword|1password|bitwarden|keepass|lastpass|password-manager)/i); }
      function structuredNonSecret(text) {
        return /^https?:\\/\\/\\S+$/i.test(text)
          || /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(text)
          || /^(?:[A-Za-z]:[\\\\/]|\\\\\\\\|\\/)[^\\r\\n]+$/.test(text)
          || /^v?\\d+\\.\\d+\\.\\d+(?:[-+][0-9A-Za-z.-]+)?$/.test(text)
          || /^[A-Z][A-Z0-9]{1,9}-\\d{1,10}$/.test(text);
      }
      function historicalOpaqueSecret(text) {
        var classes = 0;
        if (text.length < 10 || text.length > 3000 || /\\s/.test(text)) return false;
        if (/[a-z]/.test(text)) classes += 1;
        if (/[A-Z]/.test(text)) classes += 1;
        if (/\\d/.test(text)) classes += 1;
        return classes >= 2;
      }
      function isHighConfidenceSecret(value, formats) {
        var text = trim(value);
        if (secretFormat(formats)) return true;
        if (/^-----BEGIN (?:[A-Z0-9 ]+ )?(?:PRIVATE KEY|OPENSSH PRIVATE KEY)-----/m.test(text) || validJwt(text)) return true;
        if (/^(?:sk-[A-Za-z0-9_-]{20,}|ghp_[A-Za-z0-9]{30,}|github_pat_[A-Za-z0-9_]{30,}|glpat-[A-Za-z0-9_-]{20,}|xox[baprs]-[A-Za-z0-9-]{12,}|AIza[0-9A-Za-z_-]{30,}|AKIA[0-9A-Z]{16}|npm_[A-Za-z0-9_-]{20,}|pypi-[A-Za-z0-9_-]{20,})$/.test(text)) return true;
        if (/^authorization:\\s*(?:bearer|basic|token)\\s+\\S{12,}$/i.test(text)) return true;
        if(/^\\s*(?:export\\s+)?(?:[A-Za-z_][A-Za-z0-9_]*(?:PASSWORD|TOKEN|SECRET|API_KEY)[A-Za-z0-9_]*|PASSWORD|TOKEN|SECRET|API_KEY)\\s*=\\s*\\S{12,}\\s*$/i.test(text)) return true;
        if (credentialUrl(text)) return true;
        if (/^[0-9a-f]{32,128}$/i.test(text)) return true;
        if (structuredNonSecret(text)) return false;
        return historicalOpaqueSecret(text);
      }
      function isCode(text) { return /^\\s*(?:import|export|var|const|let|function|class|def|package|using|namespace|#include|#!)\\b/m.test(text) || /(?:=>|===|!==|&&|\\|\\|)/.test(text) || /\\b(?:if|for|while|switch|catch)\\s*\\(/.test(text) || /\\w+\\s*\\([^)]*\\)\\s*\\{/.test(text); }
      function isArtifact(value) {
        var text = trim(value); var parsed; var lines; var matches;
        if (!text) return false;
        if (/^[\\[{]/.test(text)) {
          try {
            parsed = JSON.parse(text);
            if (Object.prototype.toString.call(parsed) === '[object Array]') return parsed.length >= 2;
            if (parsed && Object.prototype.toString.call(parsed) === '[object Object]') return Object.keys(parsed).length >= 2;
          } catch (e) {}
        }
        if (!/[\\r\\n]/.test(text)) return false;
        lines = text.split(/\\r?\\n/).filter(function (line) { return trim(line) !== ''; });
        if (lines.length < 2) return false;
        if (/^diff --git\\s/m.test(text) || /^@@\\s+-\\d+(?:,\\d+)?\\s+\\+\\d+(?:,\\d+)?\\s+@@/m.test(text)) return true;
        if (/---\\s+backtrace\\s+---/i.test(text) || /^Traceback \\(most recent call last\\):/m.test(text)) return true;
        matches = text.match(/^\\s*at\\s+\\S.+$/gm); if (matches && matches.length >= 2) return true;
        if (/^(?:PS\\s+[A-Za-z]:\\\\[^>\\r\\n]*>|[$#>])\\s*\\S/m.test(text)) return true;
        if (/`\\s*$/m.test(text) && /^\\s{2,}-(?:\\w|-)\\S*/m.test(text)) return true;
        matches = text.match(/^(?:\\[[0-2]?\\d:[0-5]\\d:[0-5]\\d\\]|\\d{4}-\\d{2}-\\d{2}[T ][^\\s]+|\\s*(?:TRACE|DEBUG|INFO|WARN|ERROR|FATAL)\\b).+$/gim);
        if (matches && matches.length >= 2) return true;
        if (/^RELEASE_STAGE_FAILED:/m.test(text) && /(?:reason=|backtrace)/i.test(text)) return true;
        matches = text.match(/^\\s*(?:\\[[^\\]\\r\\n]+\\]|[A-Za-z_][A-Za-z0-9_.-]*\\s*(?:=|:)\\s*\\S.*)$/gm);
        return !!(matches && matches.length >= 3);
      }
      function isMarkdown(value) {
        var text = String(value == null ? '' : value); var listItems;
        if (!text) return false;
        if (/(^|\\n) {0,3}#{1,6}[ \\t]+\\S/.test(text)) return true;
        if (/(^|\\n) {0,3}```[^\\n]*\\n[\\s\\S]*\\n {0,3}```/.test(text)) return true;
        if (/\\[[^\\]\\n]+\\]\\((?:https?:\\/\\/|\\/|#|\\.{0,2}\\/)[^)]+\\)/.test(text)) return true;
        if (/(^|\\n) {0,3}>[ \\t]+\\S/.test(text)) return true;
        if (/(^|\\n)\\s*\\|?.+\\|.+\\n\\s*\\|?\\s*:?-{3,}:?\\s*\\|/.test(text)) return true;
        listItems = text.match(/(^|\\n) {0,3}(?:[-+*]|\\d+[.)])[ \\t]+\\S/g);
        return !!(listItems && listItems.length >= 2);
      }
      function fnv1a(text) { var hash = 2166136261; var i; for (i = 0; i < text.length; i += 1) { hash ^= text.charCodeAt(i); hash = (hash * 16777619) >>> 0; } return (hash >>> 0).toString(16); }
      function djb2(text) { var hash = 5381; var i; for (i = 0; i < text.length; i += 1) hash = ((hash << 5) + hash + text.charCodeAt(i)) | 0; return (hash >>> 0).toString(36); }
      function legacyKiloHash(text) { text = String(text == null ? '' : text); return text.length + '_' + djb2(text); }
      function normalizeFrequencyText(text) { return trim(text); }
      function v2HashesFor(text) { text = String(text == null ? '' : text); return { version: 2, first: fnv1a(text), second: djb2(text), key: 'v2:' + fnv1a(text) + ':' + djb2(text) }; }
      function hashesFor(text) { text = normalizeFrequencyText(text); return { version: 3, first: fnv1a(text), second: djb2(text), key: 'v3:' + fnv1a(text) + ':' + djb2(text) }; }
      function cloneCounters(counters) { var copy = {}; var key; for (key in counters || {}) if (Object.prototype.hasOwnProperty.call(counters, key)) copy[key] = { version: counters[key].version, first: counters[key].first, second: counters[key].second, count: counters[key].count, lastUsed: counters[key].lastUsed }; return copy; }
      function pruneCounters(state, maximum) {
        var keys = []; var key;
        for (key in state.counters) if (Object.prototype.hasOwnProperty.call(state.counters, key)) keys.push(key);
        keys.sort(function (a, b) { return (state.counters[a].lastUsed - state.counters[b].lastUsed) || (a < b ? -1 : 1); });
        while (keys.length > maximum) delete state.counters[keys.shift()];
      }
      function recordFrequency(state, v2State, legacy, text) {
        var next = { version: 3, counters: cloneCounters((state || {}).counters), clock: Number((state || {}).clock || 0) };
        var canonicalText = normalizeFrequencyText(text); var hash = hashesFor(canonicalText); var oldHash = v2HashesFor(canonicalText); var oldKey = legacyKiloHash(canonicalText); var entry; var imported = 0;
        if (!canonicalText) return { state: next, result: { count: 0, promote: false, key: '', canonicalText: '', hashes: hash } };
        entry = next.counters[hash.key];
        if (!entry) {
          if (v2State && v2State.counters && v2State.counters[oldHash.key]) imported += Number(v2State.counters[oldHash.key].count || 0);
          if (legacy && legacy[oldKey] !== undefined) imported += Number(legacy[oldKey] || 0);
          entry = { version: 3, first: hash.first, second: hash.second, count: imported, lastUsed: 0 };
        }
        entry.count += 1; entry.lastUsed = ++next.clock; next.counters[hash.key] = entry;
        pruneCounters(next, 4096);
        return { state: next, result: { count: entry.count, promote: entry.count >= 6, key: hash.key, canonicalText: canonicalText, hashes: hash } };
      }
      function dismissFrequency(state, key) {
        var next = { version: 3, counters: cloneCounters((state || {}).counters), clock: Number((state || {}).clock || 0) }; var entry = next.counters[key]; var parts = String(key || '').split(':'); var savedCount = 0;
        if (!entry) entry = { version: 3, first: parts[1] || '', second: parts[2] || '', count: 0, lastUsed: 0 };
        savedCount = Number(entry.count || 0); entry.count = 0; entry.lastUsed = ++next.clock; next.counters[key] = entry; pruneCounters(next, 4096);
        return { state: next, savedCount: savedCount };
      }
      function restoreFrequency(state, key, savedCount) {
        var next = { version: 3, counters: cloneCounters((state || {}).counters), clock: Number((state || {}).clock || 0) }; var parts = String(key || '').split(':'); var entry = next.counters[key];
        if (!entry) entry = { version: 3, first: parts[1] || '', second: parts[2] || '', count: 0, lastUsed: 0 };
        entry.count = Number(entry.count || 0) + Number(savedCount || 0); entry.lastUsed = ++next.clock; next.counters[key] = entry; pruneCounters(next, 4096);
        return { state: next, count: entry.count };
      }
      function isTrashExpired(deletedAt, now, retentionMs) {
        var deleted = Date.parse(String(deletedAt || ''));
        return isFinite(deleted) && Number(now) - deleted >= Number(retentionMs);
      }
      function route(input) { var text = String(input && input.text != null ? input.text : ''); var formats = input && input.formats || []; var eligible = normalizeFrequencyText(text) !== ''; if (hasFormat(formats, /^image\\//i) || (input && input.mimeOwner && hasFormat(formats, new RegExp('^' + input.mimeOwner.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&') + '$'))) || (input && input.mimeHidden && hasFormat(formats, new RegExp('^' + input.mimeHidden.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&') + '$')))) return { action: 'passthrough', frequencyEligible: false }; if (isHighConfidenceSecret(text, formats)) return { action: 'ignored', frequencyEligible: false }; if (text.length >= 5000) return { action: 'big', frequencyEligible: eligible }; if (isArtifact(text)) return { action: 'artifacts', frequencyEligible: eligible }; if (isCode(text)) return { action: 'code', frequencyEligible: eligible }; if (/^https?:\\/\\/\\S+$/i.test(text)) return { action: 'passthrough', frequencyEligible: eligible }; return { action: 'default', frequencyEligible: eligible }; }
      function sortFrequent(items) { return items.slice().sort(function (a, b) { return (b.count - a.count) || (a.order - b.order); }); }
      function titleCase(text) { var words = String(text).toLowerCase().split(/(\\s+)/); var minor = { a: 1, an: 1, the: 1, and: 1, but: 1, or: 1, for: 1, nor: 1, as: 1, at: 1, by: 1, from: 1, in: 1, into: 1, near: 1, of: 1, on: 1, onto: 1, to: 1, with: 1 }; var first = -1; var last = -1; var i; for (i = 0; i < words.length; i += 1) if (!/^\\s+$/.test(words[i])) { if (first < 0) first = i; last = i; } for (i = 0; i < words.length; i += 1) if (!/^\\s+$/.test(words[i])) { if (i !== first && i !== last && minor[words[i]]) continue; words[i] = words[i].charAt(0).toUpperCase() + words[i].slice(1); } return words.join('').replace(/\\bId\\b/g, 'ID').replace(/\\bTv\\b/g, 'TV'); }
      function sanitizeHtml(value) { var html = String(value == null ? '' : value); html = html.replace(/\\s(?:color|bgcolor)\\s*=\\s*(?:\"[^\"]*\"|'[^']*'|[^\\s>]+)/gi, ''); return html.replace(/\\sstyle\\s*=\\s*(\"([^\"]*)\"|'([^']*)')/gi, function (match, quoted, doubleQuoted, singleQuoted) { var quote = quoted.charAt(0); var style = doubleQuoted !== undefined ? doubleQuoted : singleQuoted; var declarations = style.split(';').filter(function (declaration) { var name = trim(declaration.split(':')[0]).toLowerCase(); return name && name !== 'color' && name !== 'background' && name !== 'background-color'; }); return declarations.length ? ' style=' + quote + declarations.join(';') + quote : ''; }); }
      function toggleCase(value) { var text = String(value == null ? '' : value); var i; var character; for (i = 0; i < text.length; i += 1) { character = text.charAt(i); if (character !== character.toUpperCase() && character === character.toLowerCase()) return text.toUpperCase(); } return text.toLowerCase(); }
      function validateJsonImport(text) { var parsed; var i; var key; var item; var value; try { parsed = JSON.parse(text); } catch (e) { return { ok: false, reason: 'JSON_PARSE_INVALID' }; } if (!parsed || Object.prototype.toString.call(parsed.copyq_items) !== '[object Array]') return { ok: false, reason: 'JSON_SHAPE_INVALID' }; for (i = 0; i < parsed.copyq_items.length; i += 1) { item = parsed.copyq_items[i]; if (!item || Object.prototype.toString.call(item) !== '[object Object]') return { ok: false, reason: 'JSON_SHAPE_INVALID' }; for (key in item) if (Object.prototype.hasOwnProperty.call(item, key)) { value = item[key]; if (typeof value !== 'string' && !(value && Object.prototype.toString.call(value) === '[object Object]' && typeof value.base64 === 'string')) return { ok: false, reason: 'JSON_SHAPE_INVALID' }; } } return { ok: true, value: parsed }; }
      function validateRegex(text) { try { return { ok: true, value: new RegExp(text, 'i') }; } catch (e) { return { ok: false, reason: 'REGEX_INVALID' }; } }
      function runDependency(execute, args) { var result = execute.apply(null, args || []); return result && result.exit_code === 0 ? { ok: true, value: result } : { ok: false, reason: 'DEPENDENCY_FAILED' }; }
      return { isHighConfidenceSecret: isHighConfidenceSecret, isCode: isCode, isArtifact: isArtifact, isMarkdown: isMarkdown, normalizeFrequencyText: normalizeFrequencyText, hashesFor: hashesFor, v2HashesFor: v2HashesFor, legacyKiloHash: legacyKiloHash, recordFrequency: recordFrequency, dismissFrequency: dismissFrequency, restoreFrequency: restoreFrequency, isTrashExpired: isTrashExpired, route: route, sortFrequent: sortFrequent, titleCase: titleCase, sanitizeHtml: sanitizeHtml, toggleCase: toggleCase, validateJsonImport: validateJsonImport, validateRegex: validateRegex, runDependency: runDependency };
    }());

    if (!copy()) abort(); var text = str(clipboard()); var result = CopyQCore.toggleCase(text); if (result === text) abort(); copy(result); paste();"
13\Input=text/plain
13\InMenu=true
13\InternalId=canonical.toggle-case
14\Name=Show Frequent
14\Command="
    copyq:
    menu('Frequent');"
14\Input=text/plain
14\InMenu=true
14\IsGlobalShortcut=true
14\GlobalShortcut=meta+shift+f
14\InternalId=canonical.show-frequent
15\Name=Copy and Search on Web
15\Command="
    copyq:
    if (!copy()) abort(); var text = str(clipboard()); if (!text) abort(); var engines = [{ name: 'DuckDuckGo', url: 'https://duckduckgo.com/?q=%s' }, { name: 'GitHub', url: 'https://github.com/search?q=%s' }]; var items = []; for (var i = 0; i < engines.length; i += 1) { var item = {}; item[mimeText] = engines[i].name; items.push(item); } var chosen = menuItems(items); if (chosen < 0) abort(); var url = engines[chosen].url.replace('%s', encodeURIComponent(text)); open(url);"
15\Input=text/plain
15\InMenu=true
15\InternalId=canonical.copy-and-search
16\Name=Moonlander: Smart Title Case
16\Command="
    copyq:
    var root = str(env('LOCALAPPDATA')).replace(/\\\\/g, '/') + '/MoonlanderTextTools';
    try { source(root + '/transformations.js'); source(root + '/transaction.js'); } catch (e) { notification('.id', 'moonlander-dependency', '.title', 'Moonlander Text Tools', '.message', 'MOONLANDER_DEPENDENCY_MISSING'); abort(); }
    MoonlanderTransaction.runTransaction(
        MoonlanderTransforms.smartTitleCase,
        {},
        MoonlanderTransaction.createCopyQAdapter(root + '/Moonlander.Reselect.exe')
    );"
16\Input=text/plain
16\InMenu=true
16\IsGlobalShortcut=true
16\GlobalShortcut=F13
16\InternalId=moonlander.smart-title
17\Name=Moonlander: Cycle Case
17\Command="
    copyq:
    var root = str(env('LOCALAPPDATA')).replace(/\\\\/g, '/') + '/MoonlanderTextTools';
    try { source(root + '/transformations.js'); source(root + '/transaction.js'); } catch (e) { notification('.id', 'moonlander-dependency', '.title', 'Moonlander Text Tools', '.message', 'MOONLANDER_DEPENDENCY_MISSING'); abort(); }
    MoonlanderTransaction.runTransaction(
        MoonlanderTransforms.cycleCase,
        {reselect: true},
        MoonlanderTransaction.createCopyQAdapter(root + '/Moonlander.Reselect.exe')
    );"
17\Input=text/plain
17\InMenu=true
17\IsGlobalShortcut=true
17\GlobalShortcut=F19
17\InternalId=moonlander.cycle-case
18\Name=Moonlander: Transplant Hebrew-English
18\Command="
    copyq:
    var root = str(env('LOCALAPPDATA')).replace(/\\\\/g, '/') + '/MoonlanderTextTools';
    try { source(root + '/transformations.js'); source(root + '/transaction.js'); } catch (e) { notification('.id', 'moonlander-dependency', '.title', 'Moonlander Text Tools', '.message', 'MOONLANDER_DEPENDENCY_MISSING'); abort(); }
    MoonlanderTransaction.runTransaction(
        MoonlanderTransforms.transplantHebrewEnglish,
        {},
        MoonlanderTransaction.createCopyQAdapter(root + '/Moonlander.Reselect.exe')
    );"
18\Input=text/plain
18\InMenu=true
18\IsGlobalShortcut=true
18\GlobalShortcut=F22
18\InternalId=moonlander.hebrew-layout
size=18
