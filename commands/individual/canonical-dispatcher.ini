[Command]
Name=Clipboard Router
Command="
    copyq:
    var CopyQCore = (function () {
      var modules = {};
      var api = {};
      modules.common = (function () {
        var module = { exports: {} };
        function require(id) { switch (id) {  default: throw new Error('UNDECLARED_CORE_IMPORT'); } }
    \"use strict\";


    function trim(value) { return String(value == null ? '' : value).replace(/^\\s+|\\s+$/g, ''); }
    function hasFormat(formats, expression) { var i; for (i = 0; i < (formats || []).length; i += 1) if (expression.test(String(formats[i]))) return true; return false; }

    module.exports = { trim: trim, hasFormat: hasFormat };


        return module.exports;
      }());
      for (var key in modules.common) if (Object.prototype.hasOwnProperty.call(modules.common, key)) api[key] = modules.common[key];
      modules.urls = (function () {
        var module = { exports: {} };
        function require(id) { switch (id) { case './common': return modules.common; default: throw new Error('UNDECLARED_CORE_IMPORT'); } }
    \"use strict\";

    var common = require(\"./common\");
    var trim = common.trim;

    function credentialUrl(text) { return /^(?:https?|ftps?):\\/\\/[^/\\s:@]+:[^/\\s@]+@/i.test(text) || /[?&](?:access_token|api[_-]?key|password|secret|token)=[^&#\\s]+/i.test(text) || /^https?:\\/\\/calendar\\.google\\.com\\/calendar\\/ical\\/[^\\s/]+\\/private-[0-9a-f]{32}\\/basic\\.ics(?:[?#]|$)/i.test(text); }
    // Only recognizable credentials are redacted inside documents. Do not apply
    // the conservative standalone opaque-string heuristic to every prose word.
    function isStandaloneUrl(value) {
      var text = trim(value);
      return /^(?:https?|ftps?):\\/\\/[^\\s/?#]+[^\\s]*$/i.test(text) || /^file:\\/\\/[^\\r\\n]+$/i.test(text);
    }

    module.exports = { credentialUrl: credentialUrl, isStandaloneUrl: isStandaloneUrl };


        return module.exports;
      }());
      for (var key in modules.urls) if (Object.prototype.hasOwnProperty.call(modules.urls, key)) api[key] = modules.urls[key];
      modules.secrets = (function () {
        var module = { exports: {} };
        function require(id) { switch (id) { case './common': return modules.common; case './urls': return modules.urls; default: throw new Error('UNDECLARED_CORE_IMPORT'); } }
    \"use strict\";

    var common = require(\"./common\");
    var trim = common.trim;
    var hasFormat = common.hasFormat;
    var urls = require(\"./urls\");
    var credentialUrl = urls.credentialUrl;
    var isStandaloneUrl = urls.isStandaloneUrl;

    function decode(part) {
      var alphabet = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/';
      var input = part.replace(/-/g, '+').replace(/_/g, '/'); var out = ''; var bits = 0; var bitCount = 0; var i; var n;
      while (input.length % 4) input += '=';
      for (i = 0; i < input.length; i += 1) { if (input.charAt(i) === '=') break; n = alphabet.indexOf(input.charAt(i)); if (n < 0) return null; bits = (bits << 6) | n; bitCount += 6; while (bitCount >= 8) { bitCount -= 8; out += String.fromCharCode((bits >> bitCount) & 255); } }
      return out;
    }
    function validJwt(text) { var parts = text.split('.'); var head; var body; try { head = parts.length === 3 ? JSON.parse(decode(parts[0])) : null; body = parts.length === 3 ? JSON.parse(decode(parts[1])) : null; } catch (e) { return false; } return !!(head && body && typeof head.alg === 'string' && /^[A-Za-z0-9_-]+$/.test(parts[2] || '')); }
    function secretFormat(formats) { return hasFormat(formats, /(?:application\\/x-copyq-secret|org\\.nspasteboard\\.concealedtype|clipboard viewer ignore|x-kde-passwordmanagerhint|nspasteboard concealed|mimesecret|com\\.agilebits\\.onepassword|1password|bitwarden|keepass|lastpass|password-manager)/i); }
    function structuredNonSecret(text) {
      return isStandaloneUrl(text)
        || /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(text)
        || /^(?:[A-Za-z]:[\\\\/]|\\\\\\\\|\\/)[^\\r\\n]+$/.test(text)
        || /^v?\\d+\\.\\d+\\.\\d+(?:[-+][0-9A-Za-z.-]+)?$/.test(text)
        || /^[A-Z][A-Z0-9]{1,9}-\\d{1,10}$/.test(text);
    }
    function historicalOpaqueSecret(text) {
      var classes = 0;
      if (text.length < 10 || text.length > 150 || /\\s/.test(text)) return false;
      // A compact document is not one password. Embedded credential detection
      // runs separately; this exemption only narrows the generic guessing rule.
      if (/^[\\[{]/.test(text)) {
        try { if (typeof JSON.parse(text) === 'object') return false; } catch (e) {}
      }
      if (/[a-z]/.test(text)) classes += 1;
      if (/[A-Z]/.test(text)) classes += 1;
      if (/\\d/.test(text)) classes += 1;
      return classes >= 2;
    }
    function isHighConfidenceSecret(value, formats) {
      var text = trim(value);
      if (secretFormat(formats)) return true;
      if (/^-----BEGIN (?:[A-Z0-9 ]+ )?(?:PRIVATE KEY|OPENSSH PRIVATE KEY)-----/m.test(text) || validJwt(text)) return true;
      if (/^(?:sk-[A-Za-z0-9_-]{20,}|ghp_[A-Za-z0-9]{30,}|github_pat_[A-Za-z0-9_]{30,}|glpat-[A-Za-z0-9_-]{20,}|xox[baprs]-[A-Za-z0-9-]{12,}|AIza[0-9A-Za-z_-]{30,}|AKIA[0-9A-Z]{16}|npm_[A-Za-z0-9_-]{20,}|pypi-[A-Za-z0-9_-]{20,})$/.test(text)) return true;
      if (/^authorization:\\s*(?:bearer|basic|token)\\s+\\S{12,}$/i.test(text)) return true;
      if(/^\\s*(?:export\\s+)?(?:[A-Za-z_][A-Za-z0-9_]*(?:PASSWORD|TOKEN|SECRET|API_KEY)[A-Za-z0-9_]*|PASSWORD|TOKEN|SECRET|API_KEY)\\s*=\\s*\\S{12,}\\s*$/i.test(text)) return true;
      if (credentialUrl(text)) return true;
      if (/^[0-9a-f]{32,128}$/i.test(text)) return true;
      if (structuredNonSecret(text)) return false;
      return historicalOpaqueSecret(text);
    }
    function redactSecrets(value) {
      var original = String(value == null ? '' : value); var marker = '[REDACTED]';
      function replacement(value) { return !value || /^(?:\\[REDACTED\\]|\\$\\{[A-Za-z_][A-Za-z0-9_]*\\}|\\$env:[A-Za-z_][A-Za-z0-9_]*|process\\.env\\.[A-Za-z_][A-Za-z0-9_]*)$/.test(value) ? value : marker; }
      function urlPart(url) {
        url = url.replace(/^((?:https?|ftps?):\\/\\/[^/\\s:@]+:)([^/\\s@]+)(@)/i, function (_, before, password, after) { return before + replacement(password) + after; });
        url = url.replace(/([?&#](?:access_token|api[_-]?key|password|secret|token)=)([^&#\\s]+)/gi, function (_, before, secret) { return before + replacement(secret); });
        return url.replace(/^(https?:\\/\\/calendar\\.google\\.com\\/calendar\\/ical\\/[^\\s/]+\\/private-)[0-9a-f]{32}(\\/basic\\.ics(?:[?#]|$))/i, '$1' + marker + '$2');
      }
      function textPart(text) {
        text = text.replace(/-----BEGIN ((?:[A-Z0-9]+ )*PRIVATE KEY)-----[\\s\\S]*?(?:-----END \\1-----|$)/g, marker);
        text = text.replace(/(\\bauthorization\\s*:\\s*(?:bearer|basic|token)\\s+)([^\\s\"',;]+)/gi, function (_, before, secret) { return before + replacement(secret); });
        text = text.replace(/(\\b(?:[A-Za-z_][A-Za-z0-9_]*[_-])?(?:password|passwd|token|secret|api[_-]?key|client[_-]?secret|access[_-]?token)(?:[_-][A-Za-z0-9_]+)?[\"']?\\s*[:=]\\s*)(?:\"((?:\\\\.|[^\"\\\\])*)\"|'((?:\\\\.|[^'\\\\])*)'|(\\[REDACTED\\]|\\$\\{[A-Za-z_][A-Za-z0-9_]*\\}|[^\\s,;}\\]]+))/gi, function (_, before, doubleQuoted, singleQuoted, bare) {
          if (doubleQuoted !== undefined) return before + '\"' + replacement(doubleQuoted) + '\"';
          if (singleQuoted !== undefined) return before + \"'\" + replacement(singleQuoted) + \"'\";
          return before + replacement(bare);
        });
        text = text.replace(/\\b(?:sk-[A-Za-z0-9_-]{20,}|ghp_[A-Za-z0-9]{30,}|github_pat_[A-Za-z0-9_]{30,}|glpat-[A-Za-z0-9_-]{20,}|xox[baprs]-[A-Za-z0-9-]{12,}|AIza[0-9A-Za-z_-]{30,}|AKIA[0-9A-Z]{16}|npm_[A-Za-z0-9_-]{20,}|pypi-[A-Za-z0-9_-]{20,})\\b/g, marker);
        return text.replace(/\\b[A-Za-z0-9_-]+\\.[A-Za-z0-9_-]+\\.[A-Za-z0-9_-]+\\b/g, function (candidate) { return validJwt(candidate) ? marker : candidate; });
      }
      // Separate URL spans so token-like path components are never guessed at.
      var result = ''; var position = 0; var urls = /(?:https?|ftps?|file):\\/\\/[^\\s<>\"']+/gi; var match;
      while ((match = urls.exec(original))) { result += textPart(original.slice(position, match.index)) + urlPart(match[0]); position = urls.lastIndex; }
      result += textPart(original.slice(position));
      return { text: result, redacted: result !== original };
    }

    // Shared safety decision for integrated routing and protection-only installs.
    // Destination classification and frequency state intentionally live elsewhere.
    function inspectClipboard(input) {
      var text = String(input && input.text != null ? input.text : '');
      var formats = input && input.formats || [];
      if (secretFormat(formats)) return { action: 'ignored', frequencyEligible: false };
      var redaction = redactSecrets(text);
      var standalone = trim(redaction.text).replace(/[\\u200b\\u200e\\u200f\\u2066-\\u2069]/g, '') === '[REDACTED]' || (!/[\\r\\n]/.test(trim(text)) && /^(?:authorization\\s*:|(?:export\\s+)?[A-Za-z_][A-Za-z0-9_]*\\s*=)/i.test(trim(text)) && isHighConfidenceSecret(text));
      if (standalone || (!redaction.redacted && isHighConfidenceSecret(text))) return { action: 'ignored', frequencyEligible: false };
      if (redaction.redacted) return { action: 'redacted', frequencyEligible: false, redactedText: redaction.text };
      if (hasFormat(formats, /^image\\//i) || (input && input.mimeOwner && formats.indexOf(input.mimeOwner) >= 0) || (input && input.mimeHidden && formats.indexOf(input.mimeHidden) >= 0)) return { action: 'passthrough', frequencyEligible: false };
      return { action: 'allowed', frequencyEligible: trim(text) !== '' };
    }

    module.exports = { validJwt: validJwt, secretFormat: secretFormat, isHighConfidenceSecret: isHighConfidenceSecret, redactSecrets: redactSecrets, inspectClipboard: inspectClipboard };

        return module.exports;
      }());
      for (var key in modules.secrets) if (Object.prototype.hasOwnProperty.call(modules.secrets, key)) api[key] = modules.secrets[key];
      modules.artifacts = (function () {
        var module = { exports: {} };
        function require(id) { switch (id) { case './common': return modules.common; default: throw new Error('UNDECLARED_CORE_IMPORT'); } }
    \"use strict\";

    var common = require(\"./common\");
    var trim = common.trim;

    function isArtifact(value) {
      var text = trim(value); var parsed; var lines; var matches;
      if (!text) return false;
      if (/^[\\[{]/.test(text)) {
        try {
          parsed = JSON.parse(text);
          if (Object.prototype.toString.call(parsed) === '[object Array]') return parsed.length >= 2;
          if (parsed && Object.prototype.toString.call(parsed) === '[object Object]') return Object.keys(parsed).length >= 2;
        } catch (e) {}
      }
      if (!/[\\r\\n]/.test(text)) return false;
      lines = text.split(/\\r?\\n/).filter(function (line) { return trim(line) !== ''; });
      if (lines.length < 2) return false;
      if (/^diff --git\\s/m.test(text) || /^@@\\s+-\\d+(?:,\\d+)?\\s+\\+\\d+(?:,\\d+)?\\s+@@/m.test(text)) return true;
      if (/---\\s+backtrace\\s+---/i.test(text) || /^Traceback \\(most recent call last\\):/m.test(text)) return true;
      matches = text.match(/^\\s*at\\s+\\S.+$/gm); if (matches && matches.length >= 2) return true;
      // Bare # and > also introduce Markdown headings and quotations. Require an
      // unambiguous prompt ($, PowerShell, or user@host) for transcript routing.
      if (/^(?:PS\\s+[A-Za-z]:\\\\[^>\\r\\n]*>|\\$|[\\w.-]+@[\\w.-]+:[^\\r\\n]*?[$#])\\s*\\S/m.test(text)) return true;
      if (/`\\s*$/m.test(text) && /^\\s{2,}-(?:\\w|-)\\S*/m.test(text)) return true;
      matches = text.match(/^(?:\\[[0-2]?\\d:[0-5]\\d:[0-5]\\d\\]|\\d{4}-\\d{2}-\\d{2}[T ][^\\s]+|\\s*(?:TRACE|DEBUG|INFO|WARN|ERROR|FATAL)\\b).+$/gim);
      if (matches && matches.length >= 2) return true;
      if (/^RELEASE_STAGE_FAILED:/m.test(text) && /(?:reason=|backtrace)/i.test(text)) return true;
      matches = text.match(/^\\s*(?:\\[[^\\]\\r\\n]+\\]|[A-Za-z_][A-Za-z0-9_.-]*\\s*(?:=|:)\\s*\\S.*)$/gm);
      return !!(matches && matches.length >= 3);
    }

    module.exports = { isArtifact: isArtifact };

        return module.exports;
      }());
      for (var key in modules.artifacts) if (Object.prototype.hasOwnProperty.call(modules.artifacts, key)) api[key] = modules.artifacts[key];
      modules.code = (function () {
        var module = { exports: {} };
        function require(id) { switch (id) {  default: throw new Error('UNDECLARED_CORE_IMPORT'); } }
    \"use strict\";


    function isCode(text) { return /^\\s*(?:import|export|var|const|let|function|class|def|package|using|namespace|#include|#!)\\b/m.test(text) || /(?:=>|===|!==|&&|\\|\\|)/.test(text) || /\\b(?:if|for|while|switch|catch)\\s*\\(/.test(text) || /\\w+\\s*\\([^)]*\\)\\s*\\{/.test(text); }

    module.exports = { isCode: isCode };


        return module.exports;
      }());
      for (var key in modules.code) if (Object.prototype.hasOwnProperty.call(modules.code, key)) api[key] = modules.code[key];
      modules.routing = (function () {
        var module = { exports: {} };
        function require(id) { switch (id) { case './common': return modules.common; case './secrets': return modules.secrets; case './urls': return modules.urls; case './artifacts': return modules.artifacts; case './code': return modules.code; default: throw new Error('UNDECLARED_CORE_IMPORT'); } }
    \"use strict\";

    var common = require(\"./common\");
    var trim = common.trim;
    var hasFormat = common.hasFormat;
    var secrets = require(\"./secrets\");
    var inspectClipboard = secrets.inspectClipboard;
    var urls = require(\"./urls\");
    var isStandaloneUrl = urls.isStandaloneUrl;
    var artifacts = require(\"./artifacts\");
    var isArtifact = artifacts.isArtifact;
    var code = require(\"./code\");
    var isCode = code.isCode;

    function route(input) {
      var text = String(input && input.text != null ? input.text : '');
      var policy = inspectClipboard(input); var eligible = policy.frequencyEligible;
      if (policy.action === 'ignored' || policy.action === 'passthrough') return policy;
      if (policy.action === 'redacted') {
        var action = text.length >= 5000 ? 'big' : isStandaloneUrl(text) ? 'url' : isArtifact(policy.redactedText) ? 'artifacts' : isCode(policy.redactedText) ? 'code' : 'default';
        return { action: action, frequencyEligible: false, redactedText: policy.redactedText };
      }
      if (text.length >= 5000) return { action: 'big', frequencyEligible: eligible };
      if (isStandaloneUrl(text)) return { action: 'url', frequencyEligible: eligible };
      if (isArtifact(text)) return { action: 'artifacts', frequencyEligible: eligible };
      if (isCode(text)) return { action: 'code', frequencyEligible: eligible };
      return { action: 'default', frequencyEligible: eligible };
    }

    module.exports = { route: route };

        return module.exports;
      }());
      for (var key in modules.routing) if (Object.prototype.hasOwnProperty.call(modules.routing, key)) api[key] = modules.routing[key];
      modules.frequency = (function () {
        var module = { exports: {} };
        function require(id) { switch (id) { case './common': return modules.common; default: throw new Error('UNDECLARED_CORE_IMPORT'); } }
    \"use strict\";

    var common = require(\"./common\");
    var trim = common.trim;

    function fnv1a(text) { var hash = 2166136261; var i; for (i = 0; i < text.length; i += 1) { hash ^= text.charCodeAt(i); hash = (hash * 16777619) >>> 0; } return (hash >>> 0).toString(16); }
    function djb2(text) { var hash = 5381; var i; for (i = 0; i < text.length; i += 1) hash = ((hash << 5) + hash + text.charCodeAt(i)) | 0; return (hash >>> 0).toString(36); }
    function legacyKiloHash(text) { text = String(text == null ? '' : text); return text.length + '_' + djb2(text); }
    function normalizeFrequencyText(text) { return trim(text); }
    function v2HashesFor(text) { text = String(text == null ? '' : text); return { version: 2, first: fnv1a(text), second: djb2(text), key: 'v2:' + fnv1a(text) + ':' + djb2(text) }; }
    function hashesFor(text) { text = normalizeFrequencyText(text); return { version: 3, first: fnv1a(text), second: djb2(text), key: 'v3:' + fnv1a(text) + ':' + djb2(text) }; }
    function cloneCounters(counters) { var copy = {}; var key; for (key in counters || {}) if (Object.prototype.hasOwnProperty.call(counters, key)) copy[key] = { version: counters[key].version, first: counters[key].first, second: counters[key].second, count: counters[key].count, lastUsed: counters[key].lastUsed }; return copy; }
    function pruneCounters(state, maximum) {
      var keys = []; var key;
      for (key in state.counters) if (Object.prototype.hasOwnProperty.call(state.counters, key)) keys.push(key);
      keys.sort(function (a, b) { return (state.counters[a].lastUsed - state.counters[b].lastUsed) || (a < b ? -1 : 1); });
      while (keys.length > maximum) delete state.counters[keys.shift()];
    }
    function recordFrequency(state, v2State, legacy, text) {
      var next = { version: 3, counters: cloneCounters((state || {}).counters), clock: Number((state || {}).clock || 0) };
      var canonicalText = normalizeFrequencyText(text); var hash = hashesFor(canonicalText); var oldHash = v2HashesFor(canonicalText); var oldKey = legacyKiloHash(canonicalText); var entry; var imported = 0;
      if (!canonicalText) return { state: next, result: { count: 0, promote: false, key: '', canonicalText: '', hashes: hash } };
      entry = next.counters[hash.key];
      if (!entry) {
        if (v2State && v2State.counters && v2State.counters[oldHash.key]) imported += Number(v2State.counters[oldHash.key].count || 0);
        if (legacy && legacy[oldKey] !== undefined) imported += Number(legacy[oldKey] || 0);
        entry = { version: 3, first: hash.first, second: hash.second, count: imported, lastUsed: 0 };
      }
      entry.count += 1; entry.lastUsed = ++next.clock; next.counters[hash.key] = entry;
      pruneCounters(next, 4096);
      return { state: next, result: { count: entry.count, promote: entry.count >= 6, key: hash.key, canonicalText: canonicalText, hashes: hash } };
    }
    function dismissFrequency(state, key) {
      var next = { version: 3, counters: cloneCounters((state || {}).counters), clock: Number((state || {}).clock || 0) }; var entry = next.counters[key]; var parts = String(key || '').split(':'); var savedCount = 0;
      if (!entry) entry = { version: 3, first: parts[1] || '', second: parts[2] || '', count: 0, lastUsed: 0 };
      savedCount = Number(entry.count || 0); entry.count = 0; entry.lastUsed = ++next.clock; next.counters[key] = entry; pruneCounters(next, 4096);
      return { state: next, savedCount: savedCount };
    }
    function restoreFrequency(state, key, savedCount) {
      var next = { version: 3, counters: cloneCounters((state || {}).counters), clock: Number((state || {}).clock || 0) }; var parts = String(key || '').split(':'); var entry = next.counters[key];
      if (!entry) entry = { version: 3, first: parts[1] || '', second: parts[2] || '', count: 0, lastUsed: 0 };
      entry.count = Number(entry.count || 0) + Number(savedCount || 0); entry.lastUsed = ++next.clock; next.counters[key] = entry; pruneCounters(next, 4096);
      return { state: next, count: entry.count };
    }
    function sortFrequent(items) { return items.slice().sort(function (a, b) { return (b.count - a.count) || (a.order - b.order); }); }

    module.exports = { legacyKiloHash: legacyKiloHash, normalizeFrequencyText: normalizeFrequencyText, v2HashesFor: v2HashesFor, hashesFor: hashesFor, recordFrequency: recordFrequency, dismissFrequency: dismissFrequency, restoreFrequency: restoreFrequency, sortFrequent: sortFrequent };


        return module.exports;
      }());
      for (var key in modules.frequency) if (Object.prototype.hasOwnProperty.call(modules.frequency, key)) api[key] = modules.frequency[key];
      return api;
    }());
    var LEGACY_KEY = 'frequent_usage_counts'; var V2_KEY = 'frequent_usage_counts_v2'; var V3_KEY = 'frequent_usage_counts_v3'; var FREQ_KEY_MIME = 'application/x-copyq-user-frequency-key'; var FREQ_COUNT_MIME = 'application/x-copyq-user-frequency-count';
    function settingsObject(key, fallback) { try { var value = settings(key); return value ? JSON.parse(value) : fallback; } catch (e) { return fallback; } }
    var handlers = commands().filter(function (c) { return c.automatic && (c.internalId === 'canonical.dispatcher' || c.internalId === 'canonical.secret-protection'); }); if (handlers.length > 1) { try { notification('.id', 'secret-conflict', '.message', 'SECRET_HANDLER_CONFLICT'); } finally { ignore(); } abort(); }
    var formats = dataFormats(); var textData = data(mimeText); var text = str(textData);
    var routed = CopyQCore.route({ text: text, formats: formats, mimeOwner: mimeOwner, mimeHidden: mimeHidden });
    if (routed.action === 'ignored') { try { notification('.id', 'secret-ignore', '.title', 'Ignoring secret in the clipboard', '.message', 'SECRET_IGNORED'); } finally { ignore(); } abort(); } if (routed.action === 'passthrough' && !routed.frequencyEligible) abort(); if (!textData) abort();
    if (routed.redactedText !== undefined) { try { for (var i = 0; i < formats.length; i += 1) { if (formats[i] !== mimeOutputTab && formats[i] !== 'application/x-copyq-clipboard-mode') removeData(formats[i]); } if (!setData(mimeText, routed.redactedText)) throw new Error('REDACTION_WRITE_FAILED'); text = routed.redactedText; textData = data(mimeText); } catch (error) { try { notification('.id', 'secret-ignore', '.message', 'SECRET_REDACTION_FAILED'); } finally { ignore(); } abort(); } try { notification('.id', 'secret-redacted', '.title', 'Secrets removed from history', '.message', 'SECRET_REDACTED'); } catch (noticeError) {} }
    var frequency = null; if (routed.frequencyEligible) { var state = settingsObject(V3_KEY, { version: 3, counters: {}, clock: 0 }); var v2 = settingsObject(V2_KEY, { version: 2, counters: {}, clock: 0 }); var legacy = settingsObject(LEGACY_KEY, {}); frequency = CopyQCore.recordFrequency(state, v2, legacy, text); settings(V3_KEY, JSON.stringify(frequency.state)); }
    if (frequency && frequency.result.promote) { var originalTab = selectedTab(); var key = frequency.result.key; var frequentItem = {}; frequentItem[mimeText] = frequency.result.canonicalText; frequentItem[FREQ_KEY_MIME] = key; frequentItem[FREQ_COUNT_MIME] = String(frequency.result.count); var escapedKey = key.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&'); tab('Frequent'); var existing = ItemSelection('Frequent').select(new RegExp('^' + escapedKey + '$'), FREQ_KEY_MIME); if (existing.length) { existing.setItemAtIndex(0, frequentItem); existing.move(0); } else { insert(0, frequentItem); } tab(originalTab); }
    if (routed.action === 'big') { setData(mimeOutputTab, 'BIG'); abort(); } if (routed.action === 'url') { setData(mimeOutputTab, '&URLs'); abort(); } if (routed.action === 'artifacts') { setData(mimeOutputTab, 'Artifacts'); abort(); } if (routed.action === 'code') { setData(mimeOutputTab, 'Code'); abort(); } if (routed.action === 'passthrough') abort();"
Automatic=true
InternalId=canonical.dispatcher
