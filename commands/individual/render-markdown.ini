[Command]
Name=Render Markdown
Command="
    copyq:
    var CopyQCore = (function () {
      var modules = {};
      var api = {};
      modules.markdown = (function () {
        var module = { exports: {} };
        function require(id) { switch (id) {  default: throw new Error('UNDECLARED_CORE_IMPORT'); } }
    \"use strict\";


    function isMarkdown(value) {
      var text = String(value == null ? '' : value); var listItems;
      if (!text) return false;
      if (/(^|\\n) {0,3}#{1,6}[ \\t]+\\S/.test(text)) return true;
      if (/(^|\\n) {0,3}```[^\\n]*\\n[\\s\\S]*\\n {0,3}```/.test(text)) return true;
      if (/\\[[^\\]\\n]+\\]\\((?:https?:\\/\\/|\\/|#|\\.{0,2}\\/)[^)]+\\)/.test(text)) return true;
      if (/(^|\\n) {0,3}>[ \\t]+\\S/.test(text)) return true;
      if (/(^|\\n)\\s*\\|?.+\\|.+\\n\\s*\\|?\\s*:?-{3,}:?\\s*\\|/.test(text)) return true;
      listItems = text.match(/(^|\\n) {0,3}(?:[-+*]|\\d+[.)])[ \\t]+\\S/g);
      return !!(listItems && listItems.length >= 2);
    }

    module.exports = { isMarkdown: isMarkdown };


        return module.exports;
      }());
      for (var key in modules.markdown) if (Object.prototype.hasOwnProperty.call(modules.markdown, key)) api[key] = modules.markdown[key];
      return api;
    }());
    var text = data(mimeText); if (!text || !CopyQCore.isMarkdown(str(text))) abort();
    var appData = str(env('APPDATA')).replace(/\\\\/g, '/'); var result = null; try { result = execute('marked.cmd', null, text); } catch (e) {} if (!result || result.exit_code !== 0) { try { result = execute(appData + '/npm/marked.cmd', null, text); } catch (e2) {} }
    if (!result || result.exit_code !== 0) { notification('.id', 'markdown', '.message', 'MARKDOWN_FAILED'); abort(); } setData(mimeHtml, result.stdout);"
Input=text/plain
Automatic=true
InMenu=true
InternalId=canonical.markdown-render
